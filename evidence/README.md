# Evidence manifests

只提交小型 JSON manifest，不提交大型 log、音訊、dump、driver package 或 binary。manifest 必須記錄
issue、scope digest、environment fingerprint、commands、results、limitations、tool versions 與時間。
scope digest 不包含 evidence 自己；換設備後舊 evidence 只代表原先的 environment class。
