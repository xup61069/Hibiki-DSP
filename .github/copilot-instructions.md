<!-- GENERATED_FROM: AGENTS.md -->
請先讀取 repository root 的 `AGENTS.md` 與 `docs/START_HERE.md`；取得指派後只載入對應 Issue
handoff 與它指定的 Spec／ADR，不預載全域歷史。本檔不建立第二套規則；產品行為、架構決策與
續作狀態以 Spec、ADR、source、tests、evidence 與 handoff 為準。提交前依 `AGENTS.md` 的
scope-triggered gates 驗證。
