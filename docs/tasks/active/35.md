---
schema_version: 2
issue: 35
branch: codex/35-timeline-persistence
target_branch: main
base_commit: 2e77175e92e0fd952319772f2b8123262dc34821
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-msvc-buildtools-17.14-msvc-19.44-dotnet-10.0.301-pwsh-7.6.5; target-24h2-vs2026-wdk-missing
scope_globs: ["vst-host/include/hibiki/vst3_timeline_persistence.hpp", "vst-host/src/vst3_timeline_persistence.cpp", "vst-host/CMakeLists.txt", "tests/unit/contract_tests.cpp", "docs/specs/SPEC-0008-vst3-sandbox.md", "evidence/0000-foundation/vst3-timeline-persistence-v1.json", "docs/tasks/active/35.md"]
shared_paths: ["vst-host/CMakeLists.txt", "tests/unit/contract_tests.cpp", "docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0008]
adrs: []
next_safe_action: "Implement canonical timeline serialize/parse with fail-closed parser and contract tests."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 35", "pwsh -File tools/verify.ps1", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 35 handoff

## Objective

Implement the persistence boundary SPEC-0008 documents but nothing implements:
bounded canonical serialization/parsing for `vst3-parameter-timeline-v1.schema.json`
in the vst-host lane, with a strict fail-closed reader (canonical shape + insignificant
whitespace only), byte-stable round-trips for validator-passing snapshots, bounded
input caps, and write-temp-then-replace file helpers. No general JSON library is added.

## Acceptance

- Canonical serialize/parse round-trips validator-passing snapshots byte-stably.
- Malformed inputs (unknown/duplicate keys, wrong types, non-finite/out-of-range
  numbers, wrong array length, truncation, oversized input) are rejected with distinct
  bounded error codes and leave the destination snapshot untouched.
- File save/load works on the Windows control plane and fails closed on missing or
  corrupt files.
- Contract tests cover all cases; local CMake/CTest passes.
- `docs/state/BASELINE.md` edits limited to the two counters gated by docs-check
  (tracked paths, repository JSON files); narrative stays integrator-owned.

## Completed

- Survey confirmed zero implementations reference
  `schemas/vst3-parameter-timeline-v1.schema.json` (docs/schema only).
- Claimed Issue 35 with an isolated worktree from `origin/main` (`2e77175`).
  No overlap: open claims are Issue #30 doctor fix (codex/30 branch) and codex/32
  extension CSP gate.

## Known limitations

- Reader accepts only the writer-canonical document shape; it is not a general JSON
  parser by design.
- User-space source-contract work only; proves nothing about physical audio,
  per-App delivery, drivers, HLK or signing.
- No UI/IPC consumer yet; persistence is callable control-plane API.

## Last verification

- Baseline not yet run at claim time; gates will be updated before the PR leaves
  draft.

## Remaining work

1. Add serialization header/source and register in vst-host CMake.
2. Extend contract tests with round-trip and malformed-input coverage.
3. Update SPEC-0008 wording; record evidence; bump BASELINE counters per docs-check.
4. Run local gates plus CMake/CTest; wait for GitHub Actions.

## Next safe action

Implement canonical timeline serialize/parse with fail-closed parser and contract
tests.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 35
pwsh -File tools/verify.ps1
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
