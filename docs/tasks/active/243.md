---
schema_version: 2
issue: 243
branch: opencode/243-source-policy-selftest
target_branch: main
base_commit: 4f5e2c6f975970fcaf1aa371dca0907d433d04c5
status: planned
role: worker
owner: opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: pwsh-7.6.5 windows
scope_globs: ["tools/source-policy.ps1", "evidence/0000-foundation/source-policy-selftest-v1.json", "docs/tasks/active/243.md"]
shared_paths: []
depends_on: []
specs: []
adrs: []
next_safe_action: "Implement -SelfTest in tools/source-policy.ps1, then run the self-test and required gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 243", "pwsh -File tools/source-policy.ps1 -SelfTest"]
---

# Issue 243 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/source-policy.ps1` that validates the
source-only policy gate's blocked-extension, secret-pattern, and PE-header
boundaries plus fail-closed error handling, without touching repository files or
claiming any artifact/signing delivery. The existing main-path check must keep
its observable pass/fail behavior unchanged.

## Acceptance

- Main source-policy check continues to run the current deterministic validation
  and keeps its existing success/failure contract (same output string and exit code).
- `pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest` runs bounded,
  deterministic cases without writing repository files, compiled artifacts,
  endpoint identity, or private paths.
- Self-test covers: clean pass on a scanned directory, blocked extension
  (`.exe`), secret pattern, PE-header (non-blocked extension), tracked git scope
  pass, and nested-directory scan.
- Add anonymous evidence `evidence/0000-foundation/source-policy-selftest-v1.json`
  recording that this is source-policy validation only, not physical artifact
  signing or driver/delivery evidence.
- Run handoff, docs, source-policy, source-only, and `git diff --check` gates;
  keep every gate fail-closed.

## Completed

- 尚未開始。

## Known limitations

- Self-test validates gate logic only; it is not physical artifact signing,
  driver, or delivery evidence.
- If `git` is unavailable in the environment, the tracked-scope case falls back
  to the scanned path (still exercised by other cases).

## Last verification

- 尚未執行 baseline。

## Remaining work

1. Refactor `tools/source-policy.ps1` core checks into a `Test-SourcePolicy`
   function parameterized by a root path.
2. Add `[switch]$SelfTest` and a bounded self-test block using temp directories
   outside the repository.
3. Run `pwsh -File tools/source-policy.ps1 -SelfTest` until green.
4. Write anonymous evidence JSON.
5. Run required gates and push claim commit + draft PR.

## Next safe action

Implement -SelfTest in tools/source-policy.ps1, then run the self-test and required gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 243
pwsh -File tools/source-policy.ps1 -SelfTest
```
