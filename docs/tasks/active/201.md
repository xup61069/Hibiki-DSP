---
schema_version: 2
issue: 201
branch: codex/201-portcls-adapter-entry
target_branch: main
base_commit: 036b846b33ae9737e739856eb008a7f8eff9cc73
status: in_progress
role: worker
owner: antigravity
updated_at: 2026-08-22T16:45:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["driver/wdk/**", "tools/driver-source-check.ps1", "evidence/0000-foundation/driver-portcls-adapter-entry-v1.json", "docs/tasks/active/201.md", "build/baseline-counters.json"]
shared_paths: []
depends_on: []
specs: [SPEC-0003]
adrs: [ADR-0002, ADR-0004]
next_safe_action: "Implement PortCls adapter driver entry and subdevice registration in driver/wdk and verify source gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 201", "pwsh -File tools/driver-source-check.ps1"]
---

# Issue 201 handoff: PortCls WaveRT Adapter Driver Entry and Subdevice Registration

## Objective

Provide the MS-PL PortCls adapter driver entry (`DriverEntry`), device object creation (`AddDevice`), subdevice registration (`PcRegisterSubdevice` for WaveRT miniports/ports), and PNP/Power dispatch in `driver/wdk/` that wires all four endpoints (Main, Low Latency, Surround 7.1, Virtual Mic) into the standard Windows audio driver life cycle without linking GPL user-space code.

## Acceptance

- Implement `DriverEntry` initializing PortCls driver object and registering `AddDevice` entry point.
- Implement `HibikiAddDevice` creating audio adapter device extension and registering adapter with PortCls.
- Implement `HibikiStartDevice` calling `PcNewPort` (`CLSID_PortWaveRT`), initializing `HibikiMiniportWaveRtV1` for each endpoint, and registering subdevices via `PcRegisterSubdevice` with unique endpoint names.
- Implement PNP and Power dispatch handlers (`IRP_MN_START_DEVICE`, `IRP_MN_STOP_DEVICE`, `IRP_MN_REMOVE_DEVICE`, `PowerDeviceD0`, `PowerDeviceD3`).
- Retain strict MS-PL licensing, non-blocking real-time audio paths, and zero GPL user-space linkage.
- Update `tools/driver-source-check.ps1` to enforce adapter driver entry coverage.
- Pass all local gates and record anonymous evidence.

## Completed

- Claimed Issue #201 and initialized dedicated branch `codex/201-portcls-adapter-entry`.

## Known limitations

- Source-only; this machine does not have WDK 10.0.28000.2526 installed and no `.sys` binary, HLK test or Microsoft signature is produced.

## Last verification

- Handoff schema initialized.

## Remaining work

1. Implement `driver/wdk/hibiki_adapter.h` and `driver/wdk/hibiki_adapter.cpp`.
2. Update `driver/wdk/README.md`.
3. Update `tools/driver-source-check.ps1`.
4. Generate `evidence/0000-foundation/driver-portcls-adapter-entry-v1.json`.
5. Run all mandatory gates, refresh `build/baseline-counters.json` via `-WriteCounters`, commit and push.

## Next safe action

Implement PortCls adapter driver entry and subdevice registration in driver/wdk and verify source gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 201
pwsh -File tools/driver-source-check.ps1
```
