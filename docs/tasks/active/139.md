---
schema_version: 2
issue: 139
branch: codex/139-readme-road-to-v1
target_branch: main
base_commit: 786ca24b975f5e8654ceece6cda922e8f54f524e
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5; target-24h2-vs2026-wdk-missing
scope_globs: ["evidence/0000-foundation/readme-road-to-v1-v1.json", "docs/tasks/active/139.md"]
shared_paths: ["README.md"]
depends_on: []
specs: [SPEC-0005]
adrs: []
next_safe_action: "Add the road-to-V1 milestone section to README with blocker types."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 139", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1", "git diff --check"]
---

# Issue 139 handoff

## Objective

Add an honest 邁向 V1 section to root `README.md`: V1 definition per SPEC-0005,
milestones M0–M4 in dependency order, current status, and blocker types
(工程/環境/行政). Core message from BASELINE: control-plane/DSP largely ready;
critical path = M0 target machine → M2 PortCls wiring into a signed .sys → physical
soak → signed installer delivery. No dates; M0 is outside repository control.

## Acceptance

- V1 definition sourced (SPEC-0005/BASELINE); milestone table with dependency order
  and blocker types; plain-language assessment naming PortCls wiring as the biggest
  engineering gap and target machine + signing accounts as the biggest non-engineering
  gaps.
- All existing README content preserved; every pending item keeps its non-claim wording.

## Completed

- Claimed Issue 139 with an isolated worktree from `origin/main` (`786ca24`).

## Known limitations

- Assessment reflects repository documents at base commit `786ca24`; no dates are
  promised because M0 provisioning is external.

## Last verification

- Baseline not yet run at claim time; gates will be updated before the PR leaves
  draft.

## Remaining work

1. Add the road-to-V1 section and TOC entry.
2. Run all gates locally; record evidence; wait for GitHub Actions.

## Next safe action

Add the road-to-V1 milestone section to README with blocker types.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 139
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
git diff --check
```
