---
schema_version: 2
issue: 182
branch: codex/182-counters-ownership-fix
target_branch: main
base_commit: 4b6ff2f6b69d7d0a3317a7a6c203938b896ea5ad
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T15:50:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; cmake-missing-wdk-missing-portable-user-space
scope_globs: ["tools/docs-check.ps1", "build/baseline-counters.json", "evidence/0000-foundation/docs-check-counters-ownership-fix-v1.json", "docs/tasks/active/182.md"]
shared_paths: []
depends_on: [137]
specs: [SPEC-0004]
adrs: []
next_safe_action: "Implement the base-has-counters guard, extend -SelfTest fixtures, refresh counters JSON, run local gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/handoff-check.ps1 -Issue 182", "pwsh -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 182 handoff

## Objective

修復 PR #168 引入的 docs-check PR-mode fail-open：`if ($countersChangedByHead)` 提前
exit 0 使任何對 `build/baseline-counters.json` 的編輯都免於 merge-base 一致性檢查與
head 嚴格驗證（尾部 ownership 斷言成為死碼）。將「首次引入」容忍限縮為
merge base 上確實沒有該檔案的情況，其餘 counters 編輯一律落入既有嚴格路徑。

## Acceptance

- `countersChangedByHead` 且 merge base 存在該檔 → 不再提前 exit；依序執行 base 一致性
  檢查與 head 嚴格斷言，數字不符即 fail-closed。
- `countersChangedByHead` 且 merge base 無該檔（歷史上的 first-introduction）→ 保留容忍，
  訊息不變。
- 新增 bounded 純 helper（如 `Test-CountersIntroductionCase`）與 `-SelfTest` cases：
  introduction tolerated、edit-with-base-present drift rejected、exact-match accepted；
  既有 18 cases 全數保留通過。
- 本 slice 新增 tracked 檔案，implementation commit 內含以 `-WriteCounters` 重生的
  `build/baseline-counters.json`（新制 dogfooding）。
- Required local gates pass；GitHub Actions verify pass。

## Completed

- Read AGENTS.md / START_HERE.md / AI_HANDOFF.md / MULTI_AGENT.md；synced main 至 `4b6ff2f`。
- Triple-checked claim：無 codex/182* branch 佔位、無 linked PR、active handoffs 無人宣稱
  `tools/docs-check.ps1`（2026-08-22T15:48+08:00 掃描）。
- Root cause 定位：PR #168 的 78bfcd1 將 introduction 容忍寫成對任意 counters 編輯生效。

## Known limitations

- Tooling/governance only；不宣稱 RT audio、driver、簽章或任何產品行為。
- Push-to-main strict 路徑本就正確，本修正僅影響 PR-mode 判定順序。

## Last verification

- Baseline on base commit：`docs-check.ps1 -SelfTest` 18 cases pass；完整 gate green
  （85 required / 24 specs / counters JSON 機制運作中）。

## Remaining work

1. 實作 base-has-counters guard + helper + self-test cases。
2. `-WriteCounters` 重生 counters JSON（含本 slice 新檔）。
3. Local gates 全綠後 push、開 PR、CI 綠燈轉 ready。

## Next safe action

Implement the guard and self-test fixtures in tools/docs-check.ps1, then refresh build/baseline-counters.json via -WriteCounters.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/handoff-check.ps1 -Issue 182
pwsh -File tools/docs-check.ps1
git diff --check
```
