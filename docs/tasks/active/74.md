---
schema_version: 2
issue: 74
branch: codex/74-preview-launcher-selftest
target_branch: main
base_commit: 916ceba1a7075f79a4884b823b502a5add370091
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T06:05:00+08:00
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["tools/run-preview.ps1", "evidence/0000-foundation/preview-launcher-selftest-v1.json", "docs/tasks/active/74.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0010]
adrs: []
next_safe_action: "Extract runtime/UI-resolution/argument functions in run-preview.ps1, add bounded -SelfTest cases and evidence JSON, stage everything before measuring BASELINE counters."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 74", "pwsh -File tools/run-preview.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 74 handoff

## Objective

延續 fail-closed 自測 wave：`tools/run-preview.ps1`（README 與 `Start-HibikiPreview.cmd` 的
共用入口）新增 bounded `-SelfTest`，以 in-memory fixtures 驗證三段純決策邏輯——
Windows App Runtime 1.7 x64 資格判定、UI 解析（Auto/DesktopCompat/WinUICompat 含無
runtime 時 fail-closed）、Engine Preview 參數建構——不啟動任何 process、不觸發 build、
不改動 repository 檔案。

## Acceptance

- runtime 判定、UI 解析、參數建構重構為可重用函式；main path 行為等價
  （含 Auto 選擇輸出訊息只在 `-Ui Auto` 時出現）。
- `-SelfTest` 覆蓋：完整選擇矩陣；明確 WinUICompat 無 runtime 時以可行動訊息 fail-closed；
  版本邊界（>=7000.456.1632.0，含等於）；x86／非 Ok 狀態過濾；多 package fallback；
  八種 engine flag 組合產生精確有序 tokens；計數輸出並 exit 0。
- SelfTest 不啟動 process、不呼叫 build script。
- 新增匿名 evidence JSON；BASELINE 於所有檔案 staged 後量測刷新。
- 本機 gates（含新 `-SelfTest`）通過；GitHub Actions verify 全綠後才請求 review。

## Completed

- Fetch 最新 origin/main（`916ceba`）；盤點 open work：#30/#39/#41/#43/#46/#50/#51/#52/
  #55(本 owner)/#56/#60/#64/#66/#70/#72 全部有 PR 或遠端 branch live claim。確認
  `run-preview.ps1` 無任何 owner 涉及（#52 屬 `build-preview.ps1`，不同檔案）。
- 建立 Issue #74、指派 `xup61069`、獨立 worktree／branch／本 handoff。

## Known limitations

- launcher 的 process orchestration（Start-Process/Wait-Process/cleanup）屬副作用邊界，
  故意不在 SelfTest 內偽造；覆蓋限純決策函式。
- SelfTest fixtures 與 production gate 共用版本常數與 flag tokens；變更時必須同步更新。
- CHANGE_CONTRACT 歸類：只新增既有啟動器行為的測試覆蓋，無新 invariant／API／建置鏈變更。

## Last verification

- 尚未執行；將於實作 commit 後補記錄本機 gates 與 CI runs。

## Remaining work

1. 抽出 `Test-WindowsAppRuntimePackages`／`Resolve-SelectedUi`／`Get-EngineArguments` 函式。
2. 加入 `-SelfTest` cases 與 evidence JSON；staged 後刷新 BASELINE 計數。
3. 跑本機 gates；push 後等 GitHub Actions 全綠轉 `ready_for_review`。

## Next safe action

Extract runtime/UI-resolution/argument functions in run-preview.ps1, add bounded -SelfTest cases and evidence JSON, stage everything before measuring BASELINE counters.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 74
pwsh -File tools/run-preview.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
