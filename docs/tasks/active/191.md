---
schema_version: 2
issue: 191
branch: codex/191-winui-m1-target
target_branch: main
base_commit: ad4e419494fd3517f1ed7bf0a4ad3fbe6f0079b7
status: planned
role: worker
owner: xup61069
updated_at: 2026-08-22T16:25:00+08:00
environment_fingerprint: sandbox-linux-amd64-pwsh-unavailable; target-win11-24h2-vs2026-sdk-wdk-10.0.28000.2526-unavailable
scope_globs: ["apps/winui-shell/**", "tools/build-preview.ps1", "evidence/0000-foundation/winui-m1-formal-build-v1.json", "docs/tasks/active/191.md", "build/baseline-counters.json"]
shared_paths: ["docs/AI_HANDOFF.md", "docs/state/BASELINE.md", "docs/PROJECT_MAP.md", "README.md", "docs/tasks/active/0.md"]
depends_on: []
specs: [SPEC-0005]
adrs: [ADR-0001]
next_safe_action: "On the locked Windows 11 24H2+ target machine, run the baseline commands and reproduce the formal WinUI XAML build failure before changing source."
resume_commands: ["git status --short --branch", "pwsh -File tools/doctor.ps1 -CheckOnly", "pwsh -File tools/handoff-check.ps1 -Issue 191", "pwsh -File tools/build-preview.ps1 -Target WinUI", "pwsh -File tools/winui-shell-check.ps1"]
---

# Issue 191 handoff: formal WinUI XAML build and accessibility smoke

## Objective

Re-implement the formal WinUI M1 validation slice on the current `main` baseline: make the WinUI XAML build diagnostics actionable and record target-toolchain build plus accessibility smoke evidence without weakening source-only or fail-closed boundaries.

## Acceptance

- On Windows 11 24H2+ x64 with Visual Studio 2026 and SDK/WDK 10.0.28000.2526, `pwsh -File tools/doctor.ps1 -CheckOnly` records the target fingerprint and `pwsh -File tools/build-preview.ps1 -Target WinUI` completes.
- The formal WinUI shell launches from ignored local output and a reproducible accessibility smoke check records the bounded observable control tree and failures, if any, without private identity or binaries.
- XAML/compiler failures remain actionable and fail closed; `DesktopCompat` and `WinUICompat` remain explicitly non-formal fallbacks rather than substitutes for target evidence.
- The existing bounded control-model contract and source-only policy remain intact; no driver, signing, installer, endpoint identity, private path, or physical-audio claim is added.
- Anonymous evidence is stored under `evidence/0000-foundation/winui-m1-formal-build-v1.json`; generated output remains under ignored `.local/`.

## Completed

- Created a fresh slice because closed Issue #52 / PR #57 was explicitly superseded as stale.
- Declared ownership, scope, shared paths, dependencies, and required gates on Issue #191.
- Created this isolated worktree from `origin/main` at base `ad4e4191b0e4f0f5e5a4b9d2f9f5a0bb2a9a0e6c`.

## Known limitations

- This sandbox is Linux and does not provide PowerShell 7, Visual Studio 2026, Windows App SDK, SDK/WDK 10.0.28000.2526, or a Windows UI runtime. No formal WinUI XAML or accessibility evidence can be produced here.
- Compatibility or user-space checks must not be promoted to formal target evidence. No driver, `.sys`, signing, installer, or physical-audio result is claimed.

## Last verification

- Repository and claim state inspected read-only; formal gates not run because `pwsh` is unavailable in this environment.

## Remaining work

1. Run baseline and target-toolchain checks on the locked Windows machine.
2. Reproduce and isolate any current XAML/compiler failure using the current main baseline.
3. Make the smallest scoped source/build-diagnostic correction, preserving fail-closed behavior.
4. Add reproducible anonymous target evidence and run the required gates.

## Next safe action

On the locked Windows 11 24H2+ target machine, run the baseline commands and reproduce the formal WinUI XAML build failure before changing source.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/doctor.ps1 -CheckOnly
pwsh -File tools/handoff-check.ps1 -Issue 191
pwsh -File tools/build-preview.ps1 -Target WinUI
pwsh -File tools/winui-shell-check.ps1
```
