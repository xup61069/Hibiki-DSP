---
schema_version: 2
issue: 149
branch: codex/149-extension-selftest-boundary
target_branch: main
base_commit: 6e50f347149076fb6d08d44a393d44fddafb2722
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T11:40:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/extension-check.ps1", "evidence/0000-foundation/149-extension-policy-selftest-v2.json", "docs/tasks/active/149.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [32]
specs: [SPEC-0009]
adrs: [ADR-0002]
next_safe_action: "Push the verified Issue 149 head and retain Draft status while the integrator repairs target-main BASELINE drift."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 149", "pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest", "gh pr checks 150"]
---

# Issue 149 handoff

## Objective

Extend the merged Issue #32 browser-extension source gate with deterministic, offline regression
fixtures for the remaining SPEC-0009 manifest/CSP boundary cases. This follow-up is limited to
self-test coverage and must not change extension product sources, permissions, endpoint
configuration, or browser/runtime claims.

## Acceptance

- `tools/extension-check.ps1 -SelfTest` adds cases for missing `extension_pages` CSP, invalid
  `object-src`, wrong loopback `connect-src`, and a valid multiline/whitespace-preserving CSP
  form only if the current policy intentionally permits it.
- Normal manifest/source validation behavior remains unchanged.
- Self-test is deterministic, bounded, in-memory, offline, counted, and performs no repository
  reads, process launches, browser/runtime calls, audio, driver, signing, or file writes.
- Anonymous evidence records the self-test cases and source-only limits.
- Required local gates pass and the Draft PR records actual CI runs; queued/in-progress CI is not
  treated as passing.
- No browser permission-prompt, tabCapture runtime, physical-audio, driver, HLK, or signing claim
  is made.

## Completed

- Re-fetched `origin` and confirmed the initial latest `origin/main` at
  `6e50f347149076fb6d08d44a393d44fddafb2722` before creating the branch.
- Checked open Issues, open/draft PRs, remote branch placeholders, active handoffs, worktrees,
  and related SPEC-0009 scope. Issue #119 was closed as already covered by Issue #28; Issue #137
  is an integrator-lane design with dependencies on #41/#64; Issue #139 already has an open PR.
- Confirmed no active handoff owns the Issue #149 paths. Issue #32 is merged and archived; this
  slice depends on its existing validator behavior rather than taking over its branch.
- Created and claimed Issue #149, branch `codex/149-extension-selftest-boundary`, and an isolated
  worktree from the confirmed `origin/main`.
- Added four deterministic in-memory fixtures to the existing extension policy self-test:
  missing `extension_pages` CSP, invalid `object-src`, wrong loopback endpoint, and accepted
  multiline CSP whitespace. Normal extension source validation was unchanged.
- Added anonymous evidence at
  `evidence/0000-foundation/149-extension-policy-selftest-v2.json`.
- After main advanced, fetched `origin/main` at
  `5783fbc7a5270434919dce1532ce602ace901581` and merged it into this branch with
  `77d79fa741a1777c1e2a1ff25482809263b76311`; no other AI branch or worktree was modified.
- Mechanically updated the shared BASELINE counters on this branch to 466 tracked paths and 97
  repository JSON files, matching the branch tree.
- Fetched the target baseline reconciliation at
  `4ebbdf11f5721bbfdc9023fc52c84fdac00418be` and merged it into this branch with
  `5ae1842bcf77a6bb87e905989190a02ef74da0a6`. The target now measures and claims 464/96;
  the #139 archive rename was preserved and the PR-mode docs-check passes against this target.

## Known limitations

- This is source-only validator evidence. It cannot prove browser permission prompts, user gesture
  enforcement, WebSocket runtime behavior, browser tab capture, physical audio, driver/WaveRT,
  HLK, or Microsoft signing.
- `docs/state/BASELINE.md` is a shared integrator snapshot. This branch declares it in
  `shared_paths` and changes only its mechanical counters; the integrator must reconcile the
  target branch and resolve the shared-path merge in final integration.
- A previous PR run was blocked by target-main BASELINE drift; target main is now reconciled by
  the integrator at 464/96. The current head still needs a fresh PR CI run after this merge.
- The local machine lacks the locked VS 2026, Windows App SDK, SDK/WDK target toolchain; local
  `verify.ps1` is user-space CMake/CTest evidence only.

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 149`: passed.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: passed.
- `pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest`: passed, 10 cases.
- `pwsh -NoProfile -File tools/extension-check.ps1`: passed.
- `pwsh -NoProfile -File tools/docs-check.ps1`: passed locally against the branch summary
  (85 required paths, 24 specs, 466 tracked paths, 97 JSON files).
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: passed, 7 cases.
- `pwsh -NoProfile -File tools/source-policy.ps1`: passed, 466 tracked paths.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: passed.
- `pwsh -NoProfile -File tools/verify.ps1`: passed, CMake/CTest 1 of 1; build outputs stayed
  ignored and unsubmitted.
- `git diff --check`: passed.
- PR-mode `tools/docs-check.ps1` against target `origin/main` `5783fbc` is blocked by target-main
  BASELINE drift (462/95 claimed versus 464/96 measured); this historical failure is recorded in
  evidence and is no longer current after target `4ebbdf1`.
- PR-mode `tools/docs-check.ps1` against target `origin/main` `4ebbdf1` passed, verifying the
  target's 464/96 merge base and this branch's 466/97 counters.
- Final head CI results: push run [32547631505](https://github.com/xup61069/Hibiki-DSP/actions/runs/32547631505)
  passed; pull-request run [32547633779](https://github.com/xup61069/Hibiki-DSP/actions/runs/32547633779)
  failed at the same target-main BASELINE check. The Draft PR is therefore not ready to request
  merge.

## Remaining work

1. Push the target-synchronized head and wait for fresh push and PR CI runs.
2. Record a green PR-side CI run and update this handoff to `ready_for_review` only after all
   required checks are green; the worker must not merge the PR.

## Next safe action

Push the target-synchronized head, keep PR #150 Draft, and update this handoff only from the
resulting CI state.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 149
pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest
gh pr checks 150
```
