---
schema_version: 2
issue: 16
branch: codex/16-calibration-control-model
target_branch: main
base_commit: 12ee53b8e30c5a6f696253d3d1a34cdb827368eb
status: in_progress
role: worker
owner: ox-alpha-gemini
updated_at: 2026-08-22T03:30:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-msvc-17.14-dotnet-10.0.301-pwsh-7.6.5; target-24h2-vs2026-wdk-missing
scope_globs: ["apps/control-model/CalibrationModel.cs", "apps/control-model-check/Program.cs", "docs/specs/SPEC-0011-calibration-compiler.md", "evidence/0000-foundation/calibration-control-model-v1.json", "docs/tasks/active/16.md"]
shared_paths: ["apps/control-model-check/Program.cs"]
depends_on: []
specs: [SPEC-0011]
adrs: [ADR-0002]
next_safe_action: "Implement CalibrationModel.cs, extend control-model-check with contract coverage, and record evidence."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 16", "pwsh -File tools/control-model-check.ps1", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 16 handoff

## Objective

Implement a fail-closed control-plane calibration response and bounded PEQ compiler
model (`CalibrationModel.cs`) in the unclaimed `apps/control-model` lane, aligning
with accepted `SPEC-0011` (`schemas/calibration-response-v1.schema.json` and
`schemas/peq-filter-v1.schema.json`).

## Acceptance

- Strongly-typed, schema-validated `CalibrationPointV1`, `CalibrationResponseV1`,
  `PeqFilterV1`, `PeqPresetV1`, `CalibrationCompilePolicyV1`, and
  `CalibrationCompileResultV1`.
- Strict schema validation: 1..512 points, strictly ascending positive frequency,
  finite values; 1..64 peaking filters, finite gain/Q/frequency within bounds.
- Deterministic bounded PEQ correction compiler in C# matching SPEC-0011 algorithm
  (greedy largest log-spaced residuals, max 16 filters, max 6 dB boost, -24 dB cut,
  Q 0.3..12.0, limited flag when capping/capacity reached, fail-closed on invalid data).
- Atomic JSON persistence and Equalizer APO / REW filter export.
- `apps/control-model-check/Program.cs` covers all boundaries, validation, compiler
  behavior, and file save/load.
- No RT thread, driver, permanent-identity, or IPC schema modifications.

## Completed

- Claimed Issue 16 with dedicated branch `codex/16-calibration-control-model` from
  `origin/main` (`12ee53b8e30c5a6f696253d3d1a34cdb827368eb`).
- Verified zero scope overlap with active PRs #11/#13 (Issue #8), PR #12 (Issue #10),
  and PR #15 (Issue #14).

## Known limitations

- This machine is Windows build 26200 with MSVC 17.14; target Windows 11 24H2+ /
  VS 2026 / WDK 10.0.28000.2526 is not present locally.
- The calibration model is a user-space control-plane contract only. It does not
  claim physical audio measurement, room acoustic optimization, microphone calibration,
  driver, HLK, or signing evidence.

## Last verification

- Handoff check and local gates verified at claim baseline.

## Remaining work

1. Implement `apps/control-model/CalibrationModel.cs`.
2. Extend `apps/control-model-check/Program.cs` with calibration model tests.
3. Update `docs/specs/SPEC-0011-calibration-compiler.md` to document the C# control model.
4. Record anonymous evidence in `evidence/0000-foundation/calibration-control-model-v1.json`.
5. Run all repository verification gates and push draft PR.

## Next safe action

Implement CalibrationModel.cs, extend control-model-check with contract coverage, and record evidence.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 16
pwsh -File tools/control-model-check.ps1
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
