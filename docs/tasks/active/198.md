---
schema_version: 2
issue: 198
branch: codex/198-handoff-files-retire
target_branch: main
base_commit: 444f95c336301a96834ddc22787a2c7ab5da580e
status: in_progress
role: integrator
owner: xup61069
updated_at: 2026-08-22T17:54:20+08:00
environment_fingerprint: local-windows-26200-vs2022-pwsh7
scope_globs: ["tools/handoff-check.ps1", "tools/context-pack.ps1", "tools/docs-check.ps1", "AGENTS.md", "CONTRIBUTING.md", "docs/AI_HANDOFF.md", "docs/ai/MULTI_AGENT.md", "docs/ai/HANDOFF_SCHEMA.json", "docs/tasks/**", "docs/specs/SPEC-0004*", "evidence/0000-foundation/handoff-files-retire-v1.json"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Run handoff-check, then begin protocol doc rewrite."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 198"]
---

# Issue 198 handoff

## Objective

以 GitHub Issue body 與 labels 取代 per-issue handoff files 作為唯一協調記錄；
handoff-check 改驗 issue 狀態而非讀 task files，最後刪除 docs/tasks/。

## Acceptance

- AGENTS.md、AI_HANDOFF.md、MULTI_AGENT.md 描述 issue+label protocol 為 canonical。
- handoff-check 以 gh issue view 驗證，保留 fail-closed overlap checks；SelfTest 用 mocked gh output。
- 仍有價值的 active handoff 內容遷入對應 Issue body 後刪除 docs/tasks/。
- labels claimed/in-review/done 已建立；關閉 Issue 即完成歸檔。

## Completed

- Claim commit 尚未建立。

## Known limitations

- CI runner 的 gh 可能受限於 GITHUB_TOKEN 權限；handoff-check 需 graceful fallback 或 REST API。
- #199 的 orchestrator-lanes 改寫不在本切片 scope 內。

## Last verification

- 尚未執行。

## Remaining work

1. 重寫 handoff-check.ps1（gh-based + SelfTest mocks）。
2. 更新 context-pack.ps1、docs-check.ps1 required list。
3. 更新所有協定文件與 SPEC-0004。
4. 遷移 active handoff 內容到 issue bodies。
5. 刪除 docs/tasks/。
6. Evidence JSON。
7. 全部 gates → draft PR → CI → merge。

## Next safe action

Run handoff-check, then begin protocol doc rewrite.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 198
```



