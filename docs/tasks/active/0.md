---
schema_version: 1
issue: 0
branch: main
base_commit: b92cc1f
status: in_progress
owner: hibiki-maintainers
updated_at: 2026-08-21T00:00:00Z
environment_fingerprint: run-tools-probe-on-this-machine
specs: [SPEC-0001, SPEC-0002, SPEC-0003, SPEC-0004, SPEC-0005, SPEC-0006, SPEC-0007, SPEC-0008, SPEC-0009, SPEC-0010, SPEC-0011, SPEC-0012, SPEC-0013, SPEC-0014]
adrs: [ADR-0001, ADR-0002, ADR-0003, ADR-0004]
---

# Foundation handoff

## Objective

建立可公開、可重建、可跨 AI 接手的 Hibiki DSP monorepo，逐步把 Audio Scene OS 的
driver、RT graph、ASIO、VST3、瀏覽器與校正功能落成可驗收模組。

## Completed

- Public documentation, component license map, stable distribution profile, JSON schemas and
  source-only paid-release policy.
- C++20 static library with Scene validation, volume reconciliation, ISO compensation boundary
  and device switch transaction state machine.
- CMake/CTest build and PowerShell doctor/probe/verify/docs/source-policy gates.
- SPEC-0003 draft boundary, SPEC-0004 AI handoff contract, SPEC-0005 source-only release contract
  and ADR-0003 release decision.
- Immutable RT graph snapshot with 2/6/8-channel mapping and non-allocating interleaved mixing.
- Versioned IPC frame codec, Apache C driver ABI, portable driver validator, ASIO stream model,
  VST quarantine model and MV3 tab-capture source prototype.
- Caller-owned output ring buffer, clock-drift estimator and bounded linear SRC prototype.
- Source-only PowerShell installer bootstrapper with manifest/hash dry-run gate.
- Easy Scene factory, acoustic-anchor phon mapping, PEQ/text exporters and float-WAV IR writer.
- Bounded multi-sink fan-out now rejects all-disabled plans and non-finite input blocks before
  writing; enabled sinks receive identical same-layout copies with capacity preflight.
- `OutputFanoutRuntimeV1` binds one persistent clock/SRC model per enabled sink, uses bounded
  prepare-time scratch and publishes only after every sink succeeds; failed processing restores
  the prior SRC state.
- `AudioEngineModel::process_output_group_fanout` now connects graph output, Group Master/limiter
  and the per-sink runtime while rejecting layout mismatches.
- Fan-out capacity preflight now uses each sink's current SRC phase/step, avoiding false rejection
  of ordinary 128-frame output buffers while retaining the fixed worst-case scratch bound.
- Persistent SRC phase validation now accepts the documented 0.25–4.0 source-step range across
  block boundaries; a high-ratio sink fixture covers two consecutive blocks.
- Portable MS-PL `wavert_stream_v1` now covers caller-owned WaveRT Float32 ring storage, whole
  block overrun rejection, counted underrun silence, fixed format bounds and reset semantics.
- WDK-only `hibiki_stream_adapter.cpp` now demonstrates spin-lock protected render submit/read,
  silence fallback and reset calls around that ring; it is not a built `.sys`.
- Apache `driver_stream_transport_v1` now defines fixed driver→engine Float32 packet framing;
  the GPL bridge validates exact packet length and finite samples into caller-owned lane storage.
- `AudioEngineModel::process_driver_stream_packet` now routes valid render packets into an active
  lane and rejects endpoint identity mismatch, capture type, sample-rate mismatch, channel
  mismatch and non-finite samples.
- `WindowsWasapiOutputV1` now binds `IAudioClock`, enforces extensible multichannel masks and
  feeds worker-side device position/QPC deltas into each sink's SRC model.
- `WindowsWasapiSinkHandoffV1` now owns primary/candidate WASAPI workers and performs bounded
  prepare → equal-power fade → commit/rollback without restarting the graph; candidate failures
  return to the active sink and an unavailable active sink becomes `Degraded`.
- `AudioEngineModel` now exposes the graph-to-WASAPI adapter: output-group processing applies
  Group Master/limiter before one bounded handoff submit, while start/prepare/commit/rollback
  stay on the control plane.
- `ExpertSurfaceModel` and the WinUI shell now render bounded read-only Matrix, DSP Graph, VST3
  quarantine and calibration summaries only when Expert is enabled; no missing IPC edit command
  is presented as applied.
- C++/C# IPC v1 envelope and request-session codecs share the same little-endian known-byte
  contract; C# also provides an asynchronous logical named-pipe client.
- `IrPhasePolicyV1` is a versioned scene/UI contract: the slider resolves minimum/mixed/linear/
  bypass semantics and bounded 0/80/160 ms added-delay budgets without embedding IR data.
- `EasyControlViewModel` emits SceneApply and VolumeNotification commands with fixed payloads;
  C++ `handle_control_frame_v1` validates them before handing them to a host sink.
- `ControlCommandQueueV1` gives the pipe callback a fixed 64-slot SPSC handoff; only the control
  worker may consume commands and mutate AudioEngine/graph state.
- `EngineControlWorkerV1` now consumes SceneApply/VolumeNotification commands, resolves Game,
  Movie, Voice and Studio presets, commits valid graphs and preserves the last revision on failure.
- `EngineControlWorkerV1::set_scene_preflight` adds an optional control-plane gate before graph
  Prepare; a failed VST3/state/calibration/safety preflight leaves the prior Scene, revision and
  active graph untouched.
- Graph compile now preserves each lane's output group in the immutable RT snapshot; the engine
  exposes `process_output_group` with a four-lane isolation fixture and fail-closed missing-group behavior.
- `AudioEngineModel` facade with graph transaction, Windows volume notification and one final
  Group Master gain in the RT process call.
- RT Group Master 只讀 release/acquire Q16.16 effective-dB/mute snapshots；control-plane
  volume object 不再被 audio process 直接讀取。
- `VolumeRampProcessorV1` now performs 8 ms gain, 5 ms mute and 15 ms unmute dB-domain ramps;
  tests use an 8 kHz fixture to make the frame budget explicit.
- `OutputGroupVolumeBankV1` now keeps independent canonical dB/mute/generation and RT ramps for
  up to 32 registered output groups; group rendering selects only its own master, while the
  legacy volume API remains the `main` shorthand and Strict Direct bypasses Group Master.
- `SceneCatalogV1` now stores up to 32 validated custom Scene definitions and `EngineControlWorkerV1`
  resolves non-built-in SceneApply IDs with exact output-group matching; replacement, capacity,
  remove/clear and mismatch paths are contract-tested.
- The control pipe preserves the 16-byte legacy Main volume command and adds a validated 48-byte
  grouped command; C++ and C# round-trip the selected UI output group and resolve it to the
  independent canonical volume bank.
- The C# control model now mirrors up to 32 custom Scene cards, rejects reserved/invalid IDs and
  routes a selected custom card through the existing SceneApply exact output-group contract.
- VST3 explicit bus layouts now have a bounded Main/Auxiliary/Sidechain admission validator;
  PluginHost quarantines invalid or main-layout-mismatched descriptors while the optional SDK
  worker remains explicitly one-main-bus.
- `PhysicalDeviceCatalogV1` now gives Windows watcher/worker code a bounded 32-entry render/capture
  endpoint catalog with validated identity/format, per-flow default uniqueness, monotonic event
  sequence, stale-event rejection, Active-only selection, 32-entry capacity and unplugged/invalid
  fail-closed behavior. `DeviceRecoveryCoordinator` now resolves rebind targets through that
  catalog before starting the switch transaction; it does not claim COM enumeration, a loadable
  driver or physical hotplug soak.
- `TruePeakLimiterV1` now guards non-Strict-Direct renders at −1 dBTP with fixed 8-channel,
  allocation-free inter-sample estimation and finite-sample fail-safe handling.
- `driver/inf/HibikiVirtualAudio.inf` provides the source-only MS-PL Root\HibikiDSP package
  template with stable endpoint GUID values; SYS/CAT and WDK/HLK signing remain external gates.
- `driver/include/hibiki/endpoint_topology_v1.h` and `driver/src/endpoint_topology.c` provide
  the fixed four-endpoint topology catalog: Main/Low Latency stereo render, Surround 7.1 with
  Windows `0x63f` channel mask, and Virtual Mic stereo capture, with rate flags and buffer defaults.
- `IpcNamedPipeServerV1` provides a local-only, bounded overlapped control worker with 4-byte
  length framing; Windows contract tests perform a real loopback Ack/request-ID round-trip.
- Windows `IAudioEndpointVolume` broker and `IMMNotificationClient` watcher with bounded,
  non-blocking callback snapshots, dB read-back and event-context GUID writes.
- Worker-side `DeviceRecoveryCoordinator` with monotonic event ordering, transactional rebind and
  safe-start dB plus mute after endpoint invalidation or Audio Service restart.
- Parameterized ISO 226:2023 formula with caller-supplied legal parameters and a tested 1 kHz
  invariant; no licensed coefficient table is embedded.
- `build_formula_compensation` now computes current/reference phon 1 kHz-normalized gains from
  caller-supplied ISO formula points, applies F3/boost safety limits and reports `limited`; no
  standard table, Annex data or SPL claim is added to the repository.
- `tools/source-only-ci-check.ps1` now gates GitHub workflows against artifact/package/release
  uploads, signing permissions and tracked binaries, and the public verify workflow runs it.
- The MS-PL WDK property adapter now rejects missing KS instance/value buffers and ambiguous
  GET/SET verbs before touching endpoint state; it remains a source-only PortCls boundary.
- MS-PL WaveRT endpoint control-state core validates 2/6/8-channel LPCM, Q16.16 dB safety,
  mute/generation ordering and actuator semantics; it is portable-testable but not a loadable
  PortCls miniport.
- Persistent linear SRC carries phase and one boundary frame across blocks and rejects output
  buffers that would force partial input consumption.
- VST host model enforces trusted/certified descriptors and bounded heartbeat watchdog quarantine;
  the source-only worker now runs bounded Hello/Heartbeat/ProcessBlock passthrough/Shutdown IPC,
  while the real SDK/plugin host remains pending.
- `AudioSessionRegistry` uses endpoint plus session-instance identity, keeps user lane/output
  bindings across metadata refresh, and treats PID as advisory only.
- `SessionRouteGraphBuilderV1` turns active bound sessions into a validated graph, preserving
  gain-owner semantics and rejecting Strict Direct plus internal makeup gain.
- `AudioSessionRegistry::set_makeup_gain_db` is the bounded −144..+12 dB mutator; metadata refresh
  preserves the user value.
- `ProgramAwareLevelControllerV1` provides a slow, silence-gated RMS default or fixed-state
  `KWeightedProxy` content correction with explicit caps, optional LFE exclusion and rate limit;
  neither mode is a BS.1770 conformance oracle.
- The tab-lane adapter can opt into that controller per browser tab, rejects sample-rate mismatch,
  and keeps the default path unchanged when no controller is supplied.
- `PeqProcessorV1` adds a fixed-capacity 16-filter RBJ PEQ path; the tab adapter can run it before
  content-level correction and rejects sample-rate/channel mismatches.
- `IrConvolverV1` adds a fixed-capacity 4096-tap direct FIR path with cross-block history; tab lane
  effects now run PEQ → IR → program-level correction and reject incompatible formats.
- Graph lanes can now opt into a validated 8×8 matrix for VB Matrix-style crossfeed and weighted
  routing; legacy channel maps and Strict Direct behavior remain explicit.
- `BasicNoiseSuppressorV1` adds a deterministic high-pass/downward-gate option for tab effects;
  documentation explicitly excludes RNNoise/AI/AEC claims and records the PEQ → IR → suppressor →
  level ordering.
- Windows `IAudioSessionManager2` watcher enumerates sessions on the worker and keeps
  `OnSessionCreated` callback allocation-free; live catalog enumeration is covered only by the
  opt-in probe, not by the normal contract run.
- Worker-side `ISimpleAudioVolume` dB/scalar conversion, GUID-tagged writes and readback are
  implemented; exclusive-mode and vendor ASIO are still explicit bypass paths.
- `OutputSinkModel` connects bounded clock drift ratio to persistent SRC without allocation in
  the audio-side process call; real per-device clock soak tests remain pending.
- Optional `hibiki_asio_native` source target builds against the pinned local ASIO SDK and
  implements COM discovery, stable CLSID, ASIO buffer/rate/channel callbacks and registry
  entrypoints; the target remains off in public source-only CI.
- `SceneSafetyController` implements and tests peak-triggered attenuation, 100 ms action
  coalescing, manual Windows-volume override detection and safe scene-baseline restoration.
- `OutputCrossfade` implements and tests allocation-free equal-power handoff for supported
  LPCM layouts; physical sink clock and hotplug soak remain hardware work.
- `OutputHandoffCoordinatorV1` combines the switch transaction with crossfade completion before
  commit, with rollback preserving the previous endpoint.
- `Vst3SandboxProcess` provides Windows Job Object process containment and explicit watchdog
  quarantine; the bounded worker IPC is present, while SDK/plugin certification remains pending.
- MV3 extension now emits user-gesture-gated `HIBT` AudioWorklet packets to an optional
  localhost bridge while preserving local playback when no receiver is running.
- `hibiki_tab_bridge_contract` validates HIBT packets and now includes a Windows loopback-only
  WebSocket listener; its queue can feed the selected graph lane through a no-allocation adapter,
  while denoising model provenance remains pending.
- `driver/wdk/hibiki_property_adapter.cpp` provides an MS-PL-only WDK property-dispatch scaffold
  over the portable endpoint core; it is source-checked but not a built or signed driver.
- `hibiki_asio_transport_v1` defines an Apache-2.0 fixed-layout SPSC shared-memory ring. The
  optional native ASIO DLL publishes eight-channel Float32 blocks after host callbacks, while
  `AsioTransportConsumerV1` owns the stable mapping and provides an allocation-free engine-side
  pop. A missing or mismatched mapping leaves ASIO usable but explicitly detached.
- `AudioEngineModel::process_asio_transport` now consumes one ring block into a selected Lane,
  executes the immutable graph and Group Master, and restores the caller's lane view; a Windows
  contract test exercises mapping creation, producer publication and processed output.
- `AudioEngineModel::process_asio_transport_to_wasapi` now reuses that processed block and submits
  it once to the dual-worker WASAPI handoff, preserving the same safety/rollback boundary for
  Hibiki ASIO clients.
- `process_driver_stream_packet_to_wasapi` now uses the same shared lane-to-WASAPI adapter, so
  virtual endpoint render packets and Hibiki ASIO cannot diverge in Group Master/limiter behavior.
- `process_tab_capture_lane_to_wasapi_v1` now applies the existing per-tab effects and submits
  once to that same handoff; missing sink state remains fail-closed.
- `process_virtual_mic_lane_to_wasapi_v1` now applies privacy/optional VirtualMicDsp and submits
  through the same adapter; this is not evidence of a signed capture endpoint.
- `WindowsWasapiFanoutV1` now coordinates up to eight same-layout/rate sinks with independent
  handoff states and explicit degraded-on-partial-submit behavior.
- `AudioEngineModel::prepare_wasapi_fanout` and `process_output_group_to_wasapi_fanout` now run
  one graph/Group Master/limiter pass before submitting the block to every prepared sink.
- `TabCaptureQueueV1` copies validated browser packets into a bounded four-slot SPSC queue from the
  WebSocket callback, exposes dropped-block telemetry, and gives a no-allocation pop; the
  `process_tab_capture_lane_v1` adapter now drives the selected graph lane and Group Master.
- `AudioEngineModel::process_lane_block` is now the shared caller-owned block entry for ASIO,
  TabCapture and future Virtual Mic sources, preserving the same immutable graph and Group Master
  semantics without coupling platform bridges to the RT thread.
- `WindowsWasapiOutputV1` plus `WindowsWasapiSinkWorkerV1` add a shared-mode Float32 physical
  render worker: one COM apartment owns bind/start/render/unbind, a bounded SPSC receives blocks,
  silence fills underruns, and `OutputSinkModel` applies worker-side persistent SRC plus queued
  clock observations. Invalid format/device paths fail closed; real endpoints remain unexercised;
  the graph RT thread never invokes this COM boundary.
- `vst3_worker_protocol.hpp`/`.cpp` define and test the fixed little-endian supervisor/worker frame
  ABI, including exact ProcessBlock payloads, bounded `ProcessBlockWithParameters` records and
  NaN/Inf rejection; named-pipe transport and the source-only worker executable are present. The
  optional pinned-SDK factory catalog and bounded one-main-bus SDK processor/worker build locally
  with fixed 4096-frame planar scratch, latency reporting, `IParameterChanges` conversion and
  ProcessBlock response framing; supervisor UI timeline editing and third-party compatibility
  remain open, while bounded timeline extraction, graph latency compensation and Scene state
  coordinator are committed.
- `LatencyAlignmentPlanV1` plus `FixedDelayLineV1` cover active-lane maximum-latency alignment,
  fixed 8-channel/16,384-sample storage, impulse delay and non-finite reset. `LatencyGraphCommitV1`
  and its committer add stable lane-token/revision-bound Prepare → Commit/Rollback control semantics;
  `LaneLatencyBankV1` now prepares the graph's cross-block delay state before commit and the RT mixer
  applies it without allocation; physical sink and third-party plugin end-to-end evidence remain.
- `Vst3WorkerPipeV1` is now wired into `Vst3SandboxProcess` launch/stop lifecycle, passing an
  optional `--hibiki-pipe` argument and exposing bounded control-plane wait/send/receive; no
  plugin SDK worker is bundled or claimed.
- `Vst3SandboxProcess::handshake_worker` and `process_worker_block` now provide the bounded
  supervisor exchange: HelloAck/request-ID checks, ordinary and parameterized ProcessBlock
  framing, response shape/payload/finite-value validation and silence-on-failure output. They
  remain control/IPC APIs and never run from the RT callback or auto-restart a quarantined worker.
- `Vst3WorkerLaneSessionV1` now binds the exchange to a stable lane token, reported latency and
  the bounded parameter timeline; it requires handshake, enforces contiguous block positions and
  moves the lane to `Degraded` on worker/protocol/order failure.
- `Vst3SceneAutomationSchedulerV1` now stores up to 16 timeline IDs and Scene/lane bindings,
  validates all bindings before activation, applies snapshots to prepared lanes and rejects
  concurrent per-lane blocks with explicit back-pressure. Opaque plugin state persistence and
  cross-version compatibility remain separate boundaries.
- `Vst3PluginStateStoreV1` now provides a private caller-owned state boundary with 16 × 1 MiB
  bounded slots, plugin/class/module SHA-256 identity checks, explicit state versions and
  fail-closed restore; `restore_with_migration` permits only an explicit caller-supplied handler
  after identity validation, with bounded output and fail-closed error mapping. The public schema
  contains metadata only and no opaque bytes.
- `Vst3PluginStateMigrationRegistryV1` adds a fixed 16-rule identity/source→target registry for
  approved Scene migrations; duplicate target mappings are rejected and handler context is
  non-owning. Scene-to-registry binding is now provided by the coordinator; third-party
  compatibility certification remains pending.
- `Vst3SceneStateCoordinatorV1` binds up to 16 Scene/state references, inspects the private store
  before activation, requires exact version or one approved migration rule, and restores only to
  caller-owned buffers. Its metadata schema is `scene-vst3-state-binding-v1`.
- `preflight_scene_vst3_state_v1` adapts the coordinator to `EngineControlWorkerV1` so bound Scenes
  are rejected before graph Prepare; unbound Easy Scenes remain backward-compatible.
- Optional pinned-SDK `Vst3SdkProcessorV1::save_state/load_state` now uses a bounded 1 MiB
  `IBStream` for component state, with separate overflow/plugin-error/destination-size results;
  Scene migration registry/coordinator are source-complete; third-party compatibility certification
  remains pending.
- `docs/VST3_STATE_COMPATIBILITY_REVIEW.md` now defines the mandatory human review and test
  checklist for every explicit third-party state migration handler; opaque state remains private,
  and unreviewed plugins stay quarantined.
- State migration now preserves an explicit bounded-output failure result and contract tests cover
  the exact 1 MiB limit, oversized capture rejection and caller-owned migration overflow.
- WinUI source shell now exposes explicit AutomationProperties names/help text for primary controls,
  scene cards and Expert summaries; the static gate checks the polite status live region. Full UI
  Automation, screen-reader, high-contrast and text-scaling evidence remains target-machine work.
- `SessionRouteRuleStoreV1` adds 64-entry, priority-deterministic per-App routing/makeup rules;
  matching uses stable app/session metadata rather than PID, equal-priority conflicts fail closed,
  and the 64-entry capacity boundary is covered by contract tests. `WindowsAudioSessionWatcher`
  applies the non-owning store during worker enumeration; callbacks remain read-free and
  non-blocking.
- `PluginHostModel` now exposes prepare/handshake/process entry points for that worker lane and
  detaches it whenever the host enters `Quarantined`; descriptor trust/certification, channel
  parity and stable lane-token validation remain the admission gate.
- `VirtualMicRouteModel` provides a tested privacy/reference contract for future Virtual Mic
  routing (1/2 channels, fixed rates, mute default, caller-owned copy); `VirtualMicDspV1` adds a
  bounded normalized-LMS echo-reference canceller/noise gate, while `process_virtual_mic_lane_v1`
  applies privacy and DSP before the shared graph. It is still not acoustic AEC/NS conformance or
  a signed capture driver.
- `EasyControlSession` provides a UI-independent fail-closed One-Tap Enhance contract, explicit
  Easy/Expert mode, scene selection and active output-group identity for future WinUI rendering.
- `EasyControlViewModel` now owns a fixed output-group catalog and bounded asynchronous
  named-pipe Hello/Scene/volume lifecycle with 40 ms latest-value volume debounce and
  Connected/Degraded state transitions. The
  source-only `apps/winui-shell/` binds that surface to a first-run WinUI 3 Easy/Expert window;
  it intentionally has no audio-thread or DSP access.
- The control plane now has a deterministic measured-response to bounded PEQ compiler with a
  versioned response schema, 16-filter/spacing/Q/cap policy and explicit `limited` diagnostics;
  it feeds the existing exporters and remains a re-measurement-required baseline.
- Public CI now includes WinUI source, driver boundary and JSON parse steps; it intentionally does
  not upload build outputs or expose signing/Gumroad secrets.

## Known limitations

- Current development machine is Windows build 22631, below the driver target build 26100;
  user-space contract tests pass but this is not driver-target evidence.
- The Windows broker/watcher callbacks are unit-exercised and the opt-in catalog probe sees local
  endpoint metadata; no real Hibiki virtual endpoint, physical sink rebind or Audio Service
  restart has been exercised.
- No WaveRT-backed endpoint or physical sink delivery for the ASIO ring, WinUI SDK/build and
  accessibility validation, Scene-wired VST3 SDK plugin host, or signed installer exists yet;
  the bounded calibration compiler is a control-plane baseline and the optional SDK
  processor is a worker-side single-main-bus boundary only.
  The optional ASIO COM transport and shared-memory boundary are unsigned developer code and do
  not themselves provide physical I/O.
- ISO formula parameters and contour values are caller-supplied; no licensed standard data or
  formal conformance oracle is embedded.

## Last verification

- `pwsh -File tools/verify.ps1`: passed; one explicit-check CTest executable.
- `pwsh -File tools/docs-check.ps1`: passed; 67 required paths and 15 specs.
- `pwsh -File tools/source-policy.ps1`: passed; 282 tracked paths.
- `hibiki_contract_tests`: passed fan-out finite-input/all-disabled-plan and unbound WASAPI
  handoff fail-closed cases.
- `pwsh -File tools/driver-source-check.ps1`: passed; WDK source boundary only, no `.sys` build.
- `pwsh -File tools/extension-check.ps1`: passed; MV3 manifest and source present.
- `pwsh -File tools/installer-check.ps1`: passed; bootstrapper syntax/boundary present.
- `pwsh -File tools/control-model-check.ps1`: passed; Easy/Expert behavior model, fixed output
  groups and bounded missing-engine connection fallback.
- `pwsh -File tools/winui-shell-check.ps1`: passed; source-only WinUI target/version, binding
  and audio-boundary gate.
- `pwsh -File tools/docs-check.ps1`: passed; 67 required paths and 15 specs, including the
  calibration response schema, compiler spec, VST3 latency graph commit spec, parameter timeline
  schema and output fan-out schema.
- `pwsh -File tools/distribution-check.ps1`: passed; permanent identity profile.
- Optional local build: CMake `HIBIKI_ENABLE_NATIVE_ASIO=ON` with pinned SDK commit
  `496a0765b8bb9c26f764f22f9a9712a937177db2`; `hibiki_asio_native` RelWithDebInfo build passed.
- Optional local build: CMake `HIBIKI_ENABLE_VST3_SDK=ON` with pinned SDK commit
  `3cdf9ca5d1f5b1b21e0a86832aa4abe55607bd96`; `hibiki_vst3_sdk_catalog` and
  `hibiki_vst3_sdk_worker` (catalog plus bounded processor/parameter frame bridge) RelWithDebInfo
  builds passed; no SDK checkout or compiled artifact is tracked.
- `tools/doctor.ps1 -CheckOnly` passed with explicit warnings: this machine is Windows 22631/
  VS 17 and lacks the locked Windows 26100+/VS 2026/10.0.28000.2526 SDK-WDK, so no driver or
  real-device evidence is inferred from the local user-space run.
- `dotnet restore apps/winui-shell/Hibiki.WinUI.csproj` completed, but the local WinUI build
  reached `XamlCompiler.exe` and exited 1; this is recorded as an environment/toolchain failure,
  not as target Windows App SDK or UI Automation evidence.
- `OutputGroupVolumeBankV1` Main/Movie isolation, generation handling, 8 ms ramp and Strict Direct
  bypass are covered by the CTest contract; grouped 48-byte C++/C# IPC and legacy payload
  compatibility also pass; no physical endpoint volume callback was inferred.
- C++/C# DeviceSwitch fixed 288-byte payload, strict padding/format validation, catalog sequence,
  EngineControl handler fail-closed behavior and WinUI send-failure rollback all passed.
- C++/C# DeviceCatalogSnapshot fixed-entry wire round-trip, reserved/UTF-8/format/duplicate/default
  validation, stale atomic UI replace and unsolicited frame reader source all passed.
- `DeviceCatalogSnapshotPublisherV1` converts the validated C++ catalog into one bounded snapshot
  frame; full-capacity publisher and decode coverage passed.
- `WindowsPhysicalDeviceCatalogWorker` and its watcher coordinator now provide a worker-owned COM
  enumeration source, candidate rollback, snapshot transaction and explicit DeviceCatalogRequest
  reply-provider path; only unbound/null and source compilation evidence is available locally.
- Opt-in `tools/live-device-catalog-check.ps1` built and ran against the local IMMDeviceEnumerator:
  14 endpoints, sequence 1, 5,840-byte snapshot and wire decode passed; endpoint identities were
  not printed or committed. Target Windows/driver/handoff evidence is still separate.
- JSON parse gate: passed; 34 repository JSON files.

## Remaining work

1. Turn the fixed topology catalog and WDK property scaffold into the first loadable endpoint
   without linking GPL user-space; then connect graph output to
   a real signed sink and prove end-to-end audio rather than only user-space boundaries.
2. Exercise the dual-worker WASAPI handoff with a real endpoint, including warm-up, 30 ms
   crossfade, Audio Service restart and rollback; current source-only tests cover only
   unbound/degraded fail-closed behavior.
3. Add a Windows 11 24H2+ test machine and produce driver signability evidence.
4. Acquire/legal-review ISO 226:2023 coefficient redistribution rights before adding data.

5. Run the compatibility review against each real third-party VST3 handler and complete the
   production worker policy (crash redaction, certification and side-chain/multi-bus process).
   The bounded SDK `IBStream`, identity/checksum gate, migration registry, Scene coordinator and
   EngineControl preflight transaction now exist; keep state bytes private and require an approved
   handler before any version mismatch can be restored.

## Next safe action

Build the source-only WinUI shell on the locked Windows App SDK toolchain and record
XAML/accessibility evidence; then run endpoint warm-up, crossfade and rollback soak on the target
Windows 11 24H2 machine before applying the compatibility checklist to a legally obtained
third-party plugin in an isolated test environment.

## Resume commands

```powershell
pwsh -File tools/doctor.ps1 -CheckOnly
pwsh -File tools/probe-environment.ps1
pwsh -File tools/verify.ps1
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
