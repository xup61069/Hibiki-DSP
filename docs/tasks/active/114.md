---
schema_version: 2
issue: 114
branch: codex/114-engine-preview-build-selftest
target_branch: main
base_commit: 2b96576cfc590f3cb3bb3f293b5897625b723369
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: "Windows 11 build 26200; VS 2026 18.9.12112.369; SDK 10.0.28000.0; WDK 10.0.28000.2526 unavailable; .NET 10.0.400; bundled VS CMake available only by explicit process-local PATH"
scope_globs: ["tools/build-engine-preview.ps1", "evidence/0000-foundation/build-engine-preview-selftest-v1.json", "docs/tasks/active/114.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [0]
specs: []
adrs: []
next_safe_action: "Run the required repository gates, record exact results, then push the implementation and evidence commits."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/build-engine-preview.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 114", "pwsh -NoProfile -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 114 handoff

## Objective

Add a bounded, offline `-SelfTest` mode to `tools/build-engine-preview.ps1` that validates the existing Engine Preview build dispatch contract without invoking CMake, compiling, launching a process, connecting IPC, enumerating devices, touching audio, or writing files.

## Acceptance

- `-SelfTest` is deterministic, source-only, offline, no-build, no-process, and no-file-write.
- It validates the existing Engine Preview configure/build target metadata and ignored output-root contract.
- Normal build behavior remains unchanged and still invokes CMake only without `-SelfTest`.
- Anonymous evidence and this schema-v2 active handoff are included.
- No shared integration files are modified.
- Required local gates and CI results are recorded in the Draft PR.

## Completed

- Confirmed latest `origin/main` is `2b96576cfc590f3cb3bb3f293b5897625b723369`.
- Confirmed Issue #86 owns only `tools/build-preview.ps1` and Issue #70 owns only `tools/engine-preview-smoke.ps1`; neither overlaps this scope.
- Created Issue #114, assigned owner `xup61069`, and created branch `codex/114-engine-preview-build-selftest` from the latest `origin/main` in an isolated worktree.
- Declared the exclusive write scope and integrator-owned baseline dependency.
- Pushed claim commit `08c63e7` and opened Draft PR #115.
- Added shared pure helpers for the existing configure/build argument plan and a `-SelfTest` branch that performs no CMake invocation, process launch, IPC, device enumeration, audio operation, or file write.
- Preserved the normal CMake configure/build path through the same plan helper.
- Added anonymous evidence at `evidence/0000-foundation/build-engine-preview-selftest-v1.json`.
- Built Engine Preview successfully with VS2026 bundled CMake using a process-local PATH override; generated files stayed under ignored `.local/engine-preview`.
- Ran unsigned local verification successfully with CTest 1/1.

## Known limitations

- The self-test is build-dispatch/source evidence only; it does not prove a successful CMake configure, compilation, Engine Preview launch, IPC delivery, physical audio delivery, driver, WaveRT, HLK, signing, or release behavior.
- The locked WDK prerequisite is unavailable on this machine; any formal driver or WinUI evidence remains out of scope.
- `docs/state/BASELINE.md` is shared and will not be modified by this worker; its tracked-path count may require an integrator refresh after this branch is merged.

## Last verification

- `pwsh -NoProfile -File tools/build-engine-preview.ps1 -SelfTest`: pass, 5 cases; no build/process/file activity.
- `pwsh -NoProfile -File tools/build-engine-preview.ps1` with the bundled VS2026 CMake directory prepended process-locally: pass; Engine Preview Release target built.
- `pwsh -NoProfile -File tools/verify.ps1` with the same process-local CMake PATH: pass; CTest 1/1.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 114`: pass.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass.
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass for 428 tracked paths.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass for 1 workflow and 428 tracked paths.
- `git diff --check`: pass.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked only by integrator-owned `docs/state/BASELINE.md` claiming 427 tracked paths while this branch has 428.
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: warning only; CMake is absent from default PATH and WDK 10.0.28000.2526 is unavailable.
- `pwsh -NoProfile -File tools/probe-environment.ps1`: wrote only ignored `.local/context.json`.
- Claim and implementation commits are based on `origin/main` at `2b96576cfc590f3cb3bb3f293b5897625b723369`.

## Remaining work

1. Commit and push the evidence and handoff update.
2. Record the exact push and pull-request CI run/job results in this file and PR #115.
3. Leave the handoff `ready_for_review` only after CI is complete and green; otherwise record the single blocking cause and next safe action.

## Next safe action

Commit and push the evidence and handoff update, then record the exact CI results.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/build-engine-preview.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 114
pwsh -NoProfile -File tools/docs-check.ps1
git diff --check
```
