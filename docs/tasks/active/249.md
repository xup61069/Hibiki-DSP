---
schema_version: 2
issue: 249
branch: codex/249-crlf-fix
target_branch: main
base_commit: 917b750b3e626f6815a2a89d427a85dae88f5e72
status: ready_for_review
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T18:05:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix-core.autocrlf=true; cmake-missing-wdk-missing
scope_globs: ["tools/distribution-check.ps1", "docs/tasks/active/249.md"]
shared_paths: []
depends_on: [39]
specs: [SPEC-0001]
adrs: []
next_safe_action: "Merge once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -File tools/distribution-check.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1"]
---

# Issue 249 handoff

## Objective

一行修復 `tools/distribution-check.ps1` self-test 的行尾脆弱性：CRLF checkout 下 here-string
含 `` `r`n ``，10 個 rejection fixture 的 `.Replace("...`n")` 全部靜默無效，self-test 在本機
從未真正測過拒絕路徑（CI runner 用 LF 所以沒抓到）。here-string 後正規化為 LF。

## Acceptance

- CRLF checkout（autocrlf=true）上 `-SelfTest` 11 cases 全過（含全部 rejection cases）。
- LF 樹上行為不變。
- 無其他檔案變更。

## Completed

- Reproduced on main `917b750`（CRLF checkout）：首個 fixture 即 fail。
- Added normalization line; self-test green on this CRLF worktree.

## Known limitations

- Gate 行為本身不變；僅 self-test fixture 建構確定化。

## Last verification

- `tools/distribution-check.ps1 -SelfTest`: pass, 11 cases（本 CRLF worktree）。
- docs-check / CI：PR 上驗證。

## Remaining work

1. Merge after green CI.

## Next safe action

Merge once verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/distribution-check.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
```
