---
schema_version: 2
issue: 80
branch: codex/80-control-model-engine-smoke-selftest
target_branch: main
base_commit: 81da555b3d1430fdb119664bfbd31f540ca0754a
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T06:55:28+08:00
environment_fingerprint: windows-26200-amd64-vs2026-msvc-19.51.36256.0-sdk-10.0.28000.0-cmake-4.3.1-dotnet-10.0.400-pwsh-7.6.4
scope_globs: ["tools/control-model-engine-smoke.ps1", "evidence/0000-foundation/control-model-engine-smoke-selftest-v1.json", "docs/tasks/active/80.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: ["0"]
specs: []
adrs: []
next_safe_action: "Run docs-check after staging evidence, then push the verification update and inspect PR #81 CI; keep cross-process smoke failures explicitly recorded."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 80", "pwsh -NoProfile -File tools/context-pack.ps1 -Issue 80 -NoSource", "pwsh -NoProfile -File tools/control-model-engine-smoke.ps1 -SelfTest"]
---

# Issue 80 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/control-model-engine-smoke.ps1` that verifies its deterministic `dotnet run` argument construction without building, launching, connecting to, or modifying Engine Preview.

## Acceptance

- Extract a pure helper for the control-model smoke argument list while preserving the normal launcher behavior and ordered arguments.
- `-SelfTest` covers default and `-EnableSessionRouting` argument matrices, project/output-root isolation, release configuration, and absence/presence of the session flag.
- Self-test runs before build/process checks and performs no dotnet invocation, Engine Preview process launch, named-pipe connection, file write, endpoint enumeration, or system-volume write.
- Normal smoke continues to build Engine Preview only when absent, starts it hidden, runs the C# smoke, and cleans up the child process.
- Add anonymous evidence and this schema-v2 handoff; no product source, IPC schema, DSP, RT-thread, driver, UI, Spec, or ADR change.

## Completed

- Claimed Issue #80 and reserved the independent scope on `codex/80-control-model-engine-smoke-selftest` from `origin/main` at `81da555b3d1430fdb119664bfbd31f540ca0754a`.
- Read the canonical repository, multi-agent, conflict, change-contract, and active-handoff instructions before editing.
- Added pure `Get-ControlModelSmokeArguments` and `Assert-ControlModelSmokeArguments` helpers plus a 4-case in-memory `-SelfTest`.
- Normal launcher now uses the same helper while preserving build-on-missing, hidden Engine Preview start, C# smoke invocation, and cleanup behavior.
- Added anonymous evidence at `evidence/0000-foundation/control-model-engine-smoke-selftest-v1.json`.

## Known limitations

- The self-test will be deterministic PowerShell launcher evidence only; it cannot prove C# process launch, named-pipe delivery, physical endpoint delivery, driver, WaveRT, HLK, signing, or real audio behavior.
- `docs/state/BASELINE.md` is integrator-owned; this worker may record a count mismatch but will not edit that shared path.

## Last verification

- `pwsh -NoProfile -File tools/control-model-engine-smoke.ps1 -SelfTest`: pass (4 cases; no dotnet invocation, process launch, or file write).
- `pwsh -NoProfile -File tools/build-engine-preview.ps1`: pass with VS 2026/CMake; outputs remained ignored under `.local/`.
- `pwsh -NoProfile -File tools/control-model-engine-smoke.ps1`: reached the existing C# smoke, then failed `Engine status/scene did not reconcile: sequence=3, scene=game`; launcher cleanup completed.
- `pwsh -NoProfile -File tools/control-model-engine-smoke.ps1 -EnableSessionRouting`: reached the existing C# smoke, then failed `Scene switch did not clear the prepared IR state`; launcher cleanup completed.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 80` and full handoff check: pass.
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass for 421 tracked paths; `tools/source-only-ci-check.ps1`, JSON parse, `tools/verify.ps1` (CTest 1/1), and `git diff --check`: pass.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked by integrator-owned `docs/state/BASELINE.md` (claims 419 tracked paths; this branch reports 421 after the evidence file was staged).

## Remaining work

1. Commit and push the evidence and verification handoff update.
2. Run docs-check and inspect both Draft PR CI runs.
3. If CI is green, update this handoff to `ready_for_review`; otherwise retain the exact baseline or downstream smoke limitation and next safe action.

## Next safe action

Run `pwsh -NoProfile -File tools/docs-check.ps1`, then inspect PR #81 CI. Do not modify `docs/state/BASELINE.md` or broaden this launcher-only scope.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 80
pwsh -NoProfile -File tools/context-pack.ps1 -Issue 80 -NoSource
pwsh -NoProfile -File tools/control-model-engine-smoke.ps1 -SelfTest
```
