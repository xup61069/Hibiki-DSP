---
schema_version: 2
issue: 41
branch: codex/41-docs-check-selftest
target_branch: main
base_commit: 87aac55db39821820b12a49fcce2ec9c7bd9668a
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T04:45:00+08:00
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["tools/docs-check.ps1", "evidence/0000-foundation/docs-check-selftest-v1.json", "docs/tasks/active/41.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Refactor the baseline-summary gate into Test-BaselineSummary, add bounded -SelfTest cases, add anonymous evidence JSON, refresh BASELINE counters, run local gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 41", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 41 handoff

## Objective

把 PR #26 為 `handoff-check.ps1` 引入的 `-SelfTest` 慣例鏡像到 Issue #21 的 baseline-summary
gate：`tools/docs-check.ps1` 新增 bounded 自測，以 in-memory fixtures 驗證每一個拒絕類別
（四個計數不符、marker 缺失）與合法輸入通過，全程不改動 repository 檔案。

## Acceptance

- gate 重構為可重用函式（輸入：摘要文字＋四個實測值）；main path 行為等價。
- `-SelfTest` 跑 bounded cases：合法摘要通過；required entries／specs／tracked paths／
  repository JSON 各自 mismatch 被拒；marker 缺失被拒；結尾輸出通過數並 exit 0。
- SelfTest 不依賴 repository 內容（純合成字串與注入數值）。
- 新增匿名 evidence JSON 記錄 self-test 命令與覆蓋類別。
- BASELINE.md 計數因本切片新增兩個 tracked files 而刷新。
- 本機 gates（含 `docs-check.ps1 -SelfTest`）通過；GitHub Actions verify 全綠後才請求 review。

## Completed

- Fetch 最新 origin/main（`87aac55`）；盤點 open work：#30/#34/#35 有 linked PR、#39 已有
  遠端 branch（live claim），全部不碰。確認 docs-check.ps1 無其他 owner。
- 建立 Issue #41、指派 `xup61069`、獨立 worktree／branch／本 handoff claim。

## Known limitations

- SelfTest 的 regex 樣本與 production gate 共用同一份 pattern 字串；若未來改寫 BASELINE
  摘要句式，SelfTest 的 valid-case 樣本必須同步更新——這是設計上的 fail-closed 提醒，
  不是缺陷。
- CHANGE_CONTRACT 歸類：只新增既有 accepted gate 的測試覆蓋，無新 invariant／API／建置鏈
  變更，SPEC 文字維持 PR #23／#26 更新後的內容。

## Last verification

- 尚未執行；將於實作 commit 後補記錄本機 gates 與 CI runs。

## Remaining work

1. 重構 gate 為 `Test-BaselineSummary`（文字＋實測值參數化）。
2. 加入 `-SelfTest` bounded cases 與 evidence JSON。
3. 刷新 BASELINE 計數；跑本機 gates；push 後等 GitHub Actions 全綠轉 `ready_for_review`。

## Next safe action

Refactor the baseline-summary gate into Test-BaselineSummary, add bounded -SelfTest cases, add anonymous evidence JSON, refresh BASELINE counters, run local gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 41
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
