---
schema_version: 2
issue: 43
branch: codex/43-installer-source-boundary
target_branch: main
base_commit: 4234b03ad759f9539ad6fd2e4313bd05cd8b538c
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T05:00:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/installer-check.ps1", "docs/specs/SPEC-0005-source-only-paid-release.md", "evidence/0000-foundation/installer-source-boundary-v1.json", "docs/tasks/active/43.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [0]
specs: [SPEC-0005]
adrs: []
next_safe_action: "Add anonymous Issue 43 evidence, run all repository gates, and update the Draft PR with exact local results."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 43", "pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly", "pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/installer-check.ps1"]
---

# Issue 43 handoff

## Objective

Strengthen `tools/installer-check.ps1` with a bounded source-only validator and `-SelfTest` so
the public `installer/HibikiSetup.ps1` retains manifest custody, relative-path/hash validation,
explicit signed-driver staging, and administrator/ShouldProcess boundaries. The installer source,
package, driver, signing material, and credentials are inputs or external release artifacts, not
worker outputs.

## Acceptance

- Parse `installer/HibikiSetup.ps1` fail-closed and validate the required manifest, hash,
  path-confinement, signed-package, ShouldProcess, administrator, and `pnputil` boundaries.
- Add bounded `-SelfTest` fixtures for valid source and missing/tampered security boundaries
  without invoking an installer, touching a package, or changing local machine state.
- Update SPEC-0005 and anonymous evidence with source-only coverage and explicit limits.
- No installer, package, driver, credential, signing, workflow, product API, IPC, DSP, RT-thread,
  or permanent identity change.
- Required local gates and GitHub Actions verify must pass before review; no signed installer,
  driver, HLK, or physical-audio claim is made.

## Completed

- Fetched the repository and confirmed the latest `origin/main` at
  `4234b03ad759f9539ad6fd2e4313bd05cd8b538c`.
- Reviewed the required AI workflow documents, open Issues/PRs, active handoffs, worktrees, and
  branch ownership; Issue #30, #35, #39, and #41 are already claimed, so this Issue #43 slice
  does not overlap those lanes or Issue 0 shared paths.
- Created the isolated Issue #43 branch from the latest `origin/main`.
- Committed and published the schema-v2 ownership claim after `handoff-check -Issue 43` passed.
- Added AST-backed source validation for `HibikiSetup.ps1`: manifest digest/signature fields,
  SHA-256 hashing, relative path confinement, `ShouldProcess`, administrator, and pnputil staging
  boundaries are now required in their owning functions.
- Added bounded `-SelfTest` coverage for valid source plus five tampered-boundary fixtures; the
  canonical installer source passes the normal check.
- Updated SPEC-0005 with the source-only installer gate and explicit non-runtime limitations.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: passed for all active handoffs.
- `pwsh -NoProfile -File tools/source-policy.ps1`: passed for 408 tracked paths.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: passed for 1 workflow and 408 tracked
  paths.
- `pwsh -NoProfile -File tools/verify.ps1`: passed; CMake/MSBuild build and CTest 1/1 passed.
- `git diff --check`: passed.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked by integrator-owned
  `docs/state/BASELINE.md` count drift (BASELINE claims 407 tracked paths; Git reports 408 after
  this handoff was added).

## Known limitations

- This is a source-only static boundary check; it does not run `HibikiSetup.ps1`, invoke
  `pnputil.exe`, read a signed package, validate an Authenticode signature, or modify a machine.
- It cannot prove release pipeline custody, Secure Boot/HVCI installation, upgrade/rollback,
  uninstall, driver loading, HLK, Microsoft signing, or physical audio.
- The validator covers the deliberately bounded installer source contract; future release
  operations require a separate Spec/Issue update.

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 43`: passed before implementation.
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: passed with explicit warnings that Visual
  Studio 2026 and SDK/WDK 10.0.28000.2526 are missing.
- `pwsh -NoProfile -File tools/probe-environment.ps1`: passed and wrote only ignored local
  fingerprint data.
- `pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest`: passed (6 cases).
- `pwsh -NoProfile -File tools/installer-check.ps1`: passed against the canonical installer.
- `git diff --check`: passed.

## Remaining work

1. Add anonymous evidence for source coverage and the missing target toolchain.
2. Update the Draft PR with exact local results, including the shared BASELINE dependency.
3. Wait for the integrator's BASELINE refresh and both GitHub Actions verify runs.

## Next safe action

Add the Issue 43 evidence JSON, run all required gates, and update the Draft PR with the resulting
local checks before waiting for GitHub Actions.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 43
pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly
pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest
pwsh -NoProfile -File tools/installer-check.ps1
```
