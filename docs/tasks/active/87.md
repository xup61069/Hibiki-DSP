---
schema_version: 2
issue: 87
branch: codex/87-live-wasapi-handoff-selftest
target_branch: main
base_commit: 5e9e8a6051e203dd887a671f8198e2e42773cdc3
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T09:01:47+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.4-msvc-buildtools-17.14
scope_globs: ["tools/live-wasapi-handoff-check.ps1", "evidence/0000-foundation/live-wasapi-handoff-selftest-v1.json", "docs/tasks/active/87.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [64]
specs: []
adrs: []
next_safe_action: "After integrator merges PR #67 / Issue #64, rerun PR-mode docs-check and both GitHub verify workflows; set this handoff ready_for_review only on green CI."
resume_commands: ["git fetch origin", "git merge --no-edit origin/main", "$env:GITHUB_BASE_REF='main'; pwsh -NoProfile -File tools/docs-check.ps1", "gh pr checks 95 --watch --interval 10"]
---

# Issue 87 handoff

## Objective

為 live WASAPI handoff wrapper 增加 bounded `-SelfTest`，只在記憶體中驗證既有 build/dispatch plan 的匿名 metadata；正常模式仍負責建立 `.local` build、編譯 probe 並執行 live probe。

## Acceptance

- 抽出可重用的純函式，建立並驗證現有 wrapper 的 build root、CMake definitions、target name 與 probe path，保留正常 command 行為。
- `-SelfTest` 為 deterministic、counted、offline，覆蓋 valid plan、錯誤 target、缺少 build definition、不安全 output root 與 path mismatch rejection。
- self-test 不得呼叫 CMake、build tool、process、named pipe、COM、audio endpoint 或寫入檔案。
- 新增匿名 evidence 與 schema-v2 handoff；不修改 product API、IPC/schema、DSP、RT thread、driver、signing、installer、Spec 或 ADR。

## Completed

- 已從最新 `origin/main` `5e9e8a6051e203dd887a671f8198e2e42773cdc3` 建立本 Issue 專用 branch/worktree。
- 已確認 `docs/state/BASELINE.md` 為 integrator-owned shared path，本 worker 不修改。
- 已加入 reusable plan construction/validation helpers 與 bounded `-SelfTest`，正常 CMake/build/probe command 順序保留。
- self-test 目前 5/5 cases pass；正常 opt-in user-space WASAPI handoff probe 也完成，handoff states 與 counters 已記錄。
- 已新增 `evidence/0000-foundation/live-wasapi-handoff-selftest-v1.json`，記錄實際 commands、probe output 與限制。
- 已從最新 `origin/main` `4810de2afe053e9549ee91295582247845e6ace7` merge 至本 branch，merge commit `e9979ee`；未修改其他 AI branch。
- 已從最新 `origin/main` `6441ee4874428a5b60f3e526c2377a813d6d864f`（包含已合併 PR #111）merge 至本 branch，
  merge commit `d42c800`；相對於最新主線的 diff 只包含本 Issue 的 tooling、evidence 與 handoff。

## Known limitations

- 此 slice 只驗證 user-space live-probe wrapper 的 source/dispatch contract；不代表 live probe 已執行、實體音訊、driver、WaveRT、HLK、Microsoft signing 或硬體驗收。
- 正常 live probe 仍可能建立 ignored `.local` build artifacts 並接觸本機 Audio Service；`-SelfTest` 不會做任何這些操作。
- `tools/docs-check.ps1` PR/feature/strict modes 目前因 integrator-owned `docs/state/BASELINE.md` 記錄 429 tracked paths，而本 branch 為 431 tracked paths（含 evidence 與 handoff）而受阻；本 worker 不修改 shared baseline。Issue #64 / PR #67 是明確 dependency。

## Last verification

- `pwsh -NoProfile -File tools/live-wasapi-handoff-check.ps1 -SelfTest`：pass，5/5。
- `pwsh -NoProfile -File tools/live-wasapi-handoff-check.ps1`：pass，user-space WASAPI handoff probe；format 2ch/48kHz/32-bit、submitted 13/12、dropped 0/0。
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 87`：pass。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass，431 tracked paths（latest main sync 後）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass，1 workflow、431 tracked paths（latest main sync 後）。
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`：pass，7 cases。
- `pwsh -NoProfile -File tools/verify.ps1`：pass，CMake/MSBuild 與 CTest 1/1。
- `pwsh -NoProfile -File tools/docs-check.ps1`：blocked_shared_baseline，429 vs 431；不得修改 integrator-owned shared baseline。
- `git diff --check`：pass。
- 最新 main sync 後 self-test、handoff、source policy、source-only policy 與 docs-check 結果已重跑；strict docs-check blocker 為 429 vs 431。
- 最新 head `0d39a17` 的 push verify run `32542052215` 與 pull_request verify run
  `32542054469` 均 completed，但同樣只在 Documentation gate 以 429 vs 431 失敗。

## Remaining work

1. 等待 integrator 合併 PR #67 / Issue #64；本 branch 不修改 shared baseline。
2. 重新執行 PR-mode docs-check 與兩條 GitHub Actions verify，並記錄 completed 結果。
3. 僅在 CI 全綠後將 handoff 更新為 `ready_for_review`。

## Next safe action

After PR #67 merges, rerun PR-mode docs-check and both GitHub verify workflows; update this handoff ready_for_review only on green CI.

## Resume commands

```powershell
git fetch origin
git merge --no-edit origin/main
$env:GITHUB_BASE_REF='main'; pwsh -NoProfile -File tools/docs-check.ps1
gh pr checks 95 --watch --interval 10
```
