---
schema_version: 2
issue: 86
branch: codex/86-build-preview-selftest
target_branch: main
base_commit: 9028924630069a0e26a3b9b19d4960e945b1d0f0
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T08:00:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-msvc-14.51.36231-sdk-10.0.28000.0-cmake-path-missing-dotnet-10.0.400-pwsh-7.6.4
scope_globs: ["tools/build-preview.ps1", "evidence/0000-foundation/build-preview-dispatch-selftest-v1.json", "docs/tasks/active/86.md"]
shared_paths: []
depends_on: ["0"]
specs: []
adrs: []
next_safe_action: "Wait for the integrator to refresh docs/state/BASELINE.md from 421 to 423, then rerun docs-check and CI; do not modify the shared baseline in this worker slice."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 86", "pwsh -NoProfile -File tools/build-preview.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 86 handoff

## Objective

Add a deterministic, no-build `-SelfTest` mode to `tools/build-preview.ps1` that verifies the four
supported target dispatch contracts and ignored output-root isolation without starting dotnet,
processes, WinUI, audio, driver, or installer work.

## Acceptance

- `-SelfTest` is bounded, offline, deterministic, and does not invoke dotnet, CMake, processes, or files outside ignored evidence output.
- `WinUI`, `WinUICompat`, `DesktopCompat`, and `ControlModel` retain their existing project identity and target-specific preview output roots.
- The normal build branches remain behaviorally unchanged except for reusing the tested dispatch metadata.
- Evidence records the self-test cases and environment limitations without claiming formal WinUI, audio, driver, HLK, or signing evidence.
- No shared integration file is modified; this slice does not change product APIs, Specs, ADRs, or runtime/audio behavior.

## Completed

- Confirmed latest `origin/main` at `9028924630069a0e26a3b9b19d4960e945b1d0f0`.
- Created the independent branch and worktree for Issue #86.
- Reviewed the active handoff contract and confirmed no overlap with Issues #52, #74, or #80.
- Claimed this Issue with this schema-v2 handoff.
- Added `-SelfTest` and a pure dispatch metadata helper in `tools/build-preview.ps1`.
- Preserved the existing ControlModel, DesktopCompat, WinUICompat, and formal WinUI build branches while reusing the tested project paths.
- Passed the bounded self-test plus sequential ControlModel, DesktopCompat launch smoke, and WinUICompat launch smoke.
- Recorded evidence in `evidence/0000-foundation/build-preview-dispatch-selftest-v1.json`.

## Known limitations

- This slice can validate PowerShell dispatch metadata only; it cannot prove a WinUI XAML build, Windows App Runtime availability, audio output, driver behavior, HLK, or signing.
- The shared `docs/state/BASELINE.md` is integrator-owned and is not in this slice's write scope.
- The formal WinUI build was not run as acceptance evidence because the locked SDK/WDK prerequisite is unavailable on this machine.
- A parallel validation attempt hit a VBCSCompiler lock in the shared control-model `obj` path; the sequential DesktopCompat rerun passed.

## Last verification

- `tools/doctor.ps1 -CheckOnly`: pass with CMake PATH and locked SDK/WDK limitations.
- `tools/probe-environment.ps1`: pass; local fingerprint remains under ignored `.local`.
- `tools/build-preview.ps1 -SelfTest`: pass, 5 cases.
- `tools/build-preview.ps1 -Target ControlModel`: pass.
- `tools/build-preview.ps1 -Target DesktopCompat -SmokeTest`: pass on sequential rerun.
- `tools/build-preview.ps1 -Target WinUICompat -SmokeTest`: pass; compatibility evidence only.
- `tools/handoff-check.ps1 -Issue 86` and full handoff check: pass.
- `tools/source-policy.ps1` and `tools/source-only-ci-check.ps1`: pass for 423 tracked paths.
- `tools/verify.ps1` with the VS2026 CMake directory prepended process-locally: pass; CTest 1/1.
- `tools/docs-check.ps1`: blocked by shared baseline count 421 vs branch count 423.
- `git diff --check`: pass.
- Draft PR #90 is open at head `c1550d1`.
- Push CI run `32535926814` / job `96936728107`: configure/test passed; Documentation gate failed at baseline 421 vs branch 423; later gates skipped.
- Pull-request CI run `32535972021` / job `96936858135`: configure/test passed; Documentation gate failed at baseline 421 vs branch 423; later gates skipped.

## Remaining work

1. Wait for the integrator-owned shared baseline to be refreshed to the current tracked-path count.
2. Rerun the documentation gate and CI from the updated main/merge ref.
3. Keep this handoff `in_progress` until CI is green; after merge, archive this handoff with the merge SHA and final CI.

## Next safe action

Wait for the integrator to refresh `docs/state/BASELINE.md`, then rerun docs-check and CI; do not modify the shared baseline in this worker slice.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 86
pwsh -NoProfile -File tools/build-preview.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
