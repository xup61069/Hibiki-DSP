---
schema_version: 2
issue: 51
branch: codex/51-baseline-counter-automation
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-git-2.x-pwsh-7.6.5-cmake-4.4.0-msvc-buildtools-17.14; github-actions-windows-2025-fetch-depth-0; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/docs-check.ps1", ".github/workflows/verify.yml", "docs/tasks/active/51.md", "evidence/0000-foundation/baseline-counter-automation-v1.json"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Implement merge-ref-aware baseline gate with bounded self-test, then run all gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 51", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/source-policy.ps1"]
---

# Issue 51 handoff

## Objective

Make the Issue #21 baseline-summary gate delta-aware so parallel slices stop failing
the Documentation gate purely because adding tracked files shifts the shared counters.
On pull requests the gate now verifies that the MERGE BASE is self-consistent
(BASELINE.md claims vs the base tree's measured tracked-path/JSON/spec/entry counts)
and only enforces strict head-tree matching when the PR itself edits BASELINE.md;
push-to-main and local runs stay fully strict and fail-closed.

## Acceptance

- A PR that only adds/removes tracked files passes the Documentation gate without a
  manual integrator counter refresh (observed blockers for PRs #31/#40/#42/#44).
- Missing summary markers, malformed summary text, and unexplained mismatches still
  fail closed in every mode.
- Push-to-main and local runs keep strict head-tree verification, so main cannot
  drift silently.
- Bounded `-SelfTest` coverage for the claim parser and comparator, mirroring the
  Issue #41 fixture style, without mutating repository files.
- Tooling/workflow-only change; SPEC-0004 evidence invariants not weakened.

## Completed

- Claimed Issue 51 with isolated worktree from `origin/main` (`cc899e0`).
- Survey confirmed `fetch-depth: 0` in `.github/workflows/verify.yml`, so the base
  ref tree is readable in CI without workflow changes.

## Known limitations

- Open PR #42 (Issue #41) also edits `tools/docs-check.ps1`; whoever merges second
  must resolve textual conflicts and keep both switch surfaces. Recommended order:
  this slice first, then #42 rebases and keeps its `-SelfTest` fixtures working.
- PR-mode tolerance applies only to the four summary counters; every other
  documentation requirement stays mandatory on the head tree.
- Local runs have no GitHub base-ref context and intentionally stay strict.

## Last verification

- Baseline not yet run at claim time; gates will be updated before the PR leaves
  draft.

## Remaining work

1. Extract claim-parser/comparator helpers and add merge-base-aware gating plus
   `-SelfTest`.
2. Record evidence; update handoff; run all gates locally.
3. Wait for GitHub Actions; coordinate merge order with PR #42.

## Next safe action

Implement merge-ref-aware baseline gate with bounded self-test, then run all gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 51
pwsh -File tools/docs-check.ps1
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/source-policy.ps1
```
