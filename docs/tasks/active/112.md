---
schema_version: 2
issue: 112
branch: codex/112-live-process-loopback-selftest
target_branch: main
base_commit: 2b96576cfc590f3cb3bb3f293b5897625b723369
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T08:32:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-msvc-14.51.36231-sdk-10.0.28000.0-cmake-path-missing-dotnet-10.0.400-pwsh-7.6.4
scope_globs: ["tools/live-process-loopback-check.ps1", "evidence/0000-foundation/live-process-loopback-selftest-v1.json", "docs/tasks/active/112.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0016]
adrs: [ADR-0002]
next_safe_action: "Implement the bounded self-test in tools/live-process-loopback-check.ps1, preserving the normal anonymous probe path, then run its self-test before recording evidence."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 112", "pwsh -NoProfile -File tools/context-pack.ps1 -Issue 112 -NoSource", "pwsh -NoProfile -File tools/live-process-loopback-check.ps1"]
---

# Issue 112 handoff

## Objective

Add a deterministic, no-build `-SelfTest` mode to `tools/live-process-loopback-check.ps1` that
validates its Windows-only guard and exact CMake/build/probe plan without configuring CMake,
compiling, starting the probe, activating Audio Service process loopback, capturing frames,
printing endpoint or process identities, or changing audio state.

## Acceptance

- `-SelfTest` is bounded, offline, deterministic, counted, and performs no file, build, process,
  COM, Audio Service, endpoint, process-loopback, or audio side effect.
- The self-test covers Windows guard rejection, exact CMake definitions, build root, target,
  executable path, and mismatched/unsafe plan rejection.
- Normal Windows probe behavior remains unchanged and its output remains anonymous format/frame
  aggregate evidence.
- Evidence states that the slice proves wrapper planning and anonymous user-space source/compile
  behavior only; it does not claim physical per-App delivery, Chrome tabCapture, driver, WaveRT,
  HLK, or signing.
- No product API, IPC schema, DSP ordering, RT rule, Spec, ADR, or shared integration snapshot changes.

## Completed

- Created Issue #112 after checking open Issues, open/draft PRs, active handoffs, and remote branch
  placeholders; no existing owner or scope overlap was found for this wrapper slice.
- Created the independent branch from latest `origin/main` at `2b96576cfc590f3cb3bb3f293b5897625b723369`.
- Read accepted SPEC-0016 and ADR-0002 context plus the wrapper and live probe source before claim.
- Added this schema-v2 ownership claim before product-file changes.
- Passed issue-specific and full handoff checks; pushed claim commit `f5c6d28` and opened Draft PR #113.
- Ran the normal probe with the VS2026 CMake directory prepended process-locally; it built the anonymous
  fixture and reported `loopback=unavailable stage=activate error=0x80004001` from the local Audio Service.

## Known limitations

- This slice is limited to wrapper planning and anonymous user-space process-loopback probe boundaries.
- It must not record endpoint IDs, friendly names, PIDs, session IDs, serials, private paths, or compiled artifacts.
- `docs/state/BASELINE.md` is integrator-owned; it is declared shared for tracked-path/JSON counters
  if the new evidence file causes strict local drift, but this worker will not modify it.
- A `loopback=unavailable` result is source/activation boundary evidence only; it is not proof of
  Chrome tabCapture, physical per-App reroute, DSP delivery, driver, WaveRT, HLK, or signing.
- The doctor gate reports CMake absent from PATH and the locked SDK/WDK prerequisite missing; the
  process-local VS2026 CMake directory is used only for local verification.

## Last verification

- `tools/doctor.ps1 -CheckOnly`: pass_with_limitations; Windows 26200, VS2026, MSVC 14.51.36231,
  .NET 10.0.400 and PowerShell 7.6.4 detected; CMake PATH and locked SDK/WDK remain limited.
- `tools/probe-environment.ps1`: pass; fingerprint is in ignored `.local/context.json`.
- `tools/context-pack.ps1 -Issue 112 -NoSource`: pass.
- `tools/handoff-check.ps1 -Issue 112`: pass.
- `tools/handoff-check.ps1`: pass.
- Normal live process-loopback probe with process-local VS2026 CMake: source build pass; anonymous
  activation result `loopback=unavailable stage=activate error=0x80004001`.

## Remaining work

1. Implement the bounded self-test without changing normal probe behavior.
2. Run the bounded self-test and record anonymous evidence plus limitations.
3. Run required local gates, update Draft PR #113, and inspect CI.

## Next safe action

Implement the bounded self-test in `tools/live-process-loopback-check.ps1`, preserving the normal anonymous probe path, then run its self-test before recording evidence.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 112
pwsh -NoProfile -File tools/context-pack.ps1 -Issue 112 -NoSource
pwsh -NoProfile -File tools/live-process-loopback-check.ps1
```
