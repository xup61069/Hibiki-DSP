---
schema_version: 2
issue: 286
branch: codex/286-calibration-peq-cap-test
target_branch: main
base_commit: b89a284c8e6dad0bdb7a3768b6265b985e51c954
status: ready_for_review
role: worker
owner: xup61069
updated_at: 2026-08-22T20:48:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-18.9.1-msvc-19.51-cmake-4.4.0-pwsh-7.6.4-sdk-26100; locked-wdk-28000-missing-on-host
scope_globs: ["tests/unit/contract_tests.cpp", "evidence/0000-foundation/calibration-peq-cap-v1.json", "docs/tasks/active/286.md"]
shared_paths: []
depends_on: [11, 16]
specs: [SPEC-0011]
adrs: []
next_safe_action: "Add bounded calibration compiler fixtures to tests/unit/contract_tests.cpp, run verify.ps1, then record evidence."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 286", "pwsh -NoProfile -File tools/verify.ps1", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 286 handoff

## Objective

Add a bounded, offline unit test for the src/hub calibration PEQ compiler so the 16-filter cap, frequency/Q validation, spacing rejection, and limited diagnostic have explicit in-memory fixtures. Keep the production compiler unchanged.

## Acceptance

* Add deterministic in-memory fixtures for valid 16-filter compilation, over-cap rejection via compile policy, invalid Q policy rejection, and invalid frequency spacing rejection; all offline, no file writes, no audio-thread allocation.
* Main compiler still validates the 16-filter cap and limited diagnostic fail-closed.
* Add anonymous evidence recording the fixture coverage and offline limitation.
* Run required gates including tools/verify.ps1 (ctest); do not claim acoustic conformance or physical audio evidence.

## Completed

* Claim audit (2026-08-22T20:15+08:00): no assignee/comments on Issue #286, no codex/286* remote branch, no docs/tasks/active/286.md; src/ lane free of active claims.
* Posted claim comment and self-assigned Issue #286.
* Created isolated worktree and branch codex/286-calibration-peq-cap-test from origin/main b89a284c8e6dad0bdb7a3768b6265b985e51c954 outside every existing AI session tree.
* Established this schema-v2 handoff.
* Added bounded offline fixtures to tests/unit/contract_tests.cpp for exact 16-filter compilation with limited diagnostic, over-cap compile-policy rejection, Q policy boundaries, spacing boundary rejection, and emitted-filter bound checks.
* Fixed a Windows min/max macro collision in the new fixture by parenthesizing std::min.
* Corrected the invalid-Q fixture to the compiler's real internal hard limit (100.0): max_q=12.5 exceeds the struct default but is still valid policy, so the test now asserts 12.5 is accepted and 100.1 is rejected.

## Known limitations

* Test-only slice; no product API, IPC/schema, DSP order, RT-thread behavior, driver boundary, identity, or release change.
* The host has Windows SDK 26100 but lacks the locked WDK 28000 package, so driver/signability build claims remain out of scope; C++ user-space ctest is unaffected.
* No acoustic conformance, room correction quality, microphone data, or ISO 226 restricted content is claimed or embedded.

## Last verification

* 2026-08-22T20:46+08:00: pwsh -NoProfile -File tools/verify.ps1 passed; hibiki_contract_tests built with VS 18 2026 / MSVC 19.51.36256 on Windows SDK 26100 and CTest reported 1/1 passed.
* Baseline gates passed on origin/main b89a284c before claiming this issue.

## Remaining work

1. Run handoff-check -Issue 286, docs-check, source-policy, source-only-ci-check, and git diff --check.
2. Add anonymous evidence/0000-foundation/calibration-peq-cap-v1.json, commit, push, and monitor draft PR CI.

## Next safe action

Run remaining review gates, add evidence manifest calibration-peq-cap-v1.json, then commit and push.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 286
pwsh -NoProfile -File tools/verify.ps1
pwsh -NoProfile -File tools/docs-check.ps1
```
