---
schema_version: 2
issue: 244
branch: codex/244-distribution-selftest-crlf
target_branch: main
base_commit: 4f5e2c6f975970fcaf1aa371dca0907d433d04c5
status: ready_for_review
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T23:45:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; vs2026-sdk-wdk-on-disk; target-24h2-machine-missing
scope_globs: ["tools/distribution-check.ps1", "docs/tasks/active/244.md", "evidence/0000-foundation/issue-244-distribution-selftest-crlf-v1.json"]
shared_paths: []
depends_on: [39]
specs: []
adrs: []
next_safe_action: "Integrator review/merge of PR #248 after final metadata-head CI; then archive this handoff under completed/2026/244.md."
implementation_commit: cf845d7
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 244", "git diff --check"]
---

# Issue 244 handoff: CRLF-safe distribution-profile self-test

## Objective

修復已合併的 Issue #39 `tools/distribution-check.ps1 -SelfTest` 在 Windows CRLF
PowerShell 下的 `duplicate-root-key` fixture regression，讓 bounded self-test sweep
穩定驗證 duplicate root key rejection；不改變 distribution profile contract 或產品行為。

## Acceptance

- `pwsh -NoProfile -File tools/distribution-check.ps1 -SelfTest` 在 Windows CRLF PowerShell pass。
- `pwsh -NoProfile -File tools/distribution-check.ps1` 對 repository profile pass。
- `tools/verify.ps1` 的 Gate self-test sweep 不再因 `duplicate-root-key` 失敗。
- Required source/docs gates、`git diff --check` 與 CI pass，evidence 記錄 line-ending boundary。

## Completed

- 已建立 Issue #244，確認 failure 只發生在 merge-ref target `origin/main=4f5e2c6` 的 distribution self-test；PR #239 push head 仍 pass。
- 已從最新 `origin/main=4f5e2c6f975970fcaf1aa371dca0907d433d04c5` 建立獨立 worktree 與 branch；目前只建立本 claim handoff。
- 已完成 Issue #244 claim handoff check、push claim commit `5cd4244`；live ownership 可見後才修改 `tools/distribution-check.ps1`。
- 已以最小 self-test-only patch 將 fixture newline 正規化為 LF，使 `duplicate-root-key` replacement 在 CRLF checkout 下確實注入 duplicate key；未改變 repository profile validation。

## Known limitations

- 本 lane 只修復 gate self-test fixture 的 line-ending robustness，不改變 distribution identity、產品 API、IPC/schema、DSP、RT audio、driver、HLK 或 signing 行為。
- Target machine 尚未提供；本修正不產生 physical audio、loadable driver、HLK 或 Microsoft signing evidence。

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 244`：pass。
- `pwsh -NoProfile -File tools/distribution-check.ps1 -SelfTest`：pass（11 cases）。
- `pwsh -NoProfile -File tools/distribution-check.ps1`：pass。
- `pwsh -NoProfile -File tools/verify.ps1 -SelfTest`：pass（5 cases）。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass（full active ownership）。
- `pwsh -NoProfile -File tools/docs-check.ps1`：pass（85 required paths / 24 specs；526 tracked paths / 116 repository JSON files）。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass（526 tracked paths）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass（1 workflow / 526 tracked paths）。
- `pwsh -NoProfile -File tools/verify.ps1`：pass（Visual Studio 2026/CMake build；CTest 1/1；self-test sweep includes distribution-check）。
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`：pass（Windows 11 build 26200、VS 2026、SDK/WDK QFE fingerprint）。
- `pwsh -NoProfile -File tools/probe-environment.ps1`：pass（anonymous `.local/context.json` only）。
- PR #248 push verify：pass，run `32567445909` / job `97018037913`。
- PR #248 merge-ref verify：pass，run `32567458470` / job `97018069105`。
- `git diff --check`：pass。

## Remaining work

1. Integrator review/merge of PR #248 after final metadata-head CI.
2. After merge, archive `docs/tasks/active/244.md` to `docs/tasks/completed/2026/244.md` with merge commit and final CI.

## Next safe action

Integrator review/merge of PR #248 after final metadata-head CI; then archive this handoff under completed/2026/244.md.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 244
git diff --check
```
