---
schema_version: 2
issue: 76
branch: codex/76-probe-environment-selftest
target_branch: main
base_commit: 0254f468d1526c9e03c7f3bb51188f04e405d7c2
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: unknown
scope_globs: ["tools/probe-environment.ps1", "evidence/0000-foundation/probe-environment-selftest-v1.json", "docs/tasks/active/76.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: []
adrs: []
next_safe_action: "Implement the bounded in-memory probe self-test, then run its self-test and required repository gates."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/probe-environment.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 76"]
---

# Issue 76 handoff

## Objective

在不查詢本機、建立 `.local` 檔案、啟動外部工具或列舉音訊 endpoint 的前提下，為 environment fingerprint probe 增加 bounded `-SelfTest`，以記憶體 fixture 驗證匿名 schema 與 privacy boundary，並保留正常 `.local/context.json` 輸出行為。

## Acceptance

- 抽出可重用的純函式，建立並驗證現有匿名 probe object；正常模式仍寫入相同的 `.local/context.json` schema。
- `-SelfTest` 以計數結果覆蓋 valid schema、required OS/tool/audio fields、supported-format bounds、malformed/missing fields、forbidden identity/private-path keys 與 secret-like text rejection。
- self-test 分支不得呼叫 CIM、建立目錄或檔案、執行 cmake/git、列舉音訊 endpoint，亦不得輸出真實 machine identity。
- 新增匿名 evidence 與本 schema-v2 handoff；不修改 product API、IPC/schema、DSP、RT thread、driver、signing、installer、Spec 或 ADR。

## Completed

- 已從 `origin/main` `0254f468d1526c9e03c7f3bb51188f04e405d7c2` 建立本 Issue 專用 branch/worktree。
- 已確認 `docs/state/BASELINE.md` 為 integrator-owned shared path，本 worker 不修改。

## Known limitations

- 此 slice 只驗證 source-level probe schema/privacy 與 user-space tooling；不代表實體音訊、driver、WaveRT、HLK、Microsoft signing 或任何硬體驗收。
- `.local/context.json` 僅為本機暫存輸出，真實 endpoint identity、校正資料與私人路徑不進 Git。

## Last verification

- Claim 建立後待執行 handoff self-check。

## Remaining work

1. 實作 bounded in-memory `-SelfTest` 與 reusable probe construction/validation helpers。
2. 執行 self-test、正常 probe 與 repository required gates。
3. 新增匿名 evidence、更新本 handoff、提交、push 並建立 Draft PR。

## Next safe action

Implement the bounded in-memory probe self-test, then run its self-test and required repository gates.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/probe-environment.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 76
```
