---
schema_version: 2
issue: 76
branch: codex/76-probe-environment-selftest
target_branch: main
base_commit: 0254f468d1526c9e03c7f3bb51188f04e405d7c2
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T06:52:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.4-msvc-buildtools-17.14
scope_globs: ["tools/probe-environment.ps1", "evidence/0000-foundation/probe-environment-selftest-v1.json", "docs/tasks/active/76.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: []
adrs: []
next_safe_action: "Stage only the Issue 76 evidence and handoff paths, commit them, push the branch, and create the Draft PR."
resume_commands: ["pwsh -NoProfile -File tools/probe-environment.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 76", "git add -- evidence/0000-foundation/probe-environment-selftest-v1.json docs/tasks/active/76.md", "git commit -m issue-76-probe-evidence", "git push"]
---

# Issue 76 handoff

## Objective

在不查詢本機、建立 `.local` 檔案、啟動外部工具或列舉音訊 endpoint 的前提下，為 environment fingerprint probe 增加 bounded `-SelfTest`，以記憶體 fixture 驗證匿名 schema 與 privacy boundary，並保留正常 `.local/context.json` 輸出行為。

## Acceptance

- 抽出可重用的純函式，建立並驗證現有匿名 probe object；正常模式仍寫入相同的 `.local/context.json` schema。
- `-SelfTest` 以計數結果覆蓋 valid schema、required OS/tool/audio fields、supported-format bounds、malformed/missing fields、forbidden identity/private-path keys 與 secret-like text rejection。
- self-test 分支不得呼叫 CIM、建立目錄或檔案、執行 cmake/git、列舉音訊 endpoint，亦不得輸出真實 machine identity。
- 新增匿名 evidence 與本 schema-v2 handoff；不修改 product API、IPC/schema、DSP、RT thread、driver、signing、installer、Spec 或 ADR。

## Completed

- 已從 `origin/main` `0254f468d1526c9e03c7f3bb51188f04e405d7c2` 建立本 Issue 專用 branch/worktree。
- 已確認 `docs/state/BASELINE.md` 為 integrator-owned shared path，本 worker 不修改。
- 已加入 `tools/probe-environment.ps1 -SelfTest` 的純記憶體 schema/privacy 驗證與正常模式共用 construction/validation helpers。
- self-test 目前 12/12 cases pass；正常模式已成功寫入並讀回 ignored `.local/context.json`。
- 已新增 `evidence/0000-foundation/probe-environment-selftest-v1.json`，記錄實際 commands、coverage 與限制。

## Known limitations

- 此 slice 只驗證 source-level probe schema/privacy 與 user-space tooling；不代表實體音訊、driver、WaveRT、HLK、Microsoft signing 或任何硬體驗收。
- `.local/context.json` 僅為本機暫存輸出，真實 endpoint identity、校正資料與私人路徑不進 Git。
- `tools/docs-check.ps1` strict local counter 目前因 integrator-owned `docs/state/BASELINE.md` 仍記錄 419 tracked paths，而本 branch 為 421 tracked paths（含 evidence 與 handoff）而受阻；本 worker 不修改 shared baseline。

## Last verification

- `pwsh -NoProfile -File tools/probe-environment.ps1 -SelfTest`：pass，12/12。
- `pwsh -NoProfile -File tools/probe-environment.ps1`：pass，schema v1、3 個 bounded LPCM formats read back。
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`：pass，7 cases。
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 76`：pass。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass，421 tracked paths（evidence 與 handoff staged）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass，1 workflow、421 tracked paths（evidence 與 handoff staged）。
- `pwsh -NoProfile -File tools/verify.ps1`：pass，CMake/MSBuild 與 CTest 1/1。
- `pwsh -NoProfile -File tools/docs-check.ps1`：blocked_shared_baseline，419 vs 421；不得修改 integrator-owned shared baseline。
- `git diff --check`：pass。

## Remaining work

1. 提交 evidence 與 handoff 更新，push 本 branch。
2. 建立 Draft PR，等待 GitHub Actions verify 完成並記錄 run 結果。
3. 若 integrator 合併 baseline counter fix，重新執行 strict docs-check 並更新 evidence/handoff。

## Next safe action

Stage only the Issue 76 evidence and handoff paths, commit them, push the branch, and create the Draft PR.

## Resume commands

```powershell
pwsh -NoProfile -File tools/probe-environment.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 76
git add -- evidence/0000-foundation/probe-environment-selftest-v1.json docs/tasks/active/76.md
git commit -m issue-76-probe-evidence
git push
```
