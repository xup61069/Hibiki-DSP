---
schema_version: 2
issue: 70
branch: codex/70-engine-preview-ipc-selftest
target_branch: main
base_commit: 787e682438eb055ac6e8630294aca7e6d9eb4745
status: ready_for_review
role: worker
owner: xup61069
updated_at: 2026-08-22T10:32:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["tools/engine-preview-smoke.ps1", "evidence/0000-foundation/engine-preview-ipc-selftest-v1.json", "docs/tasks/active/70.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0024]
adrs: []
next_safe_action: "Merge PR #71 once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 70", "pwsh -NoProfile -File tools/engine-preview-smoke.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 70 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/engine-preview-smoke.ps1` that exercises its in-memory v1 IPC frame construction and wire-shape assertions without building, launching, connecting to, or modifying Engine Preview.

## Acceptance

- Extract reusable pure helpers for v1 frame construction and bounded frame-shape validation while preserving the normal cross-process smoke behavior and output.
- `-SelfTest` covers valid empty/payload frames, little-endian type/length/request correlation, short or oversized frames, and payload-length mismatch rejection.
- Self-test performs no process launch, named-pipe connection, file write, device enumeration, endpoint identity output, or physical-audio operation.
- Normal smoke still requires the existing built Engine Preview and remains the only path that launches it.
- Required local gates and CI results pass.

## Completed

- Rebased on latest `origin/main` at `787e682438eb055ac6e8630294aca7e6d9eb4745`.
- Added pure `New-IpcFrame` and `Assert-IpcFrameShape` helpers and 7-case bounded `-SelfTest` in `tools/engine-preview-smoke.ps1`.
- Added anonymous evidence at `evidence/0000-foundation/engine-preview-ipc-selftest-v1.json`.
- Verified all local gates pass.

## Known limitations

- The self-test is deterministic PowerShell helper evidence; normal smoke is user-space cross-process evidence only.

## Last verification

- `tools/engine-preview-smoke.ps1 -SelfTest`: pass, 7 cases.
- `tools/handoff-check.ps1 -Issue 70`: pass.
- `tools/handoff-check.ps1`: pass.
- `tools/source-policy.ps1`: pass for 460 tracked paths.
- `tools/source-only-ci-check.ps1`: pass.
- `tools/docs-check.ps1`: pass (85 required paths, 24 specs, 460 tracked paths, 94 repository JSON files).
- `tools/verify.ps1`: pass (CTest 1/1).
- `git diff --check`: pass.

## Remaining work

1. Wait for GitHub Actions verification on PR #71.
2. Merge PR #71.

## Next safe action

Merge PR #71 once GitHub Actions verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 70
pwsh -NoProfile -File tools/engine-preview-smoke.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
