---
schema_version: 2
issue: 70
branch: codex/70-engine-preview-ipc-selftest
target_branch: main
base_commit: 09775449f331878c58926f42a15596244875fdb2
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T06:37:36+08:00
environment_fingerprint: windows-26200-amd64-vs2026-msvc-19.51.36256.0-sdk-10.0.28000.0-cmake-4.3.1-dotnet-10.0.400-pwsh-7.6.4
scope_globs: ["tools/engine-preview-smoke.ps1", "evidence/0000-foundation/engine-preview-ipc-selftest-v1.json", "docs/tasks/active/70.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: ["0"]
specs: [SPEC-0024]
adrs: []
next_safe_action: "Wait for the integrator-owned BASELINE.md tracked-path refresh, then rerun docs-check and the Draft PR CI; do not edit the shared baseline from this lane."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 70", "pwsh -NoProfile -File tools/context-pack.ps1 -Issue 70 -NoSource", "pwsh -NoProfile -File tools/engine-preview-smoke.ps1 -SelfTest"]
---

# Issue 70 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/engine-preview-smoke.ps1` that exercises its in-memory v1 IPC frame construction and wire-shape assertions without building, launching, connecting to, or modifying Engine Preview.

## Acceptance

- Extract reusable pure helpers for v1 frame construction and bounded frame-shape validation while preserving the normal cross-process smoke behavior and output.
- `-SelfTest` covers valid empty/payload frames, little-endian type/length/request correlation, short or oversized frames, and payload-length mismatch rejection.
- Self-test performs no process launch, named-pipe connection, file write, device enumeration, endpoint identity output, or physical-audio operation.
- Normal smoke still requires the existing built Engine Preview and remains the only path that launches it.
- Add anonymous evidence and this schema-v2 handoff; no product source, IPC schema, DSP, RT-thread, driver, UI, Spec, or ADR change.

## Completed

- Claimed Issue #70 and reserved the independent scope on `codex/70-engine-preview-ipc-selftest` from `origin/main` at `09775449f331878c58926f42a15596244875fdb2`.
- Read the canonical repository, multi-agent, conflict, change-contract, and active-handoff instructions before editing.
- Added the pure `New-IpcFrame` bound and `Assert-IpcFrameShape` validator plus a 7-case in-memory `-SelfTest`.
- Reused the validator for normal Engine Preview Hello/Ack, status, catalog, volume, session, and IR reply checks without changing the runtime IPC contract.
- Added anonymous evidence at `evidence/0000-foundation/engine-preview-ipc-selftest-v1.json`.

## Known limitations

- The self-test will be deterministic source-only evidence for the PowerShell IPC helpers; it cannot prove Engine Preview launch, named-pipe delivery, physical endpoint delivery, driver, WaveRT, HLK, signing, or real audio behavior.
- `docs/state/BASELINE.md` is integrator-owned; this worker may record a count mismatch but will not edit that shared path.

## Last verification

- `pwsh -NoProfile -File tools/engine-preview-smoke.ps1 -SelfTest`: pass (7 cases; no process, pipe, or file write).
- `pwsh -NoProfile -File tools/build-engine-preview.ps1`: pass with VS 2026/CMake; outputs remained ignored under `.local/`.
- `pwsh -NoProfile -File tools/engine-preview-smoke.ps1`: pass (anonymous catalog count, Hello/Ack, volume/status, and IR prepare ACK).
- `pwsh -NoProfile -File tools/engine-preview-smoke.ps1 -EnableSystemVolume -EnableSessionRouting -StatusOnly`: pass; no system-volume write and per-App delivery remains unverified.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 70` and full handoff check: pass.
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass for 419 tracked paths; `tools/source-only-ci-check.ps1`, JSON parse, and `git diff --check`: pass.
- `pwsh -NoProfile -File tools/verify.ps1`: pass; CTest 1/1 passed.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked by integrator-owned `docs/state/BASELINE.md` (claims 417 tracked paths; this branch reports 419 after the evidence file was staged).
- CI push run `32533554894`: completed failure only at Documentation gate with the same 417 vs 419 baseline mismatch; later gates were skipped.
- CI pull-request run `32533558944`: completed failure only at Documentation gate with the same 417 vs 419 baseline mismatch; later gates were skipped.

## Remaining work

1. Wait for the shared baseline refresh, then rerun `docs-check.ps1` and inspect both Draft PR CI runs.
2. If all required CI is green, update this handoff to `ready_for_review`; otherwise retain the exact failure and next safe action.

## Next safe action

Wait for Issue 0 to refresh the shared tracked-path baseline, then run `pwsh -NoProfile -File tools/docs-check.ps1` and inspect PR #71 CI. Do not modify `docs/state/BASELINE.md` in this lane.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 70
pwsh -NoProfile -File tools/context-pack.ps1 -Issue 70 -NoSource
pwsh -NoProfile -File tools/engine-preview-smoke.ps1 -SelfTest
```
