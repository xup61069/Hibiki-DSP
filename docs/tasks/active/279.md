---
schema_version: 2
issue: 279
branch: codex/279-verify-selftest-expand
target_branch: main
base_commit: c6516e1262ad65a8483ef9bc8295281ad9914572
status: ready_for_review
role: worker
owner: hibiki-worker
updated_at: 2026-08-22T21:30:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/verify.ps1", "evidence/0000-foundation/verify-selftest-expand-v1.json", "docs/tasks/active/279.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Push and open draft PR; wait for CI verify."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/verify.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 279", "pwsh -NoProfile -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 279 handoff: Expand verify self-test to cover missing cmake and ctest failure

## Objective

Expand the bounded, offline self-test for `tools/verify.ps1` so each fail-closed path has an explicit regression fixture, while keeping the production `cmake`/`ctest` orchestration unchanged.

## Acceptance

- Add deterministic in-memory fixtures for missing `cmake` binary, `cmake` configure failure, `ctest` failure, and successful `ctest` with mocked executables, while preserving existing valid and missing-source cases.
- Keep the self-test bounded and offline: no repository writes, process launches, device access, or private paths; use mocked `Get-Command`/`Start-Process` or in-memory stubs.
- Main `verify.ps1` still runs real `cmake`/`ctest` on the host and fails closed on missing tools or test failures.
- Add anonymous evidence recording the case count and offline limitation.
- Update a schema-v2 active handoff with exclusive scope, dependencies, verification commands, and CI evidence.
- Run the repository-required gates; do not claim driver, WaveRT, HLK, or Microsoft-signing evidence.

## Completed

- Created Issue #279 and verified no active handoff claims `tools/verify.ps1` (2026-08-22T21:00 scan: `git ls-remote --heads origin` no `codex/279*`, active handoffs include 273 batch but distinct verify lane).
- Created isolated worktree at `C:\AI\Hibiki-DSP-279-verify-selftest` on branch `codex/279-verify-selftest-expand` from `origin/main@c6516e1`.
- Established schema v2 handoff `docs/tasks/active/279.md`.
- Expanded `tools/verify.ps1` SelfTest from 5 to 10 cases by adding `Invoke-MockVerifyExecution` helper and fixtures for missing cmake, configure failure, build failure, ctest failure, and mocked success (all offline, no process launch).
- Created anonymous evidence `evidence/0000-foundation/verify-selftest-expand-v1.json` with case list and tool versions.
- Verified all required gates pass locally (see Last verification).

## Known limitations

- Tooling/governance only; does not produce ctest run or claim driver signing.
- Self-test is offline and mocked; real `cmake`/`ctest` still requires locked target toolchain.

## Last verification

- `pwsh -NoProfile -File tools/verify.ps1 -SelfTest`: pass (10 cases: 5 plan + 5 mocked execution)
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 279`: pass
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass (18 active handoffs, no scope overlap)
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: pass (13 cases)
- `pwsh -NoProfile -File tools/docs-check.ps1`: pass (85 required / 24 specs; live 547/126)
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass (547 tracked)
- `git diff --check`: pass (exit 0)
- `evidence/0000-foundation/verify-selftest-expand-v1.json`: valid JSON

## Remaining work

None for this slice; awaiting CI verify and review.

## Next safe action

Push and open draft PR; wait for CI verify.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/verify.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 279
pwsh -NoProfile -File tools/docs-check.ps1
git diff --check
```
