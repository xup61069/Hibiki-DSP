---
schema_version: 2
issue: 251
branch: codex/251-archive-244
target_branch: main
base_commit: 1fd76a74906d95ae65d5ece08410ab45b70a1b82
status: ready_for_review
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-23T01:45:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; vs2026-sdk-wdk-on-disk; target-24h2-machine-missing
scope_globs: ["docs/tasks/active/244.md", "docs/tasks/completed/2026/244.md", "evidence/0000-foundation/issue-244-distribution-selftest-crlf-v1.json", "docs/tasks/active/251.md", "evidence/0000-foundation/issue-251-archive-244-v1.json"]
shared_paths: []
depends_on: [39, 244, 248]
specs: []
adrs: []
archive_commit: f3db88f
next_safe_action: "Integrator review/merge of PR #253 after final metadata-head CI; then archive this handoff under completed/2026/251.md."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 251", "git diff --check"]
---

# Issue 251 handoff: archive merged Issue 244

## Objective

完成已由 PR #248 合併的 Issue #244 post-merge archive：將 active handoff 移至
`docs/tasks/completed/2026/244.md`，保留 CRLF-safe fixture 修正、PR #248 merge
與兩條 CI evidence，並記錄後續 metadata takeover 邊界；不接續或改寫已合併的
`codex/244-distribution-selftest-crlf` branch。

## Acceptance

- `docs/tasks/active/244.md` 移至 `docs/tasks/completed/2026/244.md`，保留 Issue #244 scope、implementation commit、root cause、limitations 與 final CI history。
- `evidence/0000-foundation/issue-244-distribution-selftest-crlf-v1.json` 保留並補齊 final merge/archive records。
- `docs/tasks/active/251.md` 保持 schema v2，記錄 exact base/head、archive commit、final CI 與限制。
- 新增 anonymous post-merge evidence 只描述文件封存、merge/takeover 與可重現檢查，不宣稱產品、RT audio、driver、HLK 或 signing 能力。
- Required handoff/docs/source-policy/source-only checks、`tools/verify.ps1` 與 `git diff --check` 通過；queued/in_progress 不算 CI 通過。

## Completed

- 已確認 PR #248 implementation head `2a797e47c892cf5652fad07807ac3d5c2c84d72b` 已合併至 `main` at `1fd76a74906d95ae65d5ece08410ab45b70a1b82`。
- 已確認 PR #248 push/merge-ref verify 均 pass（runs `32567445909`、`32567458470`），而後續 metadata commits `db3e3c7`、`58ab51f` 不在 main；本 lane 不接續舊 branch。
- 已從最新 `origin/main=1fd76a74906d95ae65d5ece08410ab45b70a1b82` 建立獨立 worktree 與 branch；目前只建立本 claim handoff。
- 已完成 Issue #251 claim handoff check、push claim commit `16825ea`；live ownership 可見後才展開 archive scope。
- 已將 `docs/tasks/active/244.md` 移至 `docs/tasks/completed/2026/244.md`，保留 PR #248 implementation merge、CI 與 metadata takeover boundary。
- 已完成 Issue #244 evidence 更新並新增 #251 anonymous post-merge evidence；內容只記錄 governance/documentation archive。

## Known limitations

- 本 lane 是 governance/documentation archive，不改變 distribution contract、產品 API、IPC/schema、DSP、RT audio、driver、HLK 或簽章行為。
- Target machine 尚未提供；archive 與 user-space gates 不等於 physical audio、loadable driver、HLK 或 Microsoft signing evidence。
- PR #248 merge 與未進 main 的後續 metadata commits 會保留在 evidence，不把舊 branch 變更宣稱為 main history。

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 251`：pass（claim scope）。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass（expanded scope）。
- `pwsh -NoProfile -File tools/docs-check.ps1`：pass（85 required paths / 24 specs；529 tracked paths / 117 repository JSON files）。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass（529 tracked paths）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass（1 workflow / 529 tracked paths）。
- `pwsh -NoProfile -File tools/verify.ps1`：pass（Visual Studio 2026/CMake build；CTest 1/1；distribution self-test sweep）。
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`：pass（Windows 11 build 26200、VS 2026、SDK/WDK QFE fingerprint）。
- `pwsh -NoProfile -File tools/probe-environment.ps1`：pass（anonymous `.local/context.json` only）。
- PR #253 push verify：pass，run `32567869924` / job `97019043216`。
- PR #253 merge-ref verify：pass，run `32567882278` / job `97019073872`。
- `git diff --check`：pass。

## Remaining work

1. Integrator review/merge of PR #253 after final metadata-head CI.
2. After merge, archive `docs/tasks/active/251.md` to `docs/tasks/completed/2026/251.md` with merge commit and final CI.

## Next safe action

Integrator review/merge of PR #253 after final metadata-head CI; then archive this handoff under completed/2026/251.md.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 251
git diff --check
```
