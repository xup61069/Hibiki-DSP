---
schema_version: 2
issue: 6
branch: codex/6-engine-session-volume-integration
target_branch: main
base_commit: 08958f7a055a06616a10910bdf4854943a94df1b
status: in_progress
role: integrator
owner: codex-primary
updated_at: 2026-08-22T02:33:09+08:00
environment_fingerprint: github-actions-windows-2025; integration-only-local
scope_globs: ["tests/live_engine_session_volume_probe.cpp", "tests/CMakeLists.txt", "tools/live-session-volume-check.ps1", "evidence/0000-foundation/session-volume-live-v1.json", "docs/tasks/active/6.md"]
shared_paths: ["AGENTS.md", "README.md", "docs/AI_HANDOFF.md", "docs/state/BASELINE.md", "docs/tasks/active/0.md"]
depends_on: []
specs: [SPEC-0018]
adrs: []
next_safe_action: "Resolve the Issue 0 handoff merge, run required gates and open the integration PR."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 6", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 6 handoff

## Objective

把另一台 AI 在 PR #2 合併後留於 `codex/preview-runtime-fix@08958f7` 的 Engine Preview
session-volume queue probe 整合進最新 main，同時保留多 AI v2 handoff 與 integrator ownership。

## Acceptance

- 保留 commits `22ce3e5`、`9d4802a`、`08958f7` 的 probe、tool、evidence 與限制說明。
- 不直接修改或 force-push 另一台 AI 的 branch；由本 Issue 的獨立整合 branch 進 main。
- main 的多 AI protocol、v2 Issue 0 handoff 與 completed Issue 3 archive 不退化。
- required GitHub Actions 全部通過。

## Completed

- 建立 Issue #6 與獨立 integration branch。
- 確認 source branch head `08958f7` 的 push CI `32513577497` 全綠。
- 合併最新 main；唯一內容衝突限於 integrator-owned Issue 0 handoff。

## Known limitations

- Probe 驗證 Engine Preview IPC/control queue/COM worker 的 session-volume roundtrip，不代表實體
  per-App capture/re-send 或 DSP delivery。

## Last verification

- Source branch GitHub Actions run `32513577497`: passed all verify steps。

## Remaining work

1. 完成 Issue 0 handoff conflict resolution並執行 gates。
2. Push integration branch、開 PR、確認 push/PR CI。
3. Merge 後封存本 handoff。

## Next safe action

Resolve the Issue 0 handoff merge, run required gates and open the integration PR.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 6
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
