---
schema_version: 2
issue: 325
branch: codex/325-archive-309
target_branch: main
base_commit: e576aeb397211998598a1eb0082964ad60e4d3e8
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T22:28:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-sdk-10.0.28000.2526-wdk-10.0.28000.2526-cmake-4.4.2-pwsh-7.6.4
scope_globs: ["docs/tasks/active/309.md", "docs/tasks/completed/2026/309.md", "evidence/0000-foundation/issue-325-archive-309-v1.json", "docs/tasks/active/325.md"]
shared_paths: []
depends_on: [309, 312]
specs: [SPEC-0009]
adrs: []
next_safe_action: "Run the Issue 325 baseline and inspect the merged Issue 309 handoff before editing."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 325", "pwsh -NoProfile -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 325 handoff

## Objective

Archive the merged Issue #309 CSP hardening handoff by moving
`docs/tasks/active/309.md` to `docs/tasks/completed/2026/309.md`, preserving its SPEC-0009
reference, implementation/evidence history, PR #312 merge commit and final CI, and adding
anonymous documentation-only archive evidence.

## Acceptance

- Move only `docs/tasks/active/309.md` to `docs/tasks/completed/2026/309.md` and preserve its
  scope, SPEC-0009 reference, limitations, implementation commit, and CI history.
- Record PR #312 merge commit `0413436041c6c849ff5cc10c642c74d0728e24ce` and final CI references.
- Add one anonymous evidence JSON under `evidence/0000-foundation/` with static-policy and
  documentation-only limitations.
- Run scoped/full handoff, docs self-test/docs-check, source-policy, source-only CI, doctor,
  verify, and `git diff --check` where supported; queued CI is not pass.
- Keep this archive independent from Issue #318 / PR #324 and all other archive lanes; no product,
  tools, extension source, driver, DSP, IPC, or shared snapshot changes.

## Completed

- Fetched and confirmed latest `origin/main` at
  `e576aeb397211998598a1eb0082964ad60e4d3e8`.
- Confirmed PR #312 is merged at `0413436041c6c849ff5cc10c642c74d0728e24ce` and final CI passed:
  push verify run `32576024204` / job `97038398135`, merge-ref verify run `32576021965` /
  job `97038392692`.
- Audited open Issues, PRs, remote branches, and active handoffs; no other lane owns the Issue
  #309 archive paths.
- Claimed Issue #325 as `ox-alpha-opencode` in an isolated worktree.
- Attempted `pwsh -NoProfile -File tools/docs-check.ps1 -WriteCounters`; this current gate rejects
  the parameter and current main has no `build/baseline-counters.json`, so no uncontracted counter
  file is added to this archive slice.

## Known limitations

- Documentation/governance archive only; no browser runtime, audio, endpoint, driver, WaveRT, HLK,
  signing, or hardware behavior changes.
- Static policy and archive checks do not prove extension runtime or physical audio behavior.
- The current main gate exposes no `-WriteCounters` parameter and no baseline-counters JSON; this
  governance/tooling limitation is recorded rather than worked around with an invented schema.

## Last verification

- Baseline verification is pending before the archive move.

## Remaining work

1. Run the Issue 325 baseline and confirm the exact archive scope.
2. Move the handoff, add evidence, and run all required gates.
3. Commit single-purpose changes, push, open a Draft PR, wait for CI, and request integrator review;
   this worker must not merge.

## Next safe action

Run the Issue 325 baseline and confirm the exact archive scope.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 325
pwsh -NoProfile -File tools/docs-check.ps1
git diff --check
```
