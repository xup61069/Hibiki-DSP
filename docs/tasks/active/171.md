---
schema_version: 2
issue: 171
branch: codex/171-counter-refresh
target_branch: main
base_commit: 6b139fb8c4f8b6caf0981cb7f92dd9c5cd00a4c5
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T13:55:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-dotnet10-no-vs2026-no-wdk; github-actions-windows-2025-ci
scope_globs: ["build/baseline-counters.json", "docs/tasks/active/171.md"]
shared_paths: []
depends_on: [169]
specs: []
adrs: []
next_safe_action: "Run tools/docs-check.ps1 -WriteCounters, verify strict gates, commit and open PR."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 171"]
---

# Issue 171 handoff

## Objective

Refresh `build/baseline-counters.json` after the Issue #169 archive merge added one tracked path. The post-merge push run `32555002242` failed the strict docs-check gate with: counters JSON claims 472 tracked paths but git reports 473.

## Acceptance

- `build/baseline-counters.json` matches git ls-files counts exactly (tracked paths and repository JSON files).
- Strict local docs-check passes without GITHUB_BASE_REF override.
- GitHub Actions verify passes on main after merge (push event).
- Only `build/baseline-counters.json` and this handoff file change.

## Completed

- Confirmed run `32555002242` (post-merge push of PR #170) failed on docs-check strict counter validation.
- Confirmed origin/main tree has 473 tracked files while counters JSON still claims 472.
- Created this schema-v2 claim from fetched `origin/main` at
  `6b139fb8c4f8b6caf0981cb7f92dd9c5cd00a4c5`.

## Known limitations

- This is a mechanical maintenance slice; it adds no product behavior or evidence beyond the gate fix itself.

## Last verification

- Pending: -WriteCounters refresh and strict local gates to be recorded before push.

## Remaining work

1. Run `pwsh -File tools/docs-check.ps1 -WriteCounters`.
2. Run strict local gates (docs-check, source-policy, source-only-ci-check).
3. Commit, open PR, record CI, merge.

## Next safe action

Run tools/docs-check.ps1 -WriteCounters, verify strict gates, commit and open PR.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 171
```
