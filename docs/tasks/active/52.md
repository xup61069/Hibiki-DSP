---
schema_version: 2
issue: 52
branch: codex/52-winui-xaml-build-diagnostic
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T05:28:33+08:00
environment_fingerprint: windows-26200-amd64-vs2026-18.9.1-universal-sdk-wdk-10.0.28000.2526-cmake-4.3.1-dotnet-8.0.424-pwsh-7.6.5
scope_globs: ["tools/build-preview.ps1", "apps/winui-shell/MainWindow.xaml", "apps/winui-shell/Hibiki.WinUI.csproj", "apps/control-model/EasyControlViewModel.cs", "apps/control-model/ExpertSurfaceModel.cs", "evidence/0000-foundation/winui-xaml-build-diagnostic-v1.json", "docs/tasks/active/52.md"]
shared_paths: []
depends_on: []
specs: []
adrs: []
next_safe_action: "Run the formal WinUI baseline and capture actionable XamlCompiler diagnostics before changing the locked package or project scope."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/build-preview.ps1 -Target WinUI", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 52", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 52 handoff

## Objective

Make the formal `tools/build-preview.ps1 -Target WinUI` preview build either pass on the locked
Windows 11/VS 2026/Windows App SDK toolchain or fail with actionable, reproducible XAML compiler
diagnostics. The current target machine has the Visual Studio Universal workload, but the locked
Windows App SDK 1.7.250401001 `XamlCompiler.exe` exits 1 without diagnostic output or `output.json`.

## Acceptance

- Reproduce the formal WinUI failure on the locked target and identify whether it is caused by project input, package/toolchain compatibility, or missing diagnostic plumbing.
- If a safe in-scope repair exists, implement it without weakening source-only, RT-thread, driver, signing, or physical-audio boundaries.
- If the locked package/toolchain cannot be repaired safely, add actionable fail-closed diagnostics and record the exact limitation; do not relabel DesktopCompat or WinUICompat as formal WinUI evidence.
- Add anonymous evidence and preserve schema-v2 handoff ownership; do not commit binaries, private paths, registry dumps, endpoint identities, or generated build output.
- Required local gates and GitHub Actions verify pass, subject only to the documented integrator-owned BASELINE refresh.

## Completed

- Created Issue #52 and claimed the independent WinUI build/tooling slice.
- Created this schema-v2 handoff from `origin/main` `cc899e0`.
- Confirmed no overlap with Issue #28's `tools/winui-shell-check.ps1` source accessibility gate or Issue #30's doctor scope.
- Reproduced the locked-package failure and used a process-only Windows App SDK 1.8.260710003 override to expose seven actionable `WMC0011 Unknown member 'StringFormat'` locations in `MainWindow.xaml`.
- Expanded the declared scope before implementation to include the existing control-model display projection and the affected XAML bindings; no IPC, DSP, RT-thread, driver, or permanent-identity boundary changes are planned.

## Known limitations

- Formal WinUI/XAML, UI Automation/accessibility, physical audio, driver/WaveRT, HLK, Microsoft signing, and release packaging evidence are out of scope unless separately proven.
- The current Windows App SDK package is locked at 1.7.250401001; package upgrades require evidence and review rather than an unrecorded workaround.
- Adding this handoff/evidence will require the integrator to refresh the shared BASELINE counters before the PR merge-tree documentation gate can be green.

## Last verification

- Claim baseline only; formal WinUI reproduction and diagnostic capture are next.

## Remaining work

1. Run the formal WinUI target and capture the compiler command, exit code, and any available diagnostics without committing generated output.
2. Inspect only in-scope project/tooling inputs and implement the smallest safe repair or fail-closed diagnostic.
3. Add anonymous evidence, rerun local gates, push the branch, and open a Draft PR for Issue #52.

## Next safe action

Run the formal WinUI baseline and capture actionable XamlCompiler diagnostics before changing the locked package or project scope.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/build-preview.ps1 -Target WinUI
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 52
pwsh -NoProfile -File tools/docs-check.ps1
```
