---
schema_version: 2
issue: 52
branch: codex/52-winui-xaml-build-diagnostic
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T05:41:17+08:00
environment_fingerprint: windows-26200-amd64-vs2026-18.9.1-universal-sdk-wdk-10.0.28000.2526-cmake-4.3.1-dotnet-8.0.424-pwsh-7.6.5
scope_globs: ["tools/build-preview.ps1", "apps/winui-shell/MainWindow.xaml", "apps/winui-shell/Hibiki.WinUI.csproj", "apps/control-model/EasyControlViewModel.cs", "apps/control-model/ExpertSurfaceModel.cs", "evidence/0000-foundation/winui-xaml-build-diagnostic-v1.json", "docs/tasks/active/52.md"]
shared_paths: []
depends_on: ["0"]
specs: []
adrs: []
next_safe_action: "Commit the evidence and handoff update, push the independent branch, open the Issue #52 Draft PR, and record CI plus the integrator-owned BASELINE limitation."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/build-preview.ps1 -Target WinUI", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 52", "pwsh -NoProfile -File tools/source-policy.ps1", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 52 handoff

## Objective

Make the formal `tools/build-preview.ps1 -Target WinUI` preview build either pass on the locked
Windows 11/VS 2026/Windows App SDK toolchain or fail with actionable, reproducible XAML compiler
diagnostics. The current target machine has the Visual Studio Universal workload, but the locked
Windows App SDK 1.7.250401001 `XamlCompiler.exe` exits 1 without diagnostic output or `output.json`.

## Acceptance

- Reproduce the formal WinUI failure on the locked target and identify whether it is caused by project input, package/toolchain compatibility, or missing diagnostic plumbing.
- If a safe in-scope repair exists, implement it without weakening source-only, RT-thread, driver, signing, or physical-audio boundaries.
- If the locked package/toolchain cannot be repaired safely, add actionable fail-closed diagnostics and record the exact limitation; do not relabel DesktopCompat or WinUICompat as formal WinUI evidence.
- Add anonymous evidence and preserve schema-v2 handoff ownership; do not commit binaries, private paths, registry dumps, endpoint identities, or generated build output.
- Required local gates and GitHub Actions verify pass, subject only to the documented integrator-owned BASELINE refresh.

## Completed

- Created Issue #52 and claimed the independent WinUI build/tooling slice.
- Created this schema-v2 handoff from `origin/main` `cc899e0`.
- Confirmed no overlap with Issue #28's `tools/winui-shell-check.ps1` source accessibility gate or Issue #30's doctor scope.
- Reproduced the locked-package failure and used a process-only Windows App SDK 1.8.260710003 override to expose seven actionable `WMC0011 Unknown member 'StringFormat'` locations in `MainWindow.xaml`.
- Expanded the declared scope before implementation to include the existing control-model display projection and the affected XAML bindings; no IPC, DSP, RT-thread, driver, or permanent-identity boundary changes are planned.
- Replaced the affected XAML `StringFormat` bindings with display-only control-model properties and preserved the existing raw-property source-gate coverage through automation help-text bindings.
- Updated `tools/build-preview.ps1` to prefer Visual Studio MSBuild discovered by `vswhere`, with the existing `dotnet build` path retained as a warning-bearing fallback when Visual Studio is unavailable.
- Formal `pwsh -NoProfile -File tools/build-preview.ps1 -Target WinUI` now passes with 0 warnings and 0 errors and generates ignored XBF/PRI preview output.
- Added `evidence/0000-foundation/winui-xaml-build-diagnostic-v1.json` anchored to source commit `326303321bffa24ac0f9c83a7d5e2bd56c19baa4`.

## Known limitations

- Formal WinUI/XAML, UI Automation/accessibility, physical audio, driver/WaveRT, HLK, Microsoft signing, and release packaging evidence are out of scope unless separately proven.
- The current Windows App SDK package is locked at 1.7.250401001; package upgrades require evidence and review rather than an unrecorded workaround.
- The local doctor check does not find cmake or the exact SDK/WDK QFE on its default detection surface; the C++ verification used process-local Visual Studio CMake, while the formal WinUI build used Visual Studio MSBuild.
- The handoff and evidence paths raise the branch tracked-path count to 413 while the shared `docs/state/BASELINE.md` still claims 411; Issue #0 must refresh that snapshot before the merge-tree documentation gate can be green.

## Last verification

- `pwsh -NoProfile -File tools/build-preview.ps1 -Target WinUI`: pass; Visual Studio MSBuild, 0 warnings, 0 errors, XBF/PRI generated.
- `pwsh -NoProfile -File tools/winui-shell-check.ps1`: pass; 32 interactive controls and source accessibility contract passed.
- `pwsh -NoProfile -File tools/control-model-check.ps1`: pass.
- `pwsh -NoProfile -File tools/verify.ps1` with process-local Visual Studio CMake: pass; 1/1 CTest passed.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 52`, full handoff check, source-policy, source-only-ci, and `git diff --check`: pass.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked only by shared BASELINE mismatch, 411 claimed versus 413 tracked.

## Remaining work

1. Push the implementation, evidence, and handoff commits from the independent branch.
2. Open the Issue #52 Draft PR with the exact scope, evidence, CI results, and integrator-owned BASELINE limitation.
3. Wait for GitHub Actions and the integrator's BASELINE refresh; do not modify shared integration files from this lane.

## Next safe action

Commit the evidence and handoff update, push the independent branch, and open the Issue #52 Draft PR.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/build-preview.ps1 -Target WinUI
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 52
pwsh -NoProfile -File tools/docs-check.ps1
```
