---
schema_version: 2
issue: 64
branch: codex/64-docs-check-merge-base
target_branch: main
base_commit: 4ebbdf11f5721bbfdc9023fc52c84fdac00418be
status: ready_for_review
role: worker
owner: xup61069
updated_at: 2026-08-22T10:43:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["tools/docs-check.ps1", "evidence/0000-foundation/docs-check-merge-base-regression-v1.json", "docs/tasks/active/64.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Merge PR #67 once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 64", "pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 64 handoff

## Objective

Fix the regression in the Issue #51 baseline automation where `tools/docs-check.ps1` captures the multi-line `git show` result as a PowerShell array. A pull request that leaves `docs/state/BASELINE.md` unchanged then fails the merge-base path at the strict tracked-path assertion instead of accepting the measured base-tree counters.

## Acceptance

- Normalize multi-line `git show` output as text before comparing the head and merge-base `BASELINE.md` content.
- A bounded self-test covers equal multiline texts, changed baseline text, strict updated-baseline validation, and untouched-baseline PR-mode acceptance without mutating repository files.
- Preserve fail-closed behavior for stale merge-base counters, malformed markers, missing refs, and a PR that edits BASELINE with inconsistent counters.
- Required local gates and CI results pass.

## Completed

- Reset cleanly to latest `origin/main` at `ba4d0dbad57ec7d6a5da671f6a1532be96ef68b4`.
- Added `Convert-CommandOutputToText`, `Test-MergeBaseMode`, `Resolve-CiRefName`, and `Test-BaselineChangedByHead` helpers.
- Added 17-case bounded `-SelfTest` to `tools/docs-check.ps1`.
- Added anonymous evidence at `evidence/0000-foundation/docs-check-merge-base-regression-v1.json`.
- Verified all local gates pass.

## Known limitations

- This is documentation-counter tooling evidence only; it does not claim product audio, driver, HLK, signing, installer, or physical-audio behavior.

## Last verification

- `tools/docs-check.ps1 -SelfTest`: pass, 17 cases.
- `tools/handoff-check.ps1 -Issue 64`: pass.
- `tools/handoff-check.ps1`: pass.
- `tools/source-policy.ps1`: pass for 464 tracked paths.
- `tools/source-only-ci-check.ps1`: pass.
- `tools/docs-check.ps1`: pass (85 required paths, 24 specs, 464 tracked paths, 96 repository JSON files).
- `tools/verify.ps1`: pass (CTest 1/1).
- `git diff --check`: pass.

## Remaining work

1. Wait for GitHub Actions verification on PR #67.
2. Merge PR #67.

## Next safe action

Merge PR #67 once GitHub Actions verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 64
pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
