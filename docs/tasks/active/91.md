---
schema_version: 2
issue: 91
branch: codex/91-live-system-volume-selftest
target_branch: main
base_commit: 5e9e8a6051e203dd887a671f8198e2e42773cdc3
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T09:15:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-msvc-14.51.36231-sdk-10.0.28000.0-cmake-path-missing-dotnet-10.0.400-pwsh-7.6.4
scope_globs: ["tools/live-system-volume-check.ps1", "evidence/0000-foundation/live-system-volume-selftest-v1.json", "docs/tasks/active/91.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: ["0"]
specs: []
adrs: []
next_safe_action: "Wait for the integrator to refresh docs/state/BASELINE.md in the current merge context, then rerun docs-check and CI; do not modify the shared baseline or run the live volume write test in this worker slice."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 91", "pwsh -NoProfile -File tools/live-system-volume-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 91 handoff

## Objective

Add a deterministic, no-build `-SelfTest` mode to `tools/live-system-volume-check.ps1` that
validates the explicit `-WriteTest` opt-in guard and both direct-broker and Engine Preview launch
plans without starting a process, opening IPC, touching COM, enumerating endpoints, or changing
system volume.

## Acceptance

- `-SelfTest` is bounded, offline, deterministic, counted, and performs no CMake, build, process, IPC, COM, endpoint, audio, or file-write activity.
- The self-test covers the WriteTest guard, direct-broker plan, Engine Preview plan, exact target/executable mapping, and rejection of unsafe or mismatched plans.
- Normal `-WriteTest` behavior remains unchanged and still restores temporary attenuation through the existing probe path.
- Evidence records source-only/user-space limits and does not claim physical audio, driver, WaveRT, HLK, signing, or endpoint identity evidence.
- No product API, IPC schema, DSP ordering, RT rule, Spec, ADR, or shared integration snapshot changes.

## Completed

- Confirmed latest `origin/main` at `5e9e8a6051e203dd887a671f8198e2e42773cdc3`.
- Created the independent branch and worktree for Issue #91.
- Reviewed the active handoff contract and confirmed no overlap with Issue #87 live-WASAPI wrapper, #86 build-preview dispatch, #80 control-model smoke, or closed Issue #8 implementation.
- Claimed this Issue with this schema-v2 handoff.
- Added pure WriteTest guard and direct-broker/Engine Preview plan helpers to `tools/live-system-volume-check.ps1`.
- Added a six-case bounded `-SelfTest` before all live build/process/volume operations.
- Preserved the normal `-WriteTest` path while reusing the validated plan metadata.
- Passed self-test in default and `-DirectBroker` modes; normal invocation without `-WriteTest` remains fail-closed.
- Recorded anonymous evidence in `evidence/0000-foundation/live-system-volume-selftest-v1.json`.

## Known limitations

- This slice validates wrapper planning and opt-in safety only; it cannot prove physical audio, driver/WaveRT behavior, per-App routing, HLK, Microsoft signing, or endpoint identity.
- `docs/state/BASELINE.md` is integrator-owned; this worker will not modify it even if the documentation gate needs a counter refresh.
- The actual `-WriteTest` live probe was not run because it intentionally changes local system volume; no volume state was changed by this slice.

## Last verification

- `tools/doctor.ps1 -CheckOnly`: pass with CMake PATH and locked SDK/WDK limitations.
- `tools/probe-environment.ps1`: pass; local fingerprint remains under ignored `.local`.
- `tools/live-system-volume-check.ps1 -SelfTest`: pass, 6 cases.
- `tools/live-system-volume-check.ps1 -SelfTest -DirectBroker`: pass.
- Normal invocation without `-WriteTest`: correctly rejected before live work.
- `tools/handoff-check.ps1 -Issue 91` and full handoff check: pass.
- `tools/source-policy.ps1` and `tools/source-only-ci-check.ps1`: pass for 423 tracked paths.
- `tools/verify.ps1` with the VS2026 CMake directory prepended process-locally: pass; CTest 1/1.
- `tools/docs-check.ps1`: blocked by shared baseline count 421 vs branch count 423.
- `git diff --check`: pass.
- Draft PR #94 is open at head `eaa4e07`.
- Push CI run `32536849669` / job `96939283074`: configure/test passed; Documentation gate failed at baseline 421 vs branch 423; later gates skipped.
- Pull-request CI run `32536873242` / job `96939349037`: configure/test passed; Documentation gate failed at merge-ref baseline 423 vs git 425; later gates skipped.

## Remaining work

1. Wait for the integrator-owned shared baseline to be refreshed in the current merge context.
2. Rerun docs-check and CI from the updated main/merge ref.
3. Keep this handoff `in_progress` until CI is green; after merge, archive it with the merge SHA and final CI.

## Next safe action

Wait for the integrator to refresh `docs/state/BASELINE.md`, then rerun docs-check and CI; do not modify the shared baseline or run the live volume write test in this worker slice.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 91
pwsh -NoProfile -File tools/live-system-volume-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
