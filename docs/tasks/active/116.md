---
schema_version: 2
issue: 116
branch: codex/116-control-model-engine-smoke-sequence
target_branch: main
base_commit: 6441ee4874428a5b60f3e526c2377a813d6d864f
status: ready_for_review
role: worker
owner: xup61069
updated_at: 2026-08-22T09:02:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["apps/control-model-engine-smoke/**", "evidence/0000-foundation/control-model-engine-smoke-sequence-v1.json", "docs/tasks/active/116.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0010, SPEC-0017]
adrs: []
next_safe_action: "Merge pull request for Issue 116 after CI verification."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 116"]
---

# Issue 116 handoff

## Objective

Fix the StatusSequence check in `apps/control-model-engine-smoke/Program.cs` so it tolerates updated engine status sequence after volume commands are pushed.

## Acceptance

- `apps/control-model-engine-smoke/Program.cs` checks that StatusSequence has properly updated (sequence >= 1UL) rather than assuming a static sequence == 1UL when volume was previously queued.
- `tools/control-model-engine-smoke.ps1` runs and passes.
- `tools/control-model-engine-smoke.ps1 -EnableSessionRouting` runs and passes.
- All handoff and source gates pass.

## Completed

- Updated `apps/control-model-engine-smoke/Program.cs` line 50 to verify `StatusSequence >= 1UL`.
- Verified `tools/control-model-engine-smoke.ps1` and `tools/control-model-engine-smoke.ps1 -EnableSessionRouting` both complete with exit code 0.
- Generated `evidence/0000-foundation/control-model-engine-smoke-sequence-v1.json`.

## Known limitations

- Only tests user-space smoke launcher and ViewModel-to-EnginePreview sequence reconciliation; no physical driver, WaveRT, or HLK signing evidence.

## Last verification

- `tools/control-model-engine-smoke.ps1`: pass
- `tools/control-model-engine-smoke.ps1 -EnableSessionRouting`: pass
- `tools/handoff-check.ps1 -Issue 116`: pass
- `tools/docs-check.ps1`: pass
- `tools/source-policy.ps1`: pass
- `tools/source-only-ci-check.ps1`: pass
- `tools/control-model-check.ps1`: pass
- `git diff --check`: clean

## Remaining work

1. Open PR and verify CI.

## Next safe action

Merge pull request for Issue 116 after CI verification.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 116
```
