---
schema_version: 2
issue: 217
branch: codex/217-complete-archive-187
target_branch: main
base_commit: 857c57c0a059b7e7b2541ad46d61c0e45524c261
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T17:45:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; vs2026-sdk-wdk-on-disk; target-24h2-machine-missing
scope_globs: ["docs/tasks/active/217.md", "evidence/0000-foundation/issue-217-complete-archive-187-v1.json"]
shared_paths: []
depends_on: [187, 189, 213, 214]
specs: [SPEC-0000]
adrs: []
next_safe_action: "Run the Issue 217 claim handoff check, push this claim commit, and open the required Draft PR before expanding scope and moving the stale handoffs."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 217", "git diff --check"]
---

# Issue 217 handoff: complete archive after claim-only PR #214

## Objective

完成 PR #214 claim-only merge 遺留的 post-merge archive：將 Issue #187 implementation handoff 與已合併但未完成 archive 的 Issue #213 handoff 移至 completed，保留 exact merge/takeover evidence，並不接續或改寫已合併的 `codex/213-archive-187` branch。

## Acceptance

- `docs/tasks/active/187.md` 移至 `docs/tasks/completed/2026/187.md`，保留 Issue #187/PR #189 implementation scope 與 final CI history。
- `docs/tasks/active/213.md` 移至 `docs/tasks/completed/2026/213.md`，記錄 PR #214 claim-only merge、corrective Issue #217 與 takeover boundary。
- `docs/tasks/active/217.md` 在 scope expansion 後保持 schema v2，記錄 exact base/head、archive/takeover commit、final CI 與限制。
- 新增 anonymous evidence 只描述文件封存、claim-only merge 與可重現檢查，不宣稱產品、RT audio、driver、HLK 或 signing 能力。
- Required handoff/docs/source-policy/source-only checks、`tools/verify.ps1` 與 `git diff --check` 通過；queued/in_progress 不算 CI 通過。

## Completed

- 已建立 corrective Issue #217，並在 Issue #213 記錄 PR #214 提前 claim-only merge 的 takeover 說明。
- 已確認 PR #214 已合併至 `main` at `d5bb6b6`，但只包含 claim handoff；實作曾推至已合併 branch `9d6da36`，未進入 main，後續不接續該 branch。
- 已從最新 `origin/main=857c57c0a059b7e7b2541ad46d61c0e45524c261` 建立獨立 worktree 與 branch；目前只建立本 claim handoff，尚未修改 stale source handoffs。

## Known limitations

- 本 lane 是 governance/documentation correction，不改變產品 API、IPC/schema、DSP 順序、RT thread、driver、HLK 或簽章行為。
- 本機 target machine 尚未提供；文件 archive 與 user-space gates 不等於 Windows 11 24H2 hardware、loadable driver、HLK 或 Microsoft signing evidence。
- PR #214 的 claim-only merge 與未合併 branch 差異會保留在 evidence；不把舊 branch 的未進 main 變更宣稱為 main 能力。

## Last verification

- Claim handoff 尚待執行 `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 217`。

## Remaining work

1. 執行 claim handoff check，提交並 push claim，建立 Draft PR。
2. 在 live claim 可見後，擴充 scope，封存 active/187 與 active/213，新增 evidence。
3. 執行 local gates，等待 Draft PR CI，記錄 exact push/merge-ref results。
4. CI 全綠後將 handoff 設為 `ready_for_review`；merge 後由 integrator 完成 archive lifecycle。

## Next safe action

Run the Issue 217 claim handoff check, push this claim commit, and open the required Draft PR before expanding scope and moving the stale handoffs.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 217
git diff --check
```
