---
schema_version: 2
issue: 30
branch: codex/30-doctor-sdk-qfe-detection
target_branch: main
base_commit: 413548f23dddbf6de753903915febf6e6fe86457
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T04:32:56+08:00
environment_fingerprint: windows-26200-amd64-vs2026-18.9.1-sdk-wdk-10.0.28000.2526-cmake-4.4.2-pwsh-7.6.5
scope_globs: ["tools/doctor.ps1", "docs/tasks/active/30.md", "evidence/0000-foundation/doctor-sdk-qfe-detection-v1.json"]
shared_paths: []
depends_on: [21]
specs: []
adrs: []
next_safe_action: "Wait for the integrator-owned BASELINE snapshot and merged-handoff order to settle, then rerun the PR #31 pull-request verify."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/doctor.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 30", "pwsh -NoProfile -File tools/docs-check.ps1", "pwsh -NoProfile -File tools/verify.ps1"]
---

# Issue 30 handoff

## Objective

修正 `tools/doctor.ps1` 對 Windows SDK/WDK 10.0.28000.2526 的偵測，使 Microsoft 正規的
`10.0.28000.0` 檔案目錄與 `10.1.28000.2526` QFE metadata 能被正確辨識；缺少或版本不符時仍
必須 fail-closed。

## Acceptance

- 真實 SDK/WDK 10.0.28000.2526 安裝顯示 `OK`，並說明 `.0` on-disk layout 與 exact QFE metadata。
- 缺少 metadata、QFE 不符或 kit 目錄不完整時顯示 `MISSING` 與可執行的診斷。
- 使用 PowerShell 7 相容、可重現的 bounded fixture/self-test，不提交 registry dump、私人路徑、endpoint ID 或編譯產物。
- 不把 user-space kit presence 宣稱成 driver、WaveRT、HLK、signing 或 physical-audio evidence。
- handoff、docs、source-policy、source-only、doctor、verify 與 GitHub Actions verify 通過。

## Completed

- 在 GitHub Issue #30 宣告 owner、branch、target、scope、shared paths 與 acceptance。
- 從最新 `origin/main` `413548f23dddbf6de753903915febf6e6fe86457` 建立獨立 branch/worktree。
- 確認目前 `doctor.ps1` 以不存在的 `Include\10.0.28000.2526` 目錄判斷 SDK/WDK，與已安裝的 Microsoft layout 不一致。
- `tools/doctor.ps1` 現在分別驗證 `10.0.28000.0` 的 Include/build/Tools 目錄，以及 SDK/WDK exact QFE package metadata `10.1.28000.2526`。
- 加入 `-SelfTest` 的 exact match、缺 SDK metadata、錯誤 QFE、缺 kit directory 四個 bounded fixture cases。
- 加入匿名 evidence `evidence/0000-foundation/doctor-sdk-qfe-detection-v1.json`，不含 registry dump、私人路徑或編譯產物。
- Push-triggered GitHub Actions verify `32523704037` passed all checks; pull-request verify `32523707344` exposed the current shared BASELINE counter drift and was not treated as green。

## Known limitations

- 這是環境診斷 gate 修正，不會建立 `.sys`、CAT、簽章或實體音訊 evidence。
- QFE metadata 的查詢方式需同時相容於乾淨 CI 與已安裝 Windows Kits 的 Windows 11 target machine。
- PR #31 的 pull-request merge ref currently includes merged feature handoffs whose integrator-owned BASELINE snapshot is not yet refreshed; this is a merge-order dependency, not a doctor implementation failure。

## Last verification

- `pwsh -NoProfile -File tools/doctor.ps1 -SelfTest`: passed (4 cases)。
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: passed；Windows 11 build、AMD64、git、CMake、pwsh、VS 2026、SDK/WDK QFE 均為 OK。
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 30` 與完整 handoff-check: passed。
- `pwsh -NoProfile -File tools/docs-check.ps1`: passed (85 required paths, 24 specs)。
- `pwsh -NoProfile -File tools/source-policy.ps1`: passed (402 tracked paths)。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: passed (1 workflow, 402 tracked paths)。
- `pwsh -NoProfile -File tools/verify.ps1`: passed; Visual Studio 18 2026/CMake configure、C++ build 與 CTest 1/1 passed。
- `git diff --check`: passed；只產生一般 LF-to-CRLF warning，無 whitespace error。
- GitHub Actions push verify `32523704037`: passed (all jobs)。
- GitHub Actions pull-request verify `32523707344`: failed only at Documentation gate because `BASELINE.md` claimed 403 tracked paths while the merge ref measured 405; shared snapshot refresh is pending integrator coordination。

## Remaining work

1. 等待 PR #37/archive 與 integrator-owned BASELINE refresh 的合併順序完成。
2. 從更新後的 `origin/main` 重新觸發 PR #31 pull-request verify；只有兩個 required verify runs 都通過才轉 `ready_for_review`。
3. 請 integrator review/merge；合併後將 handoff 移到 `docs/tasks/completed/2026/30.md`，記錄 merge SHA 與 final CI。

## Next safe action

Wait for the integrator-owned BASELINE snapshot and merged-handoff order to settle, then rerun the PR #31 pull-request verify.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/doctor.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 30
pwsh -NoProfile -File tools/docs-check.ps1
pwsh -NoProfile -File tools/verify.ps1
```
