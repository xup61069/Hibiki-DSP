---
schema_version: 2
issue: 272
branch: opencode/272-smoke-ir-clear-race
target_branch: main
base_commit: c6516e1
status: in_progress
role: worker
owner: opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-dotnet-10.0.400
scope_globs: ["apps/control-model-engine-smoke/Program.cs", "docs/tasks/active/272.md"]
shared_paths: []
depends_on: []
specs: []
adrs: []
next_safe_action: "Build the smoke project, run control-model-engine-smoke -SelfTest, run handoff/docs/source-policy/source-only gates, then open the draft PR."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 272", "pwsh -File tools/control-model-engine-smoke.ps1 -SelfTest"]
---

# Issue 272 handoff

## Objective

Fix the flaky IR-cleared assertion in `apps/control-model-engine-smoke/Program.cs`. The
single-shot `HasPreparedIr || !IrPrepareStatus.Contains("IR 已清除")` check after
`SelectSceneAsync("movie")` races the engine status refresh and fails intermittently on
faster machines. Replace it with a bounded wait (up to ~3 s, 100 ms steps) for eventual
consistency. Test-only change; no product code touched.

## Acceptance

- `SelectSceneAsync("movie")` ack is still required immediately.
- The prepared-IR check becomes a bounded wait for `!HasPreparedIr && IrPrepareStatus.Contains("IR 已清除")` (≈3 s / 100 ms), failing only if the deadline passes.
- No product/runtime code (EasyControlViewModel, engine) is modified.
- `pwsh -File tools/control-model-engine-smoke.ps1 -SelfTest` passes; `handoff-check -Issue 272`, `docs-check`, `source-policy`, `source-only-ci-check` and `git diff --check` pass.
- Draft PR opened; live multi-run smoke left to CI (timing-dependent) with honest limitation noted.

## Completed

- Reproduced root cause from issue: single-shot snapshot can report prepared IR before the scene-apply transaction finishes server-side.
- Replaced lines 61-63 immediate assertion with a bounded wait loop in `apps/control-model-engine-smoke/Program.cs`.
- Handoff written.

## Known limitations

- The live `control-model-engine-smoke` main-mode run needs the Engine Preview process; full multi-run evidence is timing-dependent and best produced by CI. This change only makes the assertion tolerate the documented eventual-consistency window.
- `dotnet build` of the smoke project is attempted locally; if engine/control-model dependency restore is unavailable here, build is left to CI.

## Last verification

- Edit applied in worktree `opencode/272-smoke-ir-clear-race` at base `c6516e1`.
- Pending: build + gate self-test + required gates.

## Remaining work

1. Build `apps/control-model-engine-smoke`.
2. Run `control-model-engine-smoke.ps1 -SelfTest`.
3. Run `handoff-check -Issue 272`, `docs-check`, `source-policy`, `source-only-ci-check`, `git diff --check`.
4. Commit, push, open draft PR; record honest limitation that live flaky-run evidence is CI's.

## Next safe action

Build the smoke project, run control-model-engine-smoke -SelfTest, run handoff/docs/source-policy/source-only gates, then open the draft PR.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 272
pwsh -File tools/control-model-engine-smoke.ps1 -SelfTest
```
