---
schema_version: 2
issue: 46
branch: codex/46-driver-source-selftest
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T05:14:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/driver-source-check.ps1", "evidence/0000-foundation/driver-source-check-selftest-v1.json", "docs/tasks/active/46.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [0]
specs: []
adrs: []
next_safe_action: "Run the Issue 46 handoff and environment baseline checks, then implement the bounded driver source self-test."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 46", "pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly", "pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/driver-source-check.ps1"]
---

# Issue 46 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/driver-source-check.ps1` so the existing WDK
source-only boundary gate verifies its own fail-closed rules in memory without building,
loading, signing, or installing a driver.

## Acceptance

- Refactor the existing source assertions into a reusable policy function while preserving the
  normal repository check output and all current boundaries.
- `-SelfTest` accepts a valid bounded WDK source fixture and rejects missing MS-PL, GPL/user-space
  linkage, allocation/blocking calls, missing topology/stream/property/INF boundaries, and
  forbidden INF payload cases.
- Self-test never reads or writes repository files, emits device identity, or invokes WDK/HLK/
  signing/install tooling.
- Required local gates and anonymous evidence pass; no SPEC/ADR/product/runtime contract change
  is needed because this adds test coverage for an existing source gate.
- No driver source, INF, CMake, workflow, API, IPC, DSP, RT-thread, or shared baseline change;
  no physical-audio, driver, HLK, or signing claim is made.

## Completed

- Fetched the repository and confirmed the latest `origin/main` at
  `cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9`.
- Reviewed the required AI workflow documents, open Issues/PRs, active handoffs, worktrees, and
  branch ownership; Issue #34 owns the separate INF signability tool, Issue #43 owns installer
  source policy, and Issue #41 owns docs-check, so this tool-only scope does not overlap them.
- Created the isolated Issue #46 branch from the latest `origin/main`.
- Committed and published the schema-v2 ownership claim after `handoff-check -Issue 46` passed.
- Refactored the existing WDK source assertions into a reusable policy function while preserving
  the normal source-only output and boundaries.
- Added an in-memory `-SelfTest` with one valid fixture and nine fail-closed rejection fixtures
  covering license, GPL linkage, allocation/blocking, topology, stream, property, INF, and
  forbidden payload boundaries.

## Known limitations

- This is source-only policy self-test coverage. It cannot compile or load a `.sys`, run WDK/HLK,
  prove Microsoft signing, or validate physical endpoint behavior.
- Fixtures cover the current bounded source contract; future driver source boundaries require an
  explicit Issue/spec review rather than silent acceptance.
- The target VS 2026 and SDK/WDK toolchain is not installed on this worker machine.

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 46`: passed before implementation.
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: passed with explicit warnings that Visual
  Studio 2026 and SDK/WDK 10.0.28000.2526 are missing.
- `pwsh -NoProfile -File tools/probe-environment.ps1`: passed and wrote only ignored local
  fingerprint data.
- `pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest`: passed (10 cases).
- `pwsh -NoProfile -File tools/driver-source-check.ps1`: passed against the repository source.

## Remaining work

1. Add anonymous evidence for self-test coverage and the missing target toolchain.
2. Run all required gates, including CMake/CTest verification when available.
3. Create the Draft PR with exact local results and wait for both GitHub Actions verify runs.

## Next safe action

Add the Issue 46 evidence JSON, run all required gates, and update the Draft PR with the resulting
local checks before waiting for GitHub Actions.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 46
pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly
pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest
pwsh -NoProfile -File tools/driver-source-check.ps1
```
