---
schema_version: 2
issue: 260
branch: codex/260-driver-signability-selftest
target_branch: main
base_commit: 8a153ffa1cc3fdea292527d31695550b7db4ff2e
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-23T01:55:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/driver-signability-check.ps1", "evidence/0000-foundation/issue-260-driver-signability-selftest-v1.json", "docs/tasks/active/260.md"]
shared_paths: []
depends_on: [34]
specs: [SPEC-0003]
adrs: []
next_safe_action: "Run the existing driver-signability self-test baseline, then add only bounded in-memory fixtures for the uncovered fail-closed INF policy families."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 260", "pwsh -NoProfile -File tools/driver-signability-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest"]
---

# Issue 260 handoff

## Objective

Expand the existing bounded, offline self-test for `tools/driver-signability-check.ps1` so each
fail-closed INF policy family has an explicit regression fixture. Keep the production source-only
and optional Inf2Cat paths unchanged.

## Acceptance

- Add deterministic in-memory fixtures for missing service binding, forbidden GPL payload,
  credential/private-key token, and absolute-drive-path rejection, while preserving the existing
  valid and section-placement cases.
- Keep the self-test bounded and offline: no repository writes, process launches, device access,
  SYS/CAT binaries, WDK, signing, or private paths.
- Add anonymous evidence recording the case count and source-only limitation.
- Keep this slice limited to the gate, evidence, and this handoff; do not modify driver product
  sources or shared integration snapshots.
- Run the repository-required gates and record actual CI conclusions; do not claim driver, WaveRT,
  HLK, or Microsoft-signing evidence.

## Completed

- Fetched `origin` and confirmed the latest `origin/main` at `8a153ffa1cc3fdea292527d31695550b7db4ff2e`.
- Audited open Issues, open/draft PRs, active handoffs, worktrees, and remote branches. No active
  owner or branch/PR currently writes `tools/driver-signability-check.ps1`.
- Created and claimed Issue #260, branch `codex/260-driver-signability-selftest`, and this isolated
  worktree from the confirmed main commit.

## Known limitations

- This slice validates only the source-policy parser and its deterministic self-test fixtures. It
  cannot prove a loadable driver, WaveRT operation, physical audio, Inf2Cat output, HLK, signing,
  or Microsoft approval.
- The target VS/SDK/WDK toolchain is not available on this worker machine; CI and a locked target
  machine remain authoritative for formal driver/signability evidence.

## Last verification

- Claim-only baseline: handoff checks and the existing five-case self-test are pending execution
  after the claim commit.

## Remaining work

1. Add the bounded fixtures and anonymous evidence within the declared scope.
2. Run local gates, push the branch, create a Draft PR, and wait for completed green CI.
3. Mark this handoff `ready_for_review` only after the final CI head is green.

## Next safe action

Run the existing driver-signability self-test baseline, then add only bounded in-memory fixtures
for the uncovered fail-closed INF policy families.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 260
pwsh -NoProfile -File tools/driver-signability-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest
```
