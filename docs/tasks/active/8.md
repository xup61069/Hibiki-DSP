---
schema_version: 2
issue: 8
branch: codex/8-system-volume-write-through
target_branch: main
base_commit: 12ee53b8e30c5a6f696253d3d1a34cdb827368eb
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T03:00:00+08:00
environment_fingerprint: "windows-26200-amd64; cmake-path-missing; vs2026-missing; sdk-wdk-10.0.28000.2526-missing"
scope_globs: ["tests/CMakeLists.txt", "tests/live_engine_system_volume_probe.cpp", "tools/live-system-volume-check.ps1", "evidence/0000-foundation/system-volume-live-v1.json", "docs/tasks/active/8.md"]
shared_paths: ["tests/CMakeLists.txt"]
depends_on: []
specs: [SPEC-0002]
adrs: [ADR-0002]
next_safe_action: "Run the Issue 8 handoff check and baseline gates, then verify the Engine Preview probe on this branch."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 8", "pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly", "pwsh -NoProfile -File tools/probe-environment.ps1", "git diff --check"]
---

# Issue 8 handoff

## Objective

Integrate the bounded system-volume write-through snapshot from `c42cabd` and
`5782c9e` into current `main` on a dedicated branch. The slice adds a Windows
Engine Preview IPC/control-queue-to-`IAudioEndpointVolume` live probe, registers
the probe in the live-test CMake target set, keeps the broker-only diagnostic as
an explicit bypass, and records anonymous evidence and limitations.

## Acceptance

- Issue #8 has one owner, this branch, this active schema-v2 handoff, and one Draft PR.
- The probe builds only for Windows live-probe builds, starts Engine Preview with
  the explicit `--enable-system-volume` opt-in, sends a bounded IPC
  `VolumeNotification`, verifies endpoint readback, and restores the original
  dB/mute state before exit.
- The broker-only path remains explicit behind `-DirectBroker`; neither path runs
  implicitly in CI or application startup.
- Contract/spec behavior remains within accepted SPEC-0002 and ADR-0002. Evidence
  says user-space Engine Preview control evidence is not virtual endpoint,
  WaveRT/PortCls, physical DSP, HLK, or Microsoft-signing evidence.
- Required handoff, documentation, source-policy, diff, build/test, and CI checks
  are recorded before the Draft PR is requested for review.

## Completed

- Confirmed `origin/main` at `12ee53b8e30c5a6f696253d3d1a34cdb827368eb` after fetch.
- Confirmed Issue #8 and #10 are the only open Issues; both were initially
  unassigned, with no open/draft PR and no competing active feature handoff.
- Declared `tests/CMakeLists.txt` as a shared integration path; Issue 0 global
  snapshot files remain integrator-owned and are outside this write scope.
- Created the dedicated worktree and branch from the latest `origin/main`.
- Integrated the scoped Engine Preview probe, live-tool routing, CMake target,
  and anonymous evidence from the source snapshot without applying its Issue 0
  global handoff changes.

## Known limitations

- This probe is an explicitly user-invoked user-space Windows control-plane
  check. It does not prove a Hibiki virtual endpoint, WaveRT/PortCls driver,
  physical DSP playback, per-App routing, HLK, or Microsoft signing.
- `-WriteTest` temporarily changes the local default endpoint volume by about
  3 dB and must restore it; it must never be run implicitly.
- The local machine may lack the locked Windows 11 24H2 / VS 2026 / SDK-WDK
  toolchain, so target WinUI, driver, hardware, and signing claims remain
  unverified unless the required CI/target evidence exists.

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 8`: passed.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: passed for Issues 0 and 8.
- `pwsh -NoProfile -File tools/docs-check.ps1`: passed (85 required paths,
  24 specs).
- `pwsh -NoProfile -File tools/source-policy.ps1`: passed (387 tracked paths).
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: passed (1 workflow,
  387 tracked paths).
- `git diff --check`, PowerShell script syntax, and evidence JSON parse: passed.
- `pwsh -NoProfile -File tools/verify.ps1` and the explicit
  `pwsh -NoProfile -File tools/live-system-volume-check.ps1 -WriteTest` both
  stopped before configure because `cmake` is not on PATH. No live endpoint
  volume was changed by this failed attempt.
- Source snapshot verification run `32514507807` on `ab2d677` completed with
  `success`; this branch's own PR CI remains pending and will be checked
  separately.

## Remaining work

1. Wait for the current branch's GitHub Actions checks to complete; queued or
   in-progress checks are not evidence of success.
2. If CI exposes a source/build issue, fix only paths in this Issue 8 scope and
   rerun the required gates.
3. Update the Draft PR and handoff with final CI URLs/results before requesting
   review; keep this handoff `ready_for_review` only when all required checks pass.

## Next safe action

Check Draft PR #11's completed GitHub Actions results; do not request merge while checks are queued, failing, or while the CMake/toolchain limitation remains unaddressed.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 8
pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly
pwsh -NoProfile -File tools/probe-environment.ps1
git diff --check
```
