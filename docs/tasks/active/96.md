---
schema_version: 2
issue: 96
branch: codex/96-readme-dual-audience
target_branch: main
base_commit: e9ea7fc87959ca0f2ba0e0a122065ffc3f76afa8
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-cmake-4.4.0-msvc-buildtools-17.14-dotnet-10.0.301; target-24h2-vs2026-wdk-missing
scope_globs: ["evidence/0000-foundation/readme-dual-audience-v1.json", "docs/tasks/active/96.md"]
shared_paths: ["README.md", "docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0004, SPEC-0005]
adrs: []
next_safe_action: "Restructure README into dual-audience layout preserving every fact and non-claim."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 96", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1", "git diff --check"]
---

# Issue 96 handoff

## Objective

Restructure root `README.md` for two audiences: general visitors get a plain-language
introduction, maturity summary and a minimal safe try-it path up front; AI
collaborators keep the canonical entry order (AGENTS.md → START_HERE → AI_HANDOFF),
gate commands and multi-session pointers. All existing facts, preview commands,
caveats and non-claims are preserved and reorganized — nothing deleted.

## Acceptance

- First screen tells a human what Hibiki DSP is, its maturity, and how to try a
  preview safely.
- TOC separates 給一般訪客 / 給 AI 協作者 flows; AI entry order stays canonical.
- Every existing fact/command/caveat/non-claim from the previous README remains.
- Documentation only; no product source change; BASELINE counters mechanical only.

## Completed

- Claimed Issue 96 with an isolated worktree from `origin/main` (`e9ea7fc`).
- Conflict survey: `README.md` held by Issue 0 scope → shared_paths coordination
  (precedent #35/#88); BASELINE hot with #42/#49/#58/#75/#82 counter refreshes.

## Known limitations

- `docs/state/BASELINE.md` remains multi-PR hot; integration may need a
  conflict-resolution merge.
- Documentation reflects reality through base commit `e9ea7fc`.

## Last verification

- Baseline not yet run at claim time; gates will be updated before the PR leaves
  draft.

## Remaining work

1. Rewrite README structure with dual-audience flow, preserving all content.
2. Refresh BASELINE counters via docs-check loop for this slice's two new files.
3. Run all gates locally; record evidence; wait for GitHub Actions.

## Next safe action

Restructure README into dual-audience layout preserving every fact and non-claim.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 96
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
git diff --check
```
