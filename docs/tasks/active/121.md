---
schema_version: 2
issue: 121
branch: codex/121-verify-selftest
target_branch: main
base_commit: 16b98e0e7b4d88408adfa24789e18539ccb702cf
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: "Windows 11 build 26200; VS 2026 18.9.12112.369; SDK 10.0.28000.0; WDK 10.0.28000.2526 unavailable; .NET 10.0.400; bundled VS CMake available only by explicit process-local PATH"
scope_globs: ["tools/verify.ps1", "evidence/0000-foundation/verify-selftest-v1.json", "docs/tasks/active/121.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [0]
specs: []
adrs: []
next_safe_action: "Wait for the integrator to reconcile docs/state/BASELINE.md with direct-branch 433 and current main merge-ref 434, then rerun CI; do not modify the shared baseline in this worker branch."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/verify.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 121", "pwsh -NoProfile -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 121 handoff

## Objective

Add a bounded, offline `-SelfTest` mode to `tools/verify.ps1` that validates the aggregate unsigned verification dispatch contract without invoking CMake, compiling, running CTest, deleting files, launching processes, or changing the build tree.

## Acceptance

- `-SelfTest` is deterministic, offline, no-build, no-process, no-CMake, no-CTest, no-delete, and no-file-write.
- It validates the existing configure/build/test arguments, `RelWithDebInfo` configuration, and ignored `.local/build` output root.
- Normal `verify.ps1` behavior remains unchanged, including explicit `-Clean` behavior.
- Anonymous evidence and this schema-v2 handoff are included; no binaries or private identity data are committed.
- Required local gates and CI results are recorded in the Draft PR.

## Completed

- Confirmed latest `origin/main` is `16b98e0e7b4d88408adfa24789e18539ccb702cf`.
- Confirmed Issue #92 / PR #100 owns only the workflow self-test sweep and does not own `tools/verify.ps1`.
- Confirmed no active handoff, remote branch, or open PR claims `tools/verify.ps1`.
- Created Issue #121, assigned owner `xup61069`, and created branch `codex/121-verify-selftest` from latest `origin/main` in an isolated worktree.
- Declared the exclusive write scope and integrator-owned baseline dependency.
- Pushed claim commit `3a6ca29` and opened Draft PR #122.
- Added pure `Get-VerifyBuildPlan` and `Assert-VerifyBuildPlan` helpers for the existing configure/build/test dispatch.
- Added bounded `tools/verify.ps1 -SelfTest`, which exits before CMake, CTest, `-Clean` deletion, process launch, or file creation.
- Added anonymous evidence at `evidence/0000-foundation/verify-selftest-v1.json`.
- Ran normal `tools/verify.ps1 -Clean` successfully with VS2026 bundled CMake; CTest 1/1 passed and output remained under ignored `.local/build`.

## Known limitations

- The self-test is aggregate build-dispatch/source evidence only; it does not prove CMake configure, compilation, CTest execution, physical audio delivery, driver, WaveRT, HLK, signing, or release behavior.
- The locked WDK prerequisite is unavailable on this machine; formal driver/WinUI evidence remains out of scope.
- `docs/state/BASELINE.md` is shared and will not be modified by this worker; its tracked-path count may require an integrator refresh after this branch is merged.

## Last verification

- `pwsh -NoProfile -File tools/verify.ps1 -SelfTest`: pass, 5 cases; no build/process/CMake/CTest/delete/file activity.
- `pwsh -NoProfile -File tools/verify.ps1 -Clean` with the bundled VS2026 CMake directory prepended process-locally: pass; configure/build and CTest 1/1.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 121`: pass.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass.
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass for 432 tracked paths before evidence was staged.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass for 1 workflow and 432 tracked paths before evidence was staged.
- Evidence JSON parse: pass.
- `git diff --check`: pass.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked because integrator-owned `docs/state/BASELINE.md` claims 431 tracked paths while this pre-evidence branch had 432; after the evidence file is tracked, CI is expected to report 433.
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: warning only; CMake is absent from default PATH and WDK 10.0.28000.2526 is unavailable.
- `pwsh -NoProfile -File tools/probe-environment.ps1`: wrote only ignored `.local/context.json`.
- Push CI run `32542875895`, job `96956092749`: completed failure only at Documentation gate; configure/test unsigned source passed; direct branch reported `BASELINE.md claims 431 tracked paths but git reports 433`; later gates were skipped.
- Pull-request CI run `32542878349`, job `96956098701`: completed failure only at Documentation gate; configure/test unsigned source passed; current main merge-ref reported `BASELINE.md claims 432 tracked paths but git reports 434`; later gates were skipped.
- During these CI runs, `origin/main` advanced to `d09800ce342bac8d43bb82134b05780054569211` via archive PR #123; this changes the merge-ref count but does not overlap this branch scope.
- Claim and implementation commits are based on `origin/main` at `16b98e0e7b4d88408adfa24789e18539ccb702cf`.

## Remaining work

1. Wait for the integrator-owned baseline to reconcile direct-branch count 433 and current merge-ref count 434.
2. Rerun push and pull-request CI after that shared refresh and record the exact results.
3. Leave the handoff `ready_for_review` only if all required CI is complete and green; otherwise preserve the single integrator baseline blocker.

## Next safe action

Wait for the integrator to reconcile docs/state/BASELINE.md with direct-branch 433 and current main merge-ref 434, then rerun CI; do not modify the shared baseline in this worker branch.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/verify.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 121
pwsh -NoProfile -File tools/docs-check.ps1
git diff --check
```
