---
schema_version: 2
issue: 287
branch: codex/287-hibt-packet-test
target_branch: main
base_commit: b89a284
status: ready_for_review
role: worker
owner: xup61069
updated_at: 2026-08-22T14:05:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-cmake-4.4.2-vs2026; wdk-missing
scope_globs: ["tests/unit/contract_tests.cpp", "evidence/0000-foundation/hibt-packet-v1.json", "docs/tasks/active/287.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0009]
adrs: [ADR-0002]
next_safe_action: "Review PR #299; merge after CI green."
resume_commands: ["git -C G:/Hibiki-DSP-287-hibt-packet-test status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 287", "pwsh -File tools/verify.ps1"]
---

# Issue 287 handoff

## Objective

為 HIBT packet 驗證加入 bounded、離線的單元測試 fixture：truncated header/payload、
non-finite sample、不支援 layout（channels=3）與 sample rate（22222），全部以 in-memory
bytes 直接打 C++ decoder，不改 production AudioWorklet 或 MV3 bridge。

## Acceptance

- contract_tests.cpp 新增 deterministic fixtures：valid Float32 frame（既有）、truncated、
  non-finite sample、wrong layout rejection。
- 主 decoder 維持 fail-closed：framing、finite samples、layout/rate validation 不變。
- evidence/0000-foundation/hibt-packet-v1.json 記錄 case 數與離線限制。
- 本 handoff 更新為 ready_for_review 並附驗證命令結果。

## Completed

- Claim protocol 完成：Issue 指派、branch 建立、worktree 隔離、無 scope 重疊。
- 在 contract_tests.cpp 既有 tab packet 區塊後新增 valid_packet fixture 與五組 rejection fixtures：
  truncated header (15 bytes) -> Truncated、truncated payload (少一個 float) -> LengthMismatch、
  NaN sample -> NonFiniteSample、channels=3 -> InvalidChannels、rate=22222 -> InvalidSampleRate。
- 所有案例共用同一個乾淨的 2ch x 2fr @48k valid packet 作為基底，避免繼承先前 pop_back 的殘缺狀態。
- evidence/0000-foundation/hibt-packet-v1.json 已記錄五個 case 的 coverage 與離線限制。

## Known limitations

- 僅驗證 portable C++ decoder；不宣稱瀏覽器 runtime、tab capture 或實體 per-App routing。
- audio-worklet.js 的 packetizer 本身不在本切片測試範圍（production code unchanged）。

## Last verification

全部通過（2026-08-22）：
- pwsh -File tools/verify.ps1 — ctest 1/1 passed（含新增五個 decoder rejection cases）
- pwsh -File tools/docs-check.ps1 — pass (#287@codex/287-hibt-packet-test)
- pwsh -File tools/source-policy.ps1 — pass (553 tracked paths)
- pwsh -File tools/source-only-ci-check.ps1 — pass (1 workflows, 553 tracked paths)
- pwsh -File tools/extension-check.ps1 — pass
- git diff --check — clean

## Remaining work

（無剩餘工作；等待 reviewer。）

## Next safe action

Review PR #299; merge after CI green.

## Resume commands

```powershell
git -C G:/Hibiki-DSP-287-hibt-packet-test status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 287
pwsh -File tools/verify.ps1
```
