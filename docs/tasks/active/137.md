---
schema_version: 2
issue: 137
branch: codex/137-baseline-counters-json
target_branch: main
base_commit: 15330f19e7f4816bf3b0ac4a935774d41394e93d
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T12:19:30+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-dotnet10-no-vs2026-no-wdk; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/docs-check.ps1", "build/baseline-counters.json", "docs/state/BASELINE.md", "docs/tasks/active/137.md", "evidence/0000-foundation/baseline-counters-json-v1.json"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [41, 64]
specs: [SPEC-0004]
adrs: []
next_safe_action: "Implement the baseline-counters JSON reader with -WriteCounters, extend -SelfTest fixtures, refresh BASELINE prose, run local gates and record anonymous evidence."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 137", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 137 handoff

## Objective

把 BASELINE.md 驗證摘要裡的兩個易漂移計數（tracked paths、repository JSON）移到
machine-readable 的 `build/baseline-counters.json`，讓 `tools/docs-check.ps1` 改讀該檔，
prose 不再攜帶權威數字，消除跨 lane 合併時的中文摘要句衝突。提供 `-WriteCounters`
helper 機械式更新 JSON，並以 bounded `-SelfTest` fixtures 覆蓋 missing/malformed/schema
drift/matching/drifting 路徑。

## Acceptance

- docs-check 從 `build/baseline-counters.json` 讀取 tracked paths 與 repository JSON
  計數；missing file、malformed JSON、schema_version drift 或數值 mismatch 一律 fail-closed。
- `-WriteCounters` helper 以 git 實測值確定性地重寫 JSON，不碰其他欄位。
- BASELINE.md 驗證摘要句不再包含 volatile 數字；敘述文字保留。
- `-SelfTest` 新增 missing JSON、malformed JSON、schema drift、matching counts、drifting
  counts 五類 fixture cases。
- 本機 gates 通過（verify、handoff-check -Issue 137、docs-check ±SelfTest、source-policy）；
  GitHub Actions verify 全綠後才請求 review。
- 匿名 evidence JSON 記錄 tooling-only scope 與驗證結果。

## Completed

- Issue #137 已由 GitHub assignee live claim；branch `codex/137-baseline-counters-json`
  已從 origin/main `15330f19` 建立 worktree。
- 依賴 #41/#64 已合併進 main；本切片基於其合併後 gate 實作。

## Known limitations

- 本機缺 Visual Studio 2026 與 WDK 10.0.28000.2526；driver／XAML evidence 維持 fail-closed，
  與本 tooling-only 切片無關。

## Last verification

- 尚未執行變更後 gates。

## Remaining work

1. 實作 JSON 讀取與 fail-closed 語意。
2. 加入 -WriteCounters helper。
3. 擴展 -SelfTest fixtures。
4. 更新 BASELINE.md 摘要句與 evidence JSON。
5. 執行全部相關 local gates。

## Next safe action

Implement the baseline-counters JSON reader with -WriteCounters, extend -SelfTest fixtures,
refresh BASELINE prose, run local gates and record anonymous evidence.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 137
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```

