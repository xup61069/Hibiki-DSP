---
schema_version: 2
issue: 60
branch: codex/60-control-model-check-selftest
target_branch: main
base_commit: ec8456ff36b6ac64060fa4b7068f91602727c2da
status: planned
role: worker
owner: xup61069
updated_at: 2026-08-22T06:00:49+08:00
environment_fingerprint: windows-26200-amd64-vs2026-18.9.1-universal-sdk-wdk-10.0.28000.2526-cmake-4.3.1-dotnet-8.0.424-pwsh-7.6.5
scope_globs: ["tools/control-model-check.ps1", "evidence/0000-foundation/control-model-check-selftest-v1.json", "docs/tasks/active/60.md"]
shared_paths: []
depends_on: ["0"]
specs: []
adrs: []
next_safe_action: "Run the control-model gate baseline, inspect the existing deterministic harness, and design bounded in-memory self-test cases without changing the main path."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 60", "pwsh -NoProfile -File tools/control-model-check.ps1", "pwsh -NoProfile -File tools/control-model-check.ps1 -SelfTest"]
---

# Issue 60 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/control-model-check.ps1` so the control-model gate can
validate its command/fixture boundary and fail-closed error handling without changing repository
files or claiming physical audio behavior. Preserve the existing main-path `dotnet run` check's
observable pass/fail behavior.

## Acceptance

- Main control-model check continues to execute the current deterministic validation and keeps its
  existing success/failure contract.
- `pwsh -NoProfile -File tools/control-model-check.ps1 -SelfTest` runs bounded, deterministic cases
  without writing repository files, compiled artifacts, endpoint identity, or private paths.
- Self-test covers a valid invocation boundary, missing/inaccessible project handling, non-zero
  child-process propagation, and a clean success summary.
- Add anonymous evidence and record that this is control-plane/model validation only, not physical
  audio, driver, HLK, signing, or device evidence.
- Run handoff, docs, source-policy, source-only, control-model, and `git diff --check` gates; wait
  for actual CI conclusions.

## Completed

- Created Issue #60 after the open Issue/PR audit found all existing suitable lanes claimed.
- Confirmed no overlap with active tooling, WinUI, driver, installer, extension, distribution,
  documentation, source-policy, source-only-CI, or Issue #52 application scopes.
- Created this schema-v2 claim from latest `origin/main` `ec8456f` in an independent worktree.
- Confirmed the existing main path passes unchanged in observable output: `Control model checks passed.`
- Refactored the wrapper into `Invoke-ControlModelCheck` while preserving the production `dotnet run`
  arguments, and added a runner injection seam used only by `-SelfTest`.
- Added four bounded in-memory cases: valid invocation/argument capture, missing project rejection,
  empty output-path rejection, and non-zero child-process propagation with exit code 23.

## Known limitations

- This slice is limited to the control-model gate and its deterministic test harness; it does not
  claim physical audio, per-App delivery, a loadable driver, WaveRT, HLK, Microsoft signing, or
  release packaging evidence.
- The handoff/evidence paths depend on Issue #0 refreshing shared BASELINE counters; this worker
  must not edit `docs/state/BASELINE.md`.
- The self-test shares the production invocation helper by design; changes to the command boundary
  should update both the helper and its injected-runner fixtures.
- The target Windows SDK/WDK and Visual Studio toolchain may not be available on every CI or local
  host; environment differences must be recorded rather than converted into product claims.

## Last verification

- `pwsh -NoProfile -File tools/control-model-check.ps1`: pass; existing control-model checks passed.
- `pwsh -NoProfile -File tools/control-model-check.ps1 -SelfTest`: pass; 4 bounded cases passed.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 60` and full handoff check: pass.
- `pwsh -NoProfile -File tools/source-policy.ps1` and `tools/source-only-ci-check.ps1`: pass with 415 staged tracked paths.
- `git diff --cached --check`: pass.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked only by the integrator-owned BASELINE mismatch, 413 claimed versus 415 staged tracked paths.
- Draft PR #62 push run `32530915301` and pull-request run `32530940377` both failed only at that Documentation gate; later workflow gates were skipped and are not claimed as passed.

## Remaining work

1. Wait for Issue #0 to refresh the shared BASELINE tracked-path summary.
2. Re-run PR #62 CI and record all gate conclusions; keep status `in_progress` until green.
3. After green CI and review, let the integrator archive this handoff with the merge SHA and final CI run.

## Next safe action

Add evidence for the bounded self-test, then run the complete local gate set before opening the Draft PR.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 60
pwsh -NoProfile -File tools/control-model-check.ps1
pwsh -NoProfile -File tools/control-model-check.ps1 -SelfTest
```
