---
schema_version: 2
issue: 60
branch: codex/60-control-model-check-selftest
target_branch: main
base_commit: 07b467042772bfe8f7b3439833db6152eb596e06
status: ready_for_review
role: worker
owner: xup61069
updated_at: 2026-08-22T11:19:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["tools/control-model-check.ps1", "evidence/0000-foundation/control-model-check-selftest-v1.json", "docs/tasks/active/60.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: []
adrs: []
next_safe_action: "Merge PR #62 once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 60", "pwsh -NoProfile -File tools/control-model-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 60 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/control-model-check.ps1` that validates the gate's command/fixture boundary and fail-closed error handling without changing repository files or claiming physical audio behavior.

## Acceptance

- Bounded `-SelfTest` covers valid invocation boundary, missing project handling, non-zero child-process propagation, and clean success summary.
- Self-test performs no process launch, named-pipe connection, file write, device enumeration, endpoint identity output, or physical-audio operation.
- Normal smoke still requires the existing built Engine Preview and remains the only path that launches it.
- Anonymous evidence and schema-v2 handoff; no product source, IPC schema, DSP, RT-thread, driver, UI, Spec, or ADR change.

## Completed

- Rebased on latest `origin/main` at `07b467042772bfe8f7b3439833db6152eb596e06`.
- 4-case bounded `-SelfTest` added covering valid invocation boundary, missing project handling, non-zero child-process propagation, and clean success summary.
- Added anonymous evidence at `evidence/0000-foundation/control-model-check-selftest-v1.json`.
- Verified all local gates pass.

## Known limitations

- This is control-plane/model validation only, not physical audio, driver, HLK, signing, or device evidence.

## Last verification

- `tools/control-model-check.ps1 -SelfTest`: pass, 4 cases.
- All handoff, docs, source-policy, source-only-ci, and whitespace gates: pass.

## Remaining work

1. Merge PR #62 once GitHub Actions verify passes.
2. Archive this handoff to `docs/tasks/completed/2026/60.md`.

## Next safe action

Merge PR #62 once GitHub Actions verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 60
pwsh -NoProfile -File tools/control-model-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
