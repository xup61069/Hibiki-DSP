---
schema_version: 2
issue: 39
branch: codex/39-distribution-profile-gate
target_branch: main
base_commit: 87aac55db39821820b12a49fcce2ec9c7bd9668a
status: planned
role: worker
owner: xup61069
updated_at: 2026-08-22T04:38:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/distribution-check.ps1", "docs/specs/SPEC-0001-core-contracts.md", "evidence/0000-foundation/distribution-profile-gate-v1.json", "docs/tasks/active/39.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0001]
adrs: []
next_safe_action: "Run the Issue 39 handoff and environment baseline checks, then implement the bounded profile parser/self-test."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 39", "pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly", "pwsh -NoProfile -File tools/distribution-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/distribution-check.ps1"]
---

# Issue 39 handoff

## Objective

Harden the source-only `tools/distribution-check.ps1` gate so the canonical
`config/distribution-profile.yml` cannot silently accept duplicate keys, unknown sections,
malformed indentation, missing required scalars, invalid platform/schema values, malformed or
duplicate GUIDs, or stable identity drift. This is a bounded source-policy slice; the canonical
profile is an input and is not regenerated or modified.

## Acceptance

- Parse the repository's bounded DistributionProfile v1 YAML shape with blank and full-line
  comment lines tolerated; reject unsupported YAML constructs rather than guessing.
- Fail closed on duplicate keys, unknown root/nested keys, malformed indentation, missing required
  fields, invalid schema/platform values, malformed GUIDs, duplicate GUIDs, and identity drift.
- Add bounded `-SelfTest` fixtures covering the valid profile and each rejection class without
  modifying repository files.
- Update SPEC-0001 and anonymous evidence to describe source-only profile validation and its
  limitations.
- No product API, IPC, DSP, RT-thread, driver, UI, workflow, build-chain, or permanent identity
  changes; no real endpoint IDs or private paths are emitted.
- Required local gates and GitHub Actions verify must pass before review; no physical-audio,
  driver, HLK, signing, or installer evidence is claimed.

## Completed

- Fetched the repository and confirmed the latest `origin/main` at
  `87aac55db39821820b12a49fcce2ec9c7bd9668a`.
- Reviewed the required AI workflow documents, open Issues/PRs, active handoffs, worktrees, and
  branch ownership; Issue #30, #34, and #35 are already claimed, so this new Issue #39 has no
  scope overlap with those lanes or Issue 0 shared paths.
- Created the isolated Issue #39 branch from the latest `origin/main`.

## Known limitations

- This gate validates only the repository's deliberately bounded YAML subset; it is not a general
  YAML parser and does not prove runtime consumers interpret the profile identically.
- It does not build or install a driver, access physical endpoints, run HLK, produce Microsoft
  signing evidence, or validate private release artifacts.
- The stable profile identities remain maintainer-owned configuration and must not be regenerated
  by this worker.

## Last verification

- Claim baseline is pending; run the commands in `Next safe action` before modifying the tool.

## Remaining work

1. Commit and publish the schema-v2 ownership claim.
2. Run doctor/probe baseline checks and capture the environment limitation.
3. Implement the strict bounded profile parser and `-SelfTest` fixtures.
4. Update SPEC-0001, add anonymous evidence, run all required local gates, update the Draft PR,
   and wait for both GitHub Actions verify runs.

## Next safe action

Run the Issue 39 handoff and environment baseline checks, then implement the bounded profile
parser/self-test.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 39
pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly
pwsh -NoProfile -File tools/distribution-check.ps1 -SelfTest
pwsh -NoProfile -File tools/distribution-check.ps1
```
