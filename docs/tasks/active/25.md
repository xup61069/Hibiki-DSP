---
schema_version: 2
issue: 25
branch: codex/25-handoff-scope-overlap-gate
target_branch: main
base_commit: cd3c2235a5cadb4676e3a8e5e6d4372861f76cac
status: planned
role: worker
owner: xup61069
updated_at: 2026-08-22T04:05:00+08:00
environment_fingerprint: unknown
scope_globs: ["tools/handoff-check.ps1", "docs/specs/SPEC-0004-ai-handoff-and-evidence.md", "evidence/0000-foundation/handoff-scope-overlap-v1.json", "docs/tasks/active/25.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Run the Issue 25 handoff, doctor, and environment baseline checks before implementing the overlap matcher."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 25", "pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly", "pwsh -NoProfile -File tools/probe-environment.ps1"]
---

# Issue 25 handoff

## Objective

Make the schema-v2 handoff checker fail closed when two active Issues claim overlapping
`scope_globs`, including parent/child and wildcard intersections, instead of checking only
exact duplicate strings. The change is a multi-AI coordination safety gate and does not
change product audio behavior.

## Acceptance

- Active handoff validation rejects overlapping scope globs such as `src/**` versus
  `src/hub/**` and `docs/tasks/active/*.md` versus `docs/tasks/active/21.md`.
- Non-overlapping sibling globs remain valid, and the failure identifies both Issue IDs and
  both conflicting scope values.
- A bounded `-SelfTest` path covers wildcard, parent/child, sibling, and exact cases without
  modifying the repository.
- SPEC-0004 documents the fail-closed overlap invariant and anonymous evidence records only
  source/tooling contract coverage.
- Required local gates and GitHub Actions verify pass; no product API, IPC, DSP, RT, driver,
  permanent identity, workflow, or build-chain claim is made.

## Completed

- Fetched the repository and confirmed `origin/main` at
  `cd3c2235a5cadb4676e3a8e5e6d4372861f76cac`.
- Reviewed open Issues, open/draft PRs, active handoffs, worktrees, and branch ownership.
- Confirmed Issue #21 owns `tools/docs-check.ps1`/baseline-counter work and Issue #22 owns
  the VST3 scheduler timeline work; this scope does not overlap either claim or Issue 0's
  integrator shared paths.
- Created the isolated Issue 25 worktree and branch from the latest `origin/main`.
- Created and published the Issue #25 ownership metadata; implementation has not started.

## Known limitations

- The matcher will validate repository-relative glob syntax only; it cannot prove target-machine
  audio, driver, WinUI, HLK, signing, or physical endpoint behavior.
- Existing active handoffs are the durable claim registry; GitHub Issue/PR state remains the live
  ownership authority.

## Last verification

- Claim baseline checks are pending; run the commands in `Next safe action` before modifying the
  tooling.

## Remaining work

1. Run the baseline environment and handoff checks and record the fingerprint.
2. Implement bounded glob-language intersection detection and the `-SelfTest` cases.
3. Update SPEC-0004, add anonymous evidence, run all required gates, and open the Draft PR.

## Next safe action

Run the Issue 25 handoff, doctor, and environment baseline checks before implementing the overlap matcher.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 25
pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly
pwsh -NoProfile -File tools/probe-environment.ps1
```
