---
schema_version: 2
issue: 187
branch: codex/187-archive-182
target_branch: main
base_commit: 741fbc31296b1c138adf8ab807ef19d5bd34126e
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T16:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; cmake-missing-wdk-missing-portable-user-space
scope_globs: ["docs/tasks/active/182.md", "docs/tasks/completed/2026/182.md", "docs/tasks/active/187.md", "evidence/0000-foundation/issue-187-archive-182-v1.json", "build/baseline-counters.json"]
shared_paths: []
depends_on: [182, 183, 184]
specs: [SPEC-0000]
adrs: []
next_safe_action: "Commit this ownership claim, then move only the completed Issue 182 handoff and record archive evidence."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 187", "git diff --check"]
---

# Issue 187 handoff

## Objective

封存已由 PR #183 合併完成的 Issue #182 handoff，保留其原始驗證紀錄與限制說明，並以獨立 archive lane 更新狀態與 merge evidence。

## Acceptance

- `docs/tasks/active/182.md` 只移動至 `docs/tasks/completed/2026/182.md`，不改寫其 implementation scope。
- `docs/tasks/active/187.md` 保持 schema v2，記錄 archive status、exact base/head、merge commit、CI 結果與限制。
- 新增 archive evidence 只描述文件封存與可重現的檢查結果，不宣稱產品、RT audio、driver、HLK 或 signing 能力。
- Required handoff/docs/source-policy checks 與 `git diff --check` 通過；可用時執行 `tools/verify.ps1` 與 `tools/source-only-ci-check.ps1`。
- Draft PR body 包含 `Closes #187`、owner、handoff、head/target、scope、shared paths、depends_on、Spec/ADR impact、local/CI verification、evidence 與限制。

## Completed

- 已確認 `origin/main` 為 `741fbc31296b1c138adf8ab807ef19d5bd34126e`。
- 已確認 #182 closed、PR #183 merged，且沒有既有 Issue 182 archive PR。
- 已確認 PR #185 的 shared guidance/counter scope 不包含 `docs/tasks/active/182.md`。
- 已建立獨立 worktree 與 branch，尚未修改產品程式碼。
- 依最新 counters contract，已將 `build/baseline-counters.json` 納入本 slice 的獨占 scope，以涵蓋本 lane 新增 tracked 檔案的 strict gate 計數更新。

## Known limitations

- 本 lane 僅是 governance/documentation archive，不改變產品 API、IPC/schema、DSP 順序、RT thread、driver、HLK 或簽章行為。
- 本機缺少 CMake/WDK 時，`tools/verify.ps1` 的完整 CTest/driver coverage 只能由 GitHub Actions Windows runner 驗證；queued/in_progress 不算通過。
- 不修改 integrator-owned shared paths：`AGENTS.md`、root `README.md`、`docs/AI_HANDOFF.md`、`docs/state/BASELINE.md`、`docs/PROJECT_MAP.md` 與 `docs/tasks/active/0.md`。

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 187`：pass。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass（9 active handoffs）。
- `pwsh -NoProfile -File tools/docs-check.ps1`：pass（85 required paths / 24 specs / 483 tracked paths / 103 repository JSON files）。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass（483 tracked paths）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass（1 workflow / 483 tracked paths）。
- `git diff --check`：pass。
- `pwsh -NoProfile -File tools/verify.ps1`：blocked locally because `cmake` is not installed; GitHub Actions Windows runner remains required evidence。

## Remaining work

1. Commit and push the archive move, counters refresh, and evidence.
2. Mark this handoff `ready_for_review`, open Draft PR, and wait for final CI.

## Next safe action

Commit and push the scoped archive move and evidence, then prepare the Draft PR.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 187
git diff --check
```
