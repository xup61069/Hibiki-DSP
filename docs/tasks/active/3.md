---
schema_version: 2
issue: 3
branch: codex/3-multi-ai-coordination
target_branch: codex/preview-runtime-fix
base_commit: 28c61f8c01072af1bd685118b790cc6f10dbcb46
status: in_progress
role: integrator
owner: codex-primary
updated_at: 2026-08-22T02:00:15+08:00
environment_fingerprint: windows-26200-amd64; documentation-only
scope_globs: ["AGENTS.md", "docs/ai/**", "docs/tasks/active/3.md", "docs/tasks/active/README.md", "docs/tasks/active/TEMPLATE.md", "docs/specs/SPEC-0004-ai-handoff-and-evidence.md", "schemas/task-handoff-v2.schema.json", ".github/ISSUE_TEMPLATE/**", ".github/PULL_REQUEST_TEMPLATE.md"]
shared_paths: ["docs/START_HERE.md", "docs/AI_HANDOFF.md", "docs/tasks/active/0.md", "tools/handoff-check.ps1", "tools/docs-check.ps1", "tools/context-pack.ps1"]
depends_on: [0]
specs: [SPEC-0004]
adrs: []
next_safe_action: "Merge the latest stacked target branch, resolve integration-document conflicts and rerun gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 3", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 3 handoff

## Objective

建立可機器檢查的多 AI 並行開發協定，讓每個工作切片使用獨立 Issue、worktree、branch、handoff
與 draft PR，並把高衝突整合檔改成單一 integrator ownership。

## Acceptance

- AI canonical instructions 定義 claim、scope、dependency、handoff 與 conflict protocol。
- handoff schema v2 有唯一 canonical schema，且不破壞 completed v1 歷史。
- `tools/handoff-check.ps1` 能檢查指定 Issue 或所有 numeric active handoff，不再硬編 Issue 0。
- GitHub Issue/PR template 提醒 AI 宣告 owner、scope、dependencies 與 required gates。
- documentation、handoff、source-only policy gates 通過；不變更產品行為、DSP 或 driver identity。

## Completed

- 建立 GitHub Issue #3 並從 PR #2 最新 head 建立獨立 branch。
- 完成 repository 歷史、衝突熱點與現有 handoff/schema gap 的唯讀分析。
- 新增多 AI canonical protocol、Issue/PR templates、v2 handoff schema 與 task template。
- 將 Issue 0 遷移為 integrator-only v2 handoff，並把 checker 從硬編 Issue 0 改為指定／枚舉模式。

## Known limitations

- 本變更只能定義 repository protocol；GitHub branch protection/ruleset 仍需 repository 管理者啟用。
- 本 task stacked on PR #2，合併前必須重新同步 target branch 並處理全域文件衝突。

## Last verification

- `pwsh -File tools/handoff-check.ps1 -Issue 3`: passed。
- `pwsh -File tools/handoff-check.ps1`: passed for Issues 0 and 3。
- `pwsh -File tools/docs-check.ps1`: passed, 85 required paths / 24 specs。
- `pwsh -File tools/source-policy.ps1`: passed before staging new files；staging 後需重跑。
- `pwsh -File tools/source-only-ci-check.ps1`: passed。

## Remaining work

1. 合併 PR #2 的最新 target head，保留兩邊 handoff/AI summary 內容。
2. Staging 後重跑 handoff/docs/source-policy gates，檢查 diff scope。
3. Push branch、開 stacked draft PR 並確認 CI。

## Next safe action

Merge the latest stacked target branch, resolve integration-document conflicts and rerun gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 3
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
