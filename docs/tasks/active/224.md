---
schema_version: 2
issue: 224
branch: codex/224-complete-archive-217
target_branch: main
base_commit: 1345618edb43faa47cf00f20e09f6a1ace311b67
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T19:45:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; vs2026-sdk-wdk-on-disk; target-24h2-machine-missing
scope_globs: ["docs/tasks/active/217.md", "docs/tasks/completed/2026/217.md", "evidence/0000-foundation/issue-217-complete-archive-187-v1.json", "docs/tasks/active/224.md", "evidence/0000-foundation/issue-224-complete-archive-217-v1.json"]
shared_paths: []
depends_on: [187, 189, 213, 214, 217, 220]
specs: [SPEC-0000]
adrs: []
archive_commit: pending
next_safe_action: "Commit and push the recovered archive/evidence, then wait for Draft PR #227 push and merge-ref CI before setting ready_for_review."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 224", "git diff --check"]
---

# Issue 224 handoff: complete archive after claim-only PR #220

## Objective

完成 PR #220 claim-only merge 遺留的 post-merge archive：將 Issue #217 active handoff
與其 anonymous evidence 移至 completed，保留 #220 claim-only takeover、未進入 main 的
archive commits 與 final verification 邊界，且不接續或改寫已合併的
`codex/217-complete-archive-187` branch。

## Acceptance

- `docs/tasks/active/217.md` 移至 `docs/tasks/completed/2026/217.md`，保留其 scope、local gates、PR #220 takeover 與限制。
- `evidence/0000-foundation/issue-217-complete-archive-187-v1.json` 隨 handoff 一併保留於 completed archive scope。
- `docs/tasks/active/224.md` 保持 schema v2，記錄 exact base/head、archive commit、final CI 與限制。
- 新增 anonymous corrective evidence 只描述文件封存、claim-only merge 與可重現檢查，不宣稱產品、RT audio、driver、HLK 或 signing 能力。
- Required handoff/docs/source-policy/source-only checks、`tools/verify.ps1` 與 `git diff --check` 通過；queued/in_progress 不算 CI 通過。

## Completed

- 已建立 Issue #224，確認 PR #220 已於 `10416b12608d91155a953ce8d59081679535693e` 由 claim-only head `c4422777fa2ed7b522dff053e466c029d891a7ba` 合併。
- 已確認實際 archive commits `2dedab5`、`3ae6f6d` 位於已合併 branch `codex/217-complete-archive-187`，不在 `origin/main`；本 lane 不接續該 branch。
- 已從最新 `origin/main=1345618edb43faa47cf00f20e09f6a1ace311b67` 建立獨立 worktree 與 branch；目前只建立本 claim handoff。
- 已完成 Issue #224 claim handoff check、push claim commit `39388a5`，並建立 Draft PR #227；live ownership 已可見後才擴大獨占 scope。
- 已將 `docs/tasks/active/217.md` 移至 `docs/tasks/completed/2026/217.md`，保留 PR #220 claim-only merge 與未進 main 的 archive commits。
- 已恢復 `evidence/0000-foundation/issue-217-complete-archive-187-v1.json` 並新增 #224 anonymous corrective evidence；兩者只記錄 governance/documentation archive。

## Known limitations

- 本 lane 是 governance/documentation correction，不改變產品 API、IPC/schema、DSP 順序、RT thread、driver、HLK 或簽章行為。
- 本機 target machine 尚未提供；文件 archive 與 user-space gates 不等於 Windows 11 24H2 hardware、loadable driver、HLK 或 Microsoft signing evidence。
- PR #220 的 claim-only merge 與未合併 archive commits 會保留在 evidence；不把舊 branch 的未進 main 變更宣稱為 main 能力。

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 224`：pass（claim scope）。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass（expanded scope）。
- `pwsh -NoProfile -File tools/docs-check.ps1`：pass（85 required paths / 24 specs；live measurement 515 tracked paths / 111 repository JSON files）。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass（515 tracked paths）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass（1 workflow / 515 tracked paths）。
- `pwsh -NoProfile -File tools/verify.ps1`：pass（Visual Studio 2026/CMake build；CTest 1/1）。
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`：pass（Windows 11 build 26200、VS 2026、SDK/WDK QFE fingerprint）。
- `pwsh -NoProfile -File tools/probe-environment.ps1`：pass（anonymous `.local/context.json` only）。
- `git diff --check`：pass。

## Remaining work

1. Commit and push the recovered archive/evidence.
2. Wait for Draft PR #227 push/merge-ref CI and record exact results.
3. CI 全綠後將 handoff 設為 `ready_for_review`；merge 後由 integrator 完成 archive lifecycle。

## Next safe action

Commit and push the recovered archive/evidence, then wait for Draft PR #227 push and merge-ref CI before setting ready_for_review.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 224
git diff --check
```
