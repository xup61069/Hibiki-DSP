---
schema_version: 2
issue: 263
branch: codex/263-handoff-detached-context
target_branch: main
base_commit: 62e94633bc093c8ef5cdcaf34397a8206984132d
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T19:25:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-sdk-10.0.28000.2526-wdk-10.0.28000.2526-cmake-4.4.2-pwsh-7.6.4
scope_globs: ["tools/handoff-check.ps1", "evidence/0000-foundation/issue-263-handoff-detached-context-v1.json", "docs/tasks/active/263.md"]
shared_paths: []
depends_on: [173]
specs: [SPEC-0004]
adrs: []
next_safe_action: "Run the existing handoff-check self-test and reproduce the detached branch-context boundary before extracting the pure resolver helper."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 263", "pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest"]
---

# Issue 263 handoff

## Objective

Make `tools/handoff-check.ps1` robust when run from a detached worktree, while preserving CI and
normal branch ancestry behavior. The current branch-context lookup calls `.Trim()` on a null
`git branch --show-current` result before falling back to `GITHUB_REF_NAME`.

## Acceptance

- Resolve branch context through a bounded pure helper: `GITHUB_HEAD_REF`, current branch,
  `GITHUB_REF_NAME`, then no context.
- A detached worktree with no branch context must not throw a null-method error; it may continue
  without branch-specific ancestry enforcement when no explicit `-Issue` is selected.
- Preserve explicit `-Issue` ancestry and branch-mismatch fail-closed behavior.
- Add deterministic in-memory self-test cases for head-ref priority, current-branch fallback,
  ref-name fallback, and no-context behavior.
- Add anonymous evidence and keep this slice limited to the gate, evidence, and handoff; no product
  runtime, audio, driver, signing, or shared integration path changes.
- Run required repository gates and record completed CI conclusions.

## Completed

- Fetched `origin` and confirmed the latest `origin/main` at
  `62e94633bc093c8ef5cdcaf34397a8206984132d` before creating the branch.
- Audited open Issues, open/draft PRs, active handoffs, worktrees, and remote branches. No active
  handoff, open PR, or remote branch currently owns `tools/handoff-check.ps1`.
- Created and claimed Issue #263, branch `codex/263-handoff-detached-context`, and this isolated
  worktree from the confirmed main commit.

## Known limitations

- This slice hardens governance-gate execution and does not change product runtime behavior,
  audio delivery, driver behavior, signing, or hardware evidence.
- A no-context detached run can skip branch-specific ancestry checks only when no explicit `-Issue`
  is selected; explicit issue checks remain fail-closed.

## Last verification

- Claim-only baseline: existing self-test and selected handoff checks are pending execution after
  the claim commit.

## Remaining work

1. Extract the branch-context resolver and add bounded self-test coverage without changing normal
   validation semantics.
2. Add anonymous evidence, run local gates, push the branch, create a Draft PR, and wait for
   completed green CI.
3. Mark this handoff `ready_for_review` only after the final CI head is green.

## Next safe action

Run the existing handoff-check self-test and reproduce the detached branch-context boundary before
extracting the pure resolver helper.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 263
pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest
```
