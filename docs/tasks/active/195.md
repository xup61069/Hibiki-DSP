---
schema_version: 2
issue: 195
branch: codex/195-portcls-filter-tables
target_branch: main
base_commit: 986b145d8602451349f168bb7e1f84d2cd29bdcb
status: in_progress
role: worker
owner: antigravity
updated_at: 2026-08-22T16:28:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["driver/wdk/**", "tools/driver-source-check.ps1", "evidence/0000-foundation/driver-portcls-filter-tables-v1.json", "docs/tasks/active/195.md", "build/baseline-counters.json"]
shared_paths: []
depends_on: []
specs: [SPEC-0003]
adrs: [ADR-0002, ADR-0004]
next_safe_action: "Implement PortCls filter descriptor and topology tables in driver/wdk and verify source gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 195", "pwsh -File tools/driver-source-check.ps1"]
---

# Issue 195 handoff: PortCls WaveRT Filter Descriptor and Topology Tables

## Objective

Provide the MS-PL PortCls filter descriptor (`PCFILTER_DESCRIPTOR`) and KS topology tables in `driver/wdk/` that define streaming and bridge pins, volume and mute nodes, connection topology graphs, and property automation tables for all four endpoints (Main, Low Latency, Surround 7.1, Virtual Mic) dispatching into `HibikiPropertyDispatchV1` and `endpoint_topology_v1` without linking GPL user-space code.

## Acceptance

- Implement PortCls `PCFILTER_DESCRIPTOR` generation for each endpoint index (Main, Low Latency, Surround, Virtual Mic).
- Define `PCPIN_DESCRIPTOR` tables for streaming WaveRT pin (KSPIN_COMMUNICATION_SINK/SOURCE, float LPCM datarange) and bridge physical pin (KSPIN_COMMUNICATION_NONE, KSNODETYPE_SPEAKER/MICROPHONE).
- Define `PCNODE_DESCRIPTOR` tables for `KSNODETYPE_VOLUME` and `KSNODETYPE_MUTE` with automation tables binding `KSPROPSETID_Audio` to `HibikiPropertyDispatchV1`.
- Define `PCCONNECTION_DESCRIPTOR` tables connecting streaming pins through mute/volume nodes to bridge pins.
- Wire `HibikiMiniportWaveRtV1::GetDescription` and `DataRangeIntersection` to the filter tables.
- Retain strict MS-PL licensing, non-blocking real-time audio paths, and zero GPL user-space linkage.
- Update `tools/driver-source-check.ps1` to enforce filter descriptor coverage.
- Pass all local gates and record anonymous evidence.

## Completed

- Claimed Issue #195 and initialized dedicated branch `codex/195-portcls-filter-tables`.

## Known limitations

- Source-only; this machine does not have WDK 10.0.28000.2526 installed and no `.sys` binary, HLK test or Microsoft signature is produced.

## Last verification

- Handoff schema initialized.

## Remaining work

1. Implement `driver/wdk/hibiki_filter_tables.h` and `driver/wdk/hibiki_filter_tables.cpp`.
2. Connect `GetDescription` and `DataRangeIntersection` in `driver/wdk/hibiki_miniport_wavert.cpp`.
3. Update `driver/wdk/README.md`.
4. Update `tools/driver-source-check.ps1`.
5. Generate `evidence/0000-foundation/driver-portcls-filter-tables-v1.json`.
6. Run all mandatory gates, refresh `build/baseline-counters.json` via `-WriteCounters`, commit and push.

## Next safe action

Implement PortCls filter descriptor and topology tables in driver/wdk and verify source gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 195
pwsh -File tools/driver-source-check.ps1
```
