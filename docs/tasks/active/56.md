---
schema_version: 2
issue: 56
branch: codex/56-source-policy-selftest
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T05:58:00+08:00
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/source-policy.ps1", "evidence/0000-foundation/source-policy-selftest-v1.json", "docs/tasks/active/56.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [51]
specs: [SPEC-0005]
adrs: []
next_safe_action: "Wait for the integrator to repair the merged Issue #51 docs-check merge-base path, then rerun PR #59 verify and keep this handoff in_progress until both checks pass."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 56", "pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest", "pwsh -NoProfile -File tools/source-policy.ps1"]
---

# Issue 56 handoff

## Objective

Add a bounded `-SelfTest` mode to `tools/source-policy.ps1` so the existing source-only
publication gate verifies its fail-closed blocked-extension, PE-header, and secret-pattern
rules using in-memory fixtures. This slice must not modify product code or claim driver, HLK,
signing, installer, or physical-audio evidence.

## Acceptance

- Refactor the existing extension, PE-header, and secret-pattern checks into reusable policy
  functions while preserving normal repository-scan behavior and output.
- `-SelfTest` covers a valid source-only fixture, case-insensitive blocked extensions, PE `MZ`
  header detection, each configured secret pattern, and an allowed text-only fixture, with a
  counted pass result.
- Self-test uses in-memory fixtures or byte arrays only; it does not write repository files,
  emit private paths, or invoke build, signing, install, driver, or hardware tooling.
- Add anonymous evidence JSON and retain the existing accepted SPEC-0005 source-only boundary;
  no new public API, IPC/schema, DSP order, RT-thread rule, or ADR is introduced.
- Required local gates and GitHub Actions verify must pass, subject only to the documented
  integrator-owned `docs/state/BASELINE.md` refresh.
- No overlap with Issue #55, whose scope is `tools/source-only-ci-check.ps1`, or with the active
  driver, installer, distribution, docs-gate, WinUI, doctor, or archive lanes.

## Completed

- Fetched and confirmed the latest `origin/main` at `cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9`.
- Reviewed open Issues, open/draft PRs, active handoffs, and remote branches; no unowned suitable
  Issue was available, and Issue #55 explicitly owns the adjacent source-only CI script.
- Created Issue #56 and an isolated `codex/56-source-policy-selftest` worktree from that main.
- Created this schema-v2 ownership claim within the declared scope.
- Refactored `tools/source-policy.ps1` into reusable checks and added `-SelfTest` with 10 bounded
  in-memory cases; implementation commit is `fb75920`.
- Normal source-policy scan and self-test passed; source-only CI policy passed; handoff checks and
  `git diff --check` passed.
- `tools/verify.ps1` passed CMake/MSBuild compilation and CTest 1/1; no compiled artifacts are
  tracked or included in this slice.
- Added anonymous evidence at `evidence/0000-foundation/source-policy-selftest-v1.json`.
- Removed literal secret-marker strings from the evidence after the normal source-policy scan
  correctly rejected them; the evidence remains anonymous and the scan now passes for 415 tracked
  paths.
- Synced the worker branch with merged `origin/main` `ec8456ff36b6ac64060fa4b7068f91602727c2da`
  via merge commit `803be9f`; the merged main now includes Issue #51 baseline tooling.

## Known limitations

- This self-test validates the source-policy gate's parser/policy behavior only; it is not
  evidence of signed binaries, a loaded driver, WaveRT/HLK, installer execution, or physical
  audio behavior.
- The self-test must remain synchronized with the configured policy lists when future policy
  categories are added.
- `docs/state/BASELINE.md` and the Issue #51 docs-check merge-base behavior are shared
  integrator-owned state and are not modified by this branch.

## Last verification

- `pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest`: passed (10 cases).
- `pwsh -NoProfile -File tools/source-policy.ps1`: passed for 415 tracked paths after syncing
  merged main.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 56`: passed.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: passed.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: passed for 413 staged tracked paths.
- `pwsh -NoProfile -File tools/verify.ps1`: passed CMake/MSBuild and CTest 1/1.
- `git diff --check`: passed.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked in strict local mode because merged
  `BASELINE.md` claims 413 tracked paths while this branch reports 415.
- `GITHUB_BASE_REF=main pwsh -NoProfile -File tools/docs-check.ps1`: reproduced the merged
  Issue #51 merge-base bug; multi-line `git show` output is treated as an array, so the PR path
  falls through to strict 413/415 assertion. This shared tooling issue is outside Issue #56 scope.
- PR #59 verify run `32530290362`: failed docs-check with baseline 411 versus 413 before the
  Issue #51 merge reached main.
- PR #59 verify run `32530359839`: failed docs-check with baseline 413 versus 415 on the merge
  ref that included Issue #51.

## Remaining work

1. Integrator repairs the Issue #51 merge-base handling without changing Issue #56 scope.
2. Rerun PR #59 verify after that repair; keep this handoff `in_progress` until CI is green.

## Next safe action

Wait for the integrator-owned Issue #51 docs-check repair, then rerun PR #59 verify.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 56
pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest
pwsh -NoProfile -File tools/source-policy.ps1
```
