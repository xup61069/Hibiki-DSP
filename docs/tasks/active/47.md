---
schema_version: 2
issue: 47
branch: codex/47-archive-25-28-32
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: ready_for_review
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T05:18:00+08:00
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["docs/tasks/completed/2026/25.md", "docs/tasks/completed/2026/28.md", "docs/tasks/completed/2026/32.md", "docs/tasks/active/25.md", "docs/tasks/active/28.md", "docs/tasks/active/32.md", "docs/tasks/active/47.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Integrator reviews this archive PR; after merge, Issues 25/28/32 leave no dangling active handoffs."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 47", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 47 handoff

## Objective

把已合併並關閉的 Issues #25（PR #26）、#28（PR #29）、#32（PR #33）的 active handoffs 移到
`docs/tasks/completed/2026/`，補上 merge commit、合併後 main verify run 與 `completed` 狀態，
讓 active 清單只反映仍在進行的工作。

## Acceptance

- 三份 completed handoffs 分別記錄 merge commit `143b85f`／`2e77175`／`d401b87` 與最終
  main verify runs `32522472509`／`32523448517`／`32524045933`（皆 success）。
- 原始 handoff 內容除生命週期更新外不流失。
- 每個移動對 tracked paths／JSON 計數淨零，BASELINE 摘要與 docs-check gate 不受影響。
- 本機 gates 通過；GitHub Actions verify 全綠後才請求 review。

## Completed

- Fetch 最新 origin/main（`cc899e0`）；確認 #25/#28/#32 已關閉且無人認領其封存
  （open issues/PRs 屬於其他切片：#30→PR #31、#39→PR #40、#41→PR #42、#43→PR #44、
  #46→遠端 branch live claim）。
- 建立 Issue #47、指派 `xup61069`、獨立 worktree／branch／本 handoff。
- 三份 handoffs 以 rename 方式封存，內容補上 merge SHA、main CI run、`completed` 狀態與
  封存紀錄；`shared_paths` 各自加入目的地路徑以利追蹤。

## Known limitations

- 封存僅重定位文件與補生命週期事實，不改寫各切片宣稱的能力邊界；所有「不是實體音訊／
  driver／HLK／signing evidence」限制原樣保留。
- Issue #34 的 PR #36 尚未合併，故 `docs/tasks/active/34.md` 留在 active，不在本切片範圍。
- 操作教訓（本切片首次 push 即被自己的 gate 攔下）：新增 handoff 檔案會使分支樹 +1，
  計數必須在所有檔案 staged/commit 之後量測；在 staging 前量測會得到舊值
  （411 vs 實際 412）。此陷阱在本日 #41 與 #47 各發生一次。

## Last verification

- 本機（portable pwsh 7.6.5）：handoff-check 全部 ✓（active 集合變為 #0/#34/#41/#43/#46/
  #47 等）、docs-check ✓、source-policy ✓、source-only-ci-check ✓、`git diff --check` clean ✓。
- 最終 branch push run 與 PR run：見 PR CI 記錄；queued/in_progress 不算通過。

## Remaining work

1. Issue #47 scope 內無剩餘工作；等 integrator review 並合併。

## Next safe action

Integrator reviews this archive PR; after merge, Issues 25/28/32 leave no dangling active handoffs.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 47
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
