---
schema_version: 2
issue: 278
branch: codex/278-scene-ack-rootcause
target_branch: main
base_commit: b89a284c8e6dad0bdb7a3768b6265b985e51c954
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T20:52:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-18.9-sdk-wdk-qfe-10.1.28000.2526-dotnet-10.0.400-pwsh-7.6.5
scope_globs: ["apps/control-model/EasyControlViewModel.cs", "apps/control-model/IpcProtocol.cs", "evidence/0000-foundation/control-model-scene-ack-reliability-v1.json", "docs/tasks/active/278.md"]
shared_paths: ["tools/control-model-engine-smoke.ps1"]
depends_on: [272]
specs: [SPEC-0001]
adrs: []
next_safe_action: "Reproduce the flaky scene-switch ack failure with a .local diagnostic harness, identify the exact SendCommandAsync false path, then implement the product-side fix."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 278", "pwsh -NoProfile -File tools/control-model-check.ps1", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 278 handoff: scene-switch ack intermittent false root-cause

## Objective

找出 `EasyControlViewModel.SelectSceneAsync("movie")` 在 control-model engine smoke 中
間歇回傳 `false` 的確切 `SendCommandAsync` 失敗路徑，並在不遮蔽 ack 失敗訊號的前提
下做 product-side 修復（timeout/retry/serialization 擇一，與 busy-merge 模型一致）。
控制面專屬，不影響 RT thread 規則。

## Acceptance

- 以可重跑的本地診斷（`.local/`，不進 Git）辨識四條 false path 中實際觸發的一條，
  並記錄重現統計。
- Product-side 修復限 `apps/control-model/EasyControlViewModel.cs`／`IpcProtocol.cs`，
  不以盲目 retry 遮蔽真實 ack 失敗；行為改變記錄於 evidence。
- `tools/control-model-engine-smoke.ps1` 的 StatusText 儀器化屬 follow-up：該檔目前被
  active lane #272（PR #276）佔用，scope 重疊禁止同時修改。
- 必跑 gates 通過：doctor、handoff-check（issue + full）、docs-check、source-policy、
  source-only-ci-check、control-model-check、winui-shell-check、verify、
  `git diff --check`。
- 不宣稱 physical audio / driver / signing 能力。

## Completed

- 已確認 smoke 全鏈無 caller token：`SendCommandAsync` 內 `OperationCanceledException`
  路徑在預設 token 下不可達；false 只能來自 (a) 入口/gate 內 `!IsConnected`、
  (b) gate 後 silent recheck（無 StatusText）、(d) 非 Ack reply、(e) pipe IO exception。
- 已確認 engine 端 `handle_control_frame_v1` 對 SceneApply 是 enqueue 即 Ack
  （control_service.cpp:95），且 pipe 為單一 request/response，無 unsolicited frame；
  `PrepareIrAsync` 回返時引擎 worker 可能仍在執行 IR transaction。
- ViewModel 所有 RoundTrip 共用 `_commandGate` 序列化；smoke harness 本身為喻執行緒
  順序 await。RequestId 錯配假說在協定層不成立，待重現驗證實際路徑。

## Known limitations

- 重現統計受本機負載影響（issue 觀察 1-in-3 至 burst）；修復證據為統計性而非決定性。
- `Program.cs`／`tools/control-model-engine-smoke.ps1` 儀器化需等 #272 lane 合併後另開
  child slice；本 slice 以 `.local` 診斷取代 repo 內儀器化。

## Last verification

- Baseline 尚未執行；見 Remaining work。

## Remaining work

1. `.local` 診斷 harness 重現並鎖定 false path（含 StatusText/IsDegraded 擷取）。
2. 依根因實作 product-side 修復。
3. 修复前後各跑多輪 control-model-engine-smoke 取得統計 evidence。
4. 必跑 gates、flip `ready_for_review`、push、draft PR。

## Next safe action

Reproduce the flaky scene-switch ack failure with a .local diagnostic harness, identify the exact SendCommandAsync false path, then implement the product-side fix.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 278
pwsh -NoProfile -File tools/control-model-check.ps1
pwsh -NoProfile -File tools/docs-check.ps1
```
