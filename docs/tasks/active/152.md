---
schema_version: 2
issue: 152
branch: codex/152-archive-87
target_branch: main
base_commit: 5783fbc7a5270434919dce1532ce602ace901581
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T11:02:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["docs/tasks/active/87.md", "docs/tasks/completed/2026/87.md", "docs/tasks/active/152.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: [87]
specs: []
adrs: []
next_safe_action: "Run the final scoped gates, commit the archive, push branch codex/152-archive-87, and wait for Draft PR #153 CI."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 152", "gh pr view 153 --json statusCheckRollup"]
---

# Issue 152 handoff

## Objective

Archive the completed Issue #87 handoff after PR #95 merged, preserving its
verification history and exact merge commit without changing product or shared
integration files.

## Acceptance

- Move `docs/tasks/active/87.md` to `docs/tasks/completed/2026/87.md`.
- Preserve the completed handoff and mark `status: completed`.
- Record PR #95 merge commit `34dab1a2477728a69655903f0bb0cf4093ca252b` and a terminal next-safe action.
- Keep this archive handoff schema v2 and within the declared scope.
- Run the required handoff, source-policy, source-only CI, docs, and diff checks.
- Keep `docs/state/BASELINE.md` integrator-owned; record any tracked-path drift without modifying it.

## Completed

- Confirmed Issue #87 is closed and PR #95 is merged at
  `34dab1a2477728a69655903f0bb0cf4093ca252b`.
- Confirmed this scope does not overlap the active #147, #145, #143, #141,
  #136, #131, or #128 archive lanes or another active handoff.
- Created this schema-v2 claim from fetched `origin/main`.

## Known limitations

- This slice is documentation archive evidence only.
- It does not add physical-audio, driver, WaveRT, HLK, signing, release, or
  hardware evidence.
- `docs/state/BASELINE.md` is a shared integrator file and is not writable by
  this worker.

## Last verification

- `git fetch origin`: `origin/main` is
  `5783fbc7a5270434919dce1532ce602ace901581`.
- Claim checks passed: `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 152`
  and the full `tools/handoff-check.ps1`.
- `pwsh -NoProfile -File tools/source-policy.ps1`: passed for 465 tracked paths.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: passed for one
  workflow and 465 tracked paths.
- `pwsh -NoProfile -File tools/verify.ps1` with the Visual Studio 2026 bundled
  CMake on PATH: configure/build passed and `hibiki_contract_tests` passed 1/1.
- `pwsh -NoProfile -File tools/probe-environment.ps1` with the bundled CMake:
  wrote `.local/context.json`.
- `git diff --check`: passed.
- `pwsh -NoProfile -File tools/docs-check.ps1`: blocked only by the
  integrator-owned baseline summary (BASELINE.md says 462 tracked paths while
  git reports 465); this worker did not modify `docs/state/BASELINE.md`.
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: Windows, architecture,
  git, PowerShell, and Visual Studio checks passed; default PATH does not
  expose CMake and the required WDK is unavailable. The build was still
  completed with the bundled CMake; no driver/HLK/signing claim is made.

## Remaining work

1. Commit the archive and this evidence update, then push branch
   `codex/152-archive-87`.
2. Keep Draft PR #153 updated with the exact commit and CI run results.
3. After CI completes, set this handoff to `ready_for_review` only if all
   required checks pass; baseline-only failures remain integrator-owned.

## Next safe action

Run the final scoped gates, commit the archive, push branch codex/152-archive-87, and wait for Draft PR #153 CI.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 152
gh pr view 153 --json statusCheckRollup
```
