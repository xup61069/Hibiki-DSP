---
schema_version: 2
issue: 97
branch: codex/97-live-session-volume-selftest
target_branch: main
base_commit: e9ea7fc87959ca0f2ba0e0a122065ffc3f76afa8
status: in_progress
role: worker
owner: xup61069
updated_at: 2026-08-22T10:40:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-msvc-14.51.36231-sdk-10.0.28000.0-cmake-path-missing-dotnet-10.0.400-pwsh-7.6.4
scope_globs: ["tools/live-session-volume-check.ps1", "evidence/0000-foundation/live-session-volume-selftest-v1.json", "docs/tasks/active/97.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: ["0"]
specs: []
adrs: []
next_safe_action: "Wait for the integrator to refresh docs/state/BASELINE.md in the current merge context, then rerun docs-check and CI; do not modify the shared baseline or run the live session write test in this worker slice."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 97", "pwsh -NoProfile -File tools/live-session-volume-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 97 handoff

## Objective

Add a deterministic, no-build `-SelfTest` mode to `tools/live-session-volume-check.ps1` that
validates the explicit `-WriteTest` and Windows-only guards plus the direct-coordinator and
Engine Preview session-routing launch plans without starting a process, opening IPC, touching
COM, creating a silent session, changing session volume, or changing route state.

## Acceptance

- `-SelfTest` is bounded, offline, deterministic, counted, and performs no CMake, build, process, IPC, COM, session, audio, or file-write activity.
- The self-test covers Windows and WriteTest opt-in guards, direct-coordinator and Engine Preview plan mapping, target/executable/argument mapping, and unsafe or mismatched plan rejection.
- Normal `-WriteTest` behavior remains unchanged and retains cleanup/restoration through the existing probe path.
- Evidence records user-space-only limits and does not claim physical per-App audio, driver, WaveRT, HLK, signing, or endpoint/session identity evidence.
- No product API, IPC schema, DSP ordering, RT rule, Spec, ADR, or shared integration snapshot changes.

## Completed

- Confirmed latest `origin/main` at `e9ea7fc87959ca0f2ba0e0a122065ffc3f76afa8`.
- Created the independent branch and worktree for Issue #97.
- Read the refreshed canonical AGENTS, START_HERE, AI_HANDOFF, MULTI_AGENT, CHANGE_CONTRACT, CONFLICT_POLICY, PROJECT_MAP, and active handoff contract.
- Confirmed no overlap with Issue #91 system-volume wrapper, #87 live-WASAPI handoff wrapper, #80 control-model smoke, or closed Issues #6/#10 implementation.
- Claimed this Issue with this schema-v2 handoff.
- Added pure Windows/WriteTest guard and direct-coordinator/Engine Preview session-routing plan helpers to `tools/live-session-volume-check.ps1`.
- Added a seven-case bounded `-SelfTest` before all live build/process/session/volume operations.
- Preserved the normal `-WriteTest` path while reusing the validated plan metadata.
- Passed self-test in default and `-DirectCoordinator` modes; normal invocation without `-WriteTest` remains fail-closed.
- Recorded anonymous evidence in `evidence/0000-foundation/live-session-volume-selftest-v1.json`.

## Known limitations

- This slice validates wrapper planning and opt-in safety only; it cannot prove physical per-App capture/re-send, DSP delivery, driver/WaveRT behavior, HLK, Microsoft signing, or endpoint/session identity.
- `docs/state/BASELINE.md` is integrator-owned; this worker will not modify it even if the documentation gate needs a counter refresh.
- The actual `-WriteTest` live probe was not run because it creates a silent session and changes local session volume; no session or volume state was changed by this slice.

## Last verification

- `tools/doctor.ps1 -CheckOnly`: pass with CMake PATH and locked SDK/WDK limitations.
- `tools/probe-environment.ps1`: pass; local fingerprint remains under ignored `.local`.
- `tools/live-session-volume-check.ps1 -SelfTest`: pass, 7 cases.
- `tools/live-session-volume-check.ps1 -SelfTest -DirectCoordinator`: pass.
- Normal invocation without `-WriteTest`: correctly rejected before live work.
- `tools/handoff-check.ps1 -Issue 97` and full handoff check: pass.
- `tools/source-policy.ps1` and `tools/source-only-ci-check.ps1`: pass for 425 tracked paths.
- `tools/verify.ps1` with the VS2026 CMake directory prepended process-locally: pass; CTest 1/1.
- `tools/docs-check.ps1`: blocked by shared baseline count 423 vs branch count 425.
- `git diff --check`: pass.
- Draft PR #98 is open at head `d43eddb`.
- Push CI run `32537864367` / job `96942030784`: configure/test passed; Documentation gate failed at baseline 423 vs branch 425; later gates skipped.
- Pull-request CI run `32537868125` / job `96942038840`: configure/test passed; Documentation gate failed at merge-ref baseline 425 vs git 427 (merge context included Issue #96); later gates skipped.

## Remaining work

1. Wait for the integrator-owned shared baseline to be refreshed in the current merge context.
2. Rerun docs-check and CI from the updated main/merge ref.
3. Keep this handoff `in_progress` until CI is green; after merge, archive it with the merge SHA and final CI.

## Next safe action

Wait for the integrator to refresh `docs/state/BASELINE.md`, then rerun docs-check and CI; do not modify the shared baseline or run the live session write test in this worker slice.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 97
pwsh -NoProfile -File tools/live-session-volume-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
