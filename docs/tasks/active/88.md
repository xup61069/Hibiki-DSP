---
schema_version: 2
issue: 88
branch: codex/88-ai-docs-refresh
target_branch: main
base_commit: 5e9e8a6051e203dd887a671f8198e2e42773cdc3
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-cmake-4.4.0-msvc-buildtools-17.14-dotnet-10.0.301; target-24h2-vs2026-wdk-missing
scope_globs: ["AGENTS.md", "docs/START_HERE.md", "docs/AI_HANDOFF.md", "docs/ai/MULTI_AGENT.md", "docs/state/BASELINE.md", "docs/PROJECT_MAP.md", "docs/tasks/active/88.md", "evidence/0000-foundation/ai-docs-refresh-v1.json"]
shared_paths: ["AGENTS.md", "docs/AI_HANDOFF.md", "docs/state/BASELINE.md", "docs/PROJECT_MAP.md"]
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Apply the documented drift fixes across the six entry documents, then run all gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 88", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1", "git diff --check"]
---

# Issue 88 handoff

## Objective

Refresh AI-facing entry documents (AGENTS, START_HERE, AI_HANDOFF, MULTI_AGENT,
BASELINE, PROJECT_MAP) to match repository reality after this week's merges and
capture operational lessons: pwsh-7-only gate scripts, single-machine multi-session
worktree etiquette, branch-squatting checks before claiming, mechanical BASELINE
counter refreshes under shared_paths, the #25 scope-overlap gate, the #51
merge-ref-aware gate, and the merged vst-host timeline chain (#14/#22/#35/#63/#72/#78).

## Acceptance

- Fresh agents learn pwsh-7 requirement, multi-session etiquette and claim checks
  before touching anything.
- Global snapshots reflect merged capabilities with unchanged user-space limits.
- Canonical tokens required by handoff-check adapters preserved; hard limits
  untouched.
- Documentation only; no product source change.

## Completed

- Claimed Issue 88 with an isolated worktree from `origin/main` (`5e9e8a6`).
- Conflict survey: excluded hot files (`tools/docs-check.ps1` #42/#67,
  `CONTRIBUTING.md` #82, tool self-test scopes); BASELINE limited to mechanical
  counters plus factual capability lines.

## Known limitations

- `docs/state/BASELINE.md` is triple-hot in flight (#42/#49/#58/#75 also refresh
  counters); integration order may require a conflict-resolution merge here.
- Documentation reflects reality through `5e9e8a6` only.

## Last verification

- Baseline not yet run at claim time; gates will be updated before the PR leaves
  draft.

## Remaining work

1. Apply edits across the six documents per the survey.
2. Refresh BASELINE counters via docs-check loop for this slice's two new files.
3. Run all gates locally; record evidence; wait for GitHub Actions.

## Next safe action

Apply the documented drift fixes across the six entry documents, then run all
gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 88
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
git diff --check
```
