---
schema_version: 2
issue: 34
branch: codex/34-driver-inf-section-gate
target_branch: main
base_commit: 2e77175e92e0fd952319772f2b8123262dc34821
status: planned
role: worker
owner: xup61069
updated_at: 2026-08-22T04:58:00+08:00
environment_fingerprint: unknown
scope_globs: ["tools/driver-signability-check.ps1", "docs/specs/SPEC-0003-virtual-endpoints-and-routing.md", "evidence/0000-foundation/driver-inf-section-gate-v1.json", "docs/tasks/active/34.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0003]
adrs: []
next_safe_action: "Run the Issue 34 handoff, doctor, and environment baseline checks before modifying the INF gate."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 34", "pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly", "pwsh -NoProfile -File tools/probe-environment.ps1"]
---

# Issue 34 handoff

## Objective

Make the source-only driver signability gate parse INF section structure instead of accepting
required tokens anywhere in the file. The gate must fail closed on missing or misplaced section
directives while keeping the external WDK/Inf2Cat package path explicit.

## Acceptance

- The source gate strips comments and validates required directives in their expected INF sections:
  Version, Manufacturer/NTamd64, install mapping, CopyFiles, service, and ServiceBinary.
- Missing, misplaced, or comment-only directives fail with section-aware diagnostics; the current
  source INF passes.
- Forbidden GPL payloads, private credentials, traversal paths, and non-source package claims
  remain rejected.
- A bounded `-SelfTest` covers valid INF, missing section directive, misplaced directive,
  comment-only token, and forbidden payload cases without modifying the repository.
- SPEC-0003 documents the section-aware source-only invariant and anonymous evidence records
  only source coverage.
- Required local gates and GitHub Actions verify pass. No `.sys`, HLK, signing, or install claim
  is made.

## Completed

- Fetched the repository and confirmed `origin/main` at
  `2e77175e92e0fd952319772f2b8123262dc34821`.
- Reviewed open Issues, open/draft PRs, active handoffs, worktrees, and branch ownership.
- Confirmed Issue #30 owns the doctor SDK layout gate and Issue #32 owns the extension permission/
  CSP gate; this scope does not overlap those claims or Issue 0 shared paths.
- Created the isolated Issue 34 worktree and branch from the latest `origin/main`.
- Created and published the Issue #34 ownership metadata; implementation has not started.

## Known limitations

- This is a source-only INF policy gate. It cannot build or install a SYS, run Inf2Cat without
  the locked WDK, exercise Driver Verifier/HLK, or prove Microsoft signing.
- The parser validates this repository's bounded INF source contract; future install sections
  require an explicit Spec/Issue update rather than silent acceptance.

## Last verification

- Claim baseline checks are pending; run the commands in `Next safe action` before modifying
  the tooling.

## Remaining work

1. Run the baseline environment and handoff checks and record the fingerprint.
2. Implement the section-aware INF parser and `-SelfTest` cases.
3. Update SPEC-0003, add anonymous evidence, run all required gates, and open the Draft PR.

## Next safe action

Run the Issue 34 handoff, doctor, and environment baseline checks before modifying the INF gate.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 34
pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly
pwsh -NoProfile -File tools/probe-environment.ps1
```
