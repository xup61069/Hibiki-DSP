---
schema_version: 2
issue: 197
branch: codex/197-live-baseline-counters
target_branch: main
base_commit: 31dbb6a7fe81f930057dd903a278d7ec69c70ec4
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-git-2.x-cmake-4.4.0; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/docs-check.ps1", "build/baseline-counters.json", "docs/state/BASELINE.md", "docs/ai/MULTI_AGENT.md", "evidence/0000-foundation/docs-check-live-counters-v1.json", "docs/tasks/active/197.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Refactor docs-check to measure counters live via git ls-files; delete committed counter file and prose digits."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 197", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 197 handoff

## Objective

Make `tools/docs-check.ps1` measure tracked-path and repository-JSON counters live
via `git ls-files` on every run (local, push, pull_request). Delete
`build/baseline-counters.json`; remove the counter summary line from BASELINE.md;
remove the counter-refresh chore from MULTI_AGENT. Keep fail-closed checks for
required entries, spec parsing, and summary markers.

## Acceptance

- No committed counter numbers are read anywhere.
- `build/baseline-counters.json` deleted.
- BASELINE.md keeps narrative but no counter digits.
- `-SelfTest` covers live measurement path.
- All gates stay green.

## Completed

- Claimed with isolated worktree from origin/main (`31dbb6a`).

## Known limitations

- None beyond documentation-only scope.

## Last verification

- Baseline not yet run at claim time.

## Remaining work

1. Refactor docs-check.ps1.
2. Delete build/baseline-counters.json.
3. Update BASELINE.md narrative and MULTI_AGENT.
4. Extend -SelfTest; run all gates; record evidence.

## Next safe action

Refactor docs-check to measure counters live via git ls-files; delete committed
counter file and prose digits.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 197
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
git diff --check
```
