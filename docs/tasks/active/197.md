---
schema_version: 2
issue: 197
branch: codex/197-live-baseline-counters-v2
target_branch: main
base_commit: 31dbb6a7fe81f930057dd903a278d7ec69c70ec4
status: ready_for_review
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T00:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/docs-check.ps1", "build/baseline-counters.json", "docs/state/BASELINE.md", "docs/ai/MULTI_AGENT.md", "evidence/0000-foundation/docs-check-live-counters-v1.json", "docs/tasks/active/197.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Refactor docs-check to measure counters live via git ls-files; delete committed counter file."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 197", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 197 handoff

## Objective

Make docs-check measure tracked-path and JSON counters live via git ls-files on every run.
Delete build/baseline-counters.json; remove counter summary line from BASELINE.md;
remove counter-refresh chore from MULTI_AGENT.

## Acceptance

- No committed counter numbers read anywhere.
- build/baseline-counters.json deleted.
- BASELINE.md keeps narrative but no counter digits.
- -SelfTest covers live measurement path.
- All gates stay green.

## Completed

- Claimed with isolated worktree from origin/main (31dbb6a).

## Known limitations

- None beyond documentation-only scope.

## Last verification

- `pwsh -File tools/docs-check.ps1 -SelfTest`: passed (13 cases including live
  git ls-files measurement).
- `pwsh -File tools/docs-check.ps1`: passed (85 paths / 24 specs; live measurement:
  500 tracked / 107 JSON).
- `pwsh -File tools/handoff-check.ps1 -Issue 197` and full run: passed.
- `pwsh -File tools/verify.ps1`: passed (CTest 1/1).
- `pwsh -File tools/source-policy.ps1` and `tools/source-only-ci-check.ps1`:
  passed.
- `git diff --check`: clean.
- Source commit: `cf6f3e9` (-324 lines of gate code removed).

## Remaining work

1. Wait for GitHub Actions on PR; queued/in_progress runs are not passing evidence.

## Next safe action

Wait for green CI, then request review and merge.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 197
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
git diff --check
```
