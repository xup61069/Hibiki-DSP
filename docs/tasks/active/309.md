---
schema_version: 2
issue: 309
branch: codex/309-extension-csp-allowlist
target_branch: main
base_commit: f18e57d37b363bebbcbb470220b5e7cdbebf2b61
status: in_progress
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T21:31:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-sdk-10.0.28000.2526-wdk-10.0.28000.2526-cmake-4.4.2-pwsh-7.6.4
scope_globs: ["tools/extension-check.ps1", "evidence/0000-foundation/extension-csp-allowlist-v1.json", "docs/tasks/active/309.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0009]
adrs: []
next_safe_action: "Run the post-evidence gates, push the branch, and open a Draft PR; this worker must not merge."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 309", "pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest", "git diff --check"]
---

# Issue 309 handoff

## Objective

Make the browser extension CSP policy gate fail closed when `script-src` or `object-src`
contains source expressions beyond the repository policy. Preserve the loopback-only
`connect-src` rule and keep this slice limited to the static policy gate, offline self-tests,
handoff, and anonymous evidence.

## Acceptance

- Reject `script-src` values containing any source expression beyond `'self'`.
- Reject `object-src` values containing any source expression beyond `'self'` or `'none'`.
- Preserve the existing `connect-src ws://127.0.0.1:17842` allowlist behavior.
- Add bounded offline self-test fixtures for external script and object sources.
- Run the scoped and full handoff checks, documentation/source policy gates, source-only CI gate,
  extension self-test, and `git diff --check`.
- Record anonymous evidence and keep the limitation explicit: static policy-gate evidence is not
  browser runtime, audio, driver, HLK, or signing evidence.

## Completed

- Fetched and confirmed latest `origin/main` at `f18e57d37b363bebbcbb470220b5e7cdbebf2b61`.
- Audited open Issues, PRs, remote branches, and active handoffs; no existing owner or PR covers
  the `tools/extension-check.ps1` CSP source-allowlist behavior.
- Claimed Issue #309 as `ox-alpha-opencode` in an isolated worktree.
- Confirmed the local Windows/Visual Studio/SDK/WDK/CMake/PowerShell environment with doctor.

## Known limitations

- This is a static extension policy-gate hardening slice; it does not launch a browser or prove
  extension runtime behavior.
- It does not provide physical audio, endpoint, driver, WaveRT, HLK, or Microsoft signing evidence.
- No production extension source, shared integration snapshot, IPC, DSP, or driver path is in scope.

## Last verification

- Baseline `tools/extension-check.ps1 -SelfTest` passed with 10 cases; repository handoff,
  documentation, source-policy, source-only, and diff gates also passed before implementation.
- `docs/specs/SPEC-0009-browser-tab-bridge.md` was read; this change enforces its existing exact
  extension-page CSP requirement without changing the accepted Spec.
- The scoped implementation is committed at `feb07c86c0238210b910a14b891137dadf10307a`.
- `tools/extension-check.ps1 -SelfTest`: pass, 12 cases; repository manifest policy check: pass.
- `tools/doctor.ps1 -CheckOnly`: pass; `tools/verify.ps1`: pass with Visual Studio 2026/CMake
  build, CTest 2/2, and unsigned local verification.
- Pre-evidence handoff, docs, source-policy, source-only, and diff gates passed; evidence is at
  `evidence/0000-foundation/extension-csp-allowlist-v1.json`.

## Remaining work

1. Run post-evidence handoff, docs, source-policy, source-only, and diff gates.
2. Commit the handoff/evidence metadata, push, open a Draft PR, and wait for CI.
3. Update the handoff with final CI and request integrator review; this worker must not merge.

## Next safe action

Run post-evidence gates, commit metadata, push, and open a Draft PR; this worker must not merge.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 309
pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest
git diff --check
```
