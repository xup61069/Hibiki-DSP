---
schema_version: 2
issue: 86
branch: codex/86-build-preview-selftest
target_branch: main
base_commit: 34dab1a2477728a69655903f0bb0cf4093ca252b
status: completed
role: worker
owner: xup61069
updated_at: 2026-08-22T10:17:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["tools/build-preview.ps1", "evidence/0000-foundation/build-preview-dispatch-selftest-v1.json", "docs/tasks/active/86.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: []
adrs: []
merge_commit: 07c17455aaa8b06f80c861b6dda5bd74d9e3ee42
next_safe_action: "None; Issue #86 is merged via PR #90."
resume_commands: ["gh pr view 90", "git show 07c17455aaa8b06f80c861b6dda5bd74d9e3ee42"]
---

# Issue 86 handoff

## Objective

Add a deterministic, no-build `-SelfTest` mode to `tools/build-preview.ps1` that verifies the four
supported target dispatch contracts and ignored output-root isolation without starting dotnet,
processes, WinUI, audio, driver, or installer work.

## Acceptance

- `-SelfTest` is bounded, offline, deterministic, and does not invoke dotnet, CMake, processes, or files outside ignored evidence output.
- `WinUI`, `WinUICompat`, `DesktopCompat`, and `ControlModel` retain their existing project identity and target-specific preview output roots.
- The normal build branches remain behaviorally unchanged except for reusing the tested dispatch metadata.
- Required local gates and CI results pass.

## Completed

- Rebased on latest `origin/main` at `34dab1a2477728a69655903f0bb0cf4093ca252b`.
- Added pure dispatch metadata helper and 5-case bounded `-SelfTest` to `tools/build-preview.ps1`.
- Added anonymous evidence at `evidence/0000-foundation/build-preview-dispatch-selftest-v1.json`.
- Verified all local gates pass.
- PR #90 merged into `main` at
  `07c17455aaa8b06f80c861b6dda5bd74d9e3ee42`.

## Known limitations

- This slice can validate PowerShell dispatch metadata only; it cannot prove a WinUI XAML build, Windows App Runtime availability, audio output, driver behavior, HLK, or signing.

## Last verification

- `tools/build-preview.ps1 -SelfTest`: pass, 5 cases.
- `tools/handoff-check.ps1 -Issue 86`: pass.
- `tools/handoff-check.ps1`: pass.
- `tools/source-policy.ps1`: pass for 454 tracked paths.
- `tools/source-only-ci-check.ps1`: pass.
- `tools/docs-check.ps1`: pass (85 required paths, 24 specs, 454 tracked paths, 91 repository JSON files).
- `tools/verify.ps1`: pass (CTest 1/1).
- `git diff --check`: pass.
- PR #90 merged at `07c17455aaa8b06f80c861b6dda5bd74d9e3ee42`.

## Remaining work

None. The implementation was merged by PR #90.

## Next safe action

None; Issue #86 is merged via PR #90.

## Resume commands

```powershell
gh pr view 90
git show 07c17455aaa8b06f80c861b6dda5bd74d9e3ee42
```
