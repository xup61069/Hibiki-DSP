---
schema_version: 2
issue: 41
branch: codex/41-docs-check-selftest
target_branch: main
base_commit: 87aac55db39821820b12a49fcce2ec9c7bd9668a
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T04:59:35+08:00
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["tools/docs-check.ps1", "evidence/0000-foundation/docs-check-selftest-v1.json", "docs/tasks/active/41.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0004]
adrs: []
merge_commit: 15330f19e7f4816bf3b0ac4a935774d41394e93d
next_safe_action: "None; Issue #41 is merged via PR #42."
resume_commands: ["gh pr view 42", "git show 15330f19e7f4816bf3b0ac4a935774d41394e93d"]
---

# Issue 41 handoff

## Objective

把 PR #26 為 `handoff-check.ps1` 引入的 `-SelfTest` 慣例鏡像到 Issue #21 的 baseline-summary
gate：`tools/docs-check.ps1` 新增 bounded 自測，以 in-memory fixtures 驗證每一個拒絕類別
（四個計數不符、marker 缺失）與合法輸入通過，全程不改動 repository 檔案。

## Acceptance

- gate 重構為可重用函式（輸入：摘要文字＋四個實測值）；main path 行為等價。
- `-SelfTest` 跑 bounded cases：合法摘要通過；required entries／specs／tracked paths／
  repository JSON 各自 mismatch 被拒；marker 缺失被拒；結尾輸出通過數並 exit 0。
- SelfTest 不依賴 repository 內容（純合成字串與注入數值）。
- 新增匿名 evidence JSON 記錄 self-test 命令與覆蓋類別。
- BASELINE.md 計數因本切片新增兩個 tracked files 而刷新。
- 本機 gates（含 `docs-check.ps1 -SelfTest`）通過；GitHub Actions verify 全綠後才請求 review。

## Completed

- Fetch 最新 origin/main（`87aac55`）；盤點 open work：#30/#34/#35 有 linked PR、#39 已有
  遠端 branch（live claim），全部不碰。確認 docs-check.ps1 無其他 owner。
- 建立 Issue #41、指派 `xup61069`、獨立 worktree／branch／本 handoff claim；draft PR #42。
- `tools/docs-check.ps1`：四項計數比對重構為參數化函式 `Test-BaselineSummary`
  （文字＋實測值）；main path 行為等價（同一組 pattern 與錯誤訊息）。
- 新增 `-SelfTest`：8 個 bounded in-memory cases——合法摘要通過、平移數值後仍通過
  （證明無寫死數字）、四個計數各自 mismatch 的拒絕訊息、全部 marker 缺失、僅缺
  tracked-path marker；結尾輸出通過數並 exit 0。
- 新增 `evidence/0000-foundation/docs-check-selftest-v1.json`（匿名、source_commit 錨定
  實作 commit `0944a8e`）。
- BASELINE.md 計數刷新為本 branch 實測 407 tracked paths／70 JSON（+1 handoff、+1 evidence）。
- PR #42 merged into `main` at
  `15330f19e7f4816bf3b0ac4a935774d41394e93d`.

## Known limitations

- **BASELINE 策略變更（整合排隊期間的經驗修正）**：本切片曾自帶計數 bump，但多個排隊
  PR 各自的 BASELINE 編輯會讓 integrator 逐一手動解衝突。現改為「BASELINE 與 origin/main
  完全一致、不自帶計數」——依 #51 的 PR 模式容差設計（未動 BASELINE 的 PR 由 base-tree
  驗證），讓合併序列零衝突；計數由 integrator 於合併時統一刷新。本 branch 的 BASELINE
  已與 `origin/main` 逐位元組一致。
- SelfTest 的 regex 樣本與 production gate 共用同一份 pattern 字串；若未來改寫 BASELINE
  摘要句式，SelfTest 的 valid-case 樣本必須同步更新。註：#64 lane 已將此 gate 演進為
  merge-ref-aware 版本並重寫 fixtures，本切片原始 8-case 版本由其取代。
- gate 只在 PR 自己的 branch 上執行；若 main 在 PR 生命週期內又前進且動到 BASELINE，
  需再同步一次（本日已三次）。
- CHANGE_CONTRACT 歸類：只新增既有 accepted gate 的測試覆蓋，無新 invariant／API／建置鏈
  變更，SPEC 文字維持 PR #23／#26 更新後的內容。

## Last verification

- Claim commit runs：push `32525597881`、PR run `32525638353`——皆 **failure**；原因：claim
  新增本 handoff 使 tracked paths 406→407，但 BASELINE 計數在 claim 時尚未同步，被本切片
  要強化的 gate 正確攔截。教訓已記錄：新增檔案的切片應在第一個 commit 就一併刷新計數。
- Implementation push run `32526237243`、PR run `32526241573`（feat commit）與
  `32526296616`／`32526301234`（evidence anchor commit）：結果以 PR #42 的最終 CI 清單為準；
  queued/in_progress 不算通過。
- Final PR #42 CI: PR merge-tree run `32527229206` success, push run `32527225118` success.
- 本機（portable pwsh 7.6.5）：docs-check `-SelfTest` 8 cases ✓ EXIT=0；docs-check 主路徑 ✓
  （85/24/407/70）；handoff-check -Issue 41 ✓；handoff-check 全部 ✓；source-policy 407 ✓；
  source-only-ci-check ✓；`git diff --check` clean ✓。

## Remaining work

None. The implementation was merged by PR #42.

## Next safe action

None; Issue #41 is merged via PR #42.

## Resume commands

```powershell
gh pr view 42
git show 15330f19e7f4816bf3b0ac4a935774d41394e93d
```
