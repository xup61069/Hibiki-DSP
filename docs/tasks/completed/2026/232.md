---
schema_version: 2
issue: 232
branch: codex/232-source-policy-selftest
target_branch: main
base_commit: bf9e38b17be01f124e4ec0f490784211bff7e755
status: completed
role: worker
owner: hibiki-worker
updated_at: 2026-08-22T17:50:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/source-policy.ps1", "evidence/0000-foundation/source-policy-selftest-v1.json", "docs/tasks/active/232.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0005]
adrs: []
next_safe_action: "Push and open draft PR; wait for CI verify including Gate self-test sweep."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 232", "pwsh -NoProfile -File tools/docs-check.ps1", "pwsh -NoProfile -File tools/source-only-ci-check.ps1"]
---

# Issue 232 handoff: Source-policy gate bounded SelfTest

## Objective

Add a bounded `-SelfTest` mode to `tools/source-policy.ps1` so the source-only secret/binary gate validates its blocked-extension, PE-header, and secret-pattern boundaries with fail-closed offline fixtures without touching repository files or claiming device/signature evidence.

## Acceptance

- Main source-policy check preserves its deterministic `git ls-files` (or fallback scan) + blocked-extension + PE-header + secret-pattern validation and fail-closed messages.
- `pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest` runs bounded, deterministic in-memory fixtures without writing repository files, compiled artifacts, endpoint identity, or private paths.
- Self-test covers: allowed text paths pass, single and multiple blocked extensions flagged case-insensitively preserving order, secret-pattern keyword detection, and clean success summary with explicit case count; failures remain fail-closed.
- Add anonymous evidence `evidence/0000-foundation/source-policy-selftest-v1.json` and record limitation that this is source-policy validation only, not physical artifact or device evidence.
- Run handoff, docs, source-policy, source-only, and `git diff --check` gates; CI verify passes.

## Completed

- Created Issue #232 and verified no active handoff claims `tools/source-policy.ps1` (2026-08-22T17:30 scan of `docs/tasks/active/*.md` and `git ls-remote --heads origin` shows no `codex/232*` branch).
- Created isolated worktree at `C:\AI\Hibiki-DSP-232-source-policy-selftest` on branch `codex/232-source-policy-selftest` from `origin/main@bf9e38b`.
- Established schema v2 handoff `docs/tasks/active/232.md`.
- Refactored `tools/source-policy.ps1` to extract `Get-SourcePolicyBlockedPaths` / `Get-SourcePolicySecretViolation` / `Test-SourcePolicyPeHeader` and added bounded `-SelfTest` with 10 in-memory fixtures (allowed, single/multiple/case-insensitive/all extensions, 3 secret patterns, clean text, PE header); main-path scan unchanged.
- Created anonymous evidence `evidence/0000-foundation/source-policy-selftest-v1.json` with tool versions and limitations.
- Verified all required gates pass locally (see Last verification).

## Known limitations

- Tooling/governance only; does not produce artifacts, signing, or physical device evidence.
- This machine is Windows 26200 with cmake missing (portable/user-space verification only); full build requires locked target toolchain.

## Last verification

- `pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest`: pass (10 cases: allowed, single/multiple/case-insensitive/all extensions, 3 secret patterns, clean text, PE header)
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass (515 tracked)
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 232`: pass
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass (22 active handoffs, no scope overlap)
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: pass (13 cases)
- `pwsh -NoProfile -File tools/docs-check.ps1`: pass (85 required / 24 specs; live 515 tracked / 111 JSON)
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1 -SelfTest`: pass (16 cases)
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass (1 workflow, 515 tracked)
- `pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest`: pass (6 cases)
- `pwsh -NoProfile -File tools/distribution-check.ps1`: pass
- `pwsh -NoProfile -File tools/winui-shell-check.ps1 -SelfTest`: pass (4 cases, 32 controls)
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: pass (Windows 26200 / VS2026 18.9 / SDK-WDK 28000.2526 present, cmake missing as expected)
- Gate self-test sweep: pass (27 modes including new source-policy)
- `git diff --check`: pass (exit 0)
- `evidence/0000-foundation/source-policy-selftest-v1.json`: valid JSON

## Remaining work

None for this slice; awaiting CI verify and review.

## Next safe action

Push and open draft PR; wait for CI verify including Gate self-test sweep.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/source-policy.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 232
pwsh -NoProfile -File tools/docs-check.ps1
pwsh -NoProfile -File tools/source-only-ci-check.ps1
```
