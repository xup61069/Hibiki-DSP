---
schema_version: 2
issue: 318
branch: codex/318-archive-301
target_branch: main
base_commit: 20b6bc152ffe9fc837f824b857daacbf44aa0e68
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T22:15:15+08:00
environment_fingerprint: windows-26200-amd64-vs2026-sdk-10.0.28000.2526-wdk-10.0.28000.2526-cmake-4.4.2-pwsh-7.6.4
scope_globs: ["docs/tasks/active/301.md", "docs/tasks/completed/2026/301.md", "evidence/0000-foundation/issue-313-archive-301-v1.json", "docs/tasks/active/318.md"]
shared_paths: []
depends_on: [301, 302]
specs: []
adrs: []
archive_commit: cf8b81f36f673268f56a51d78c5a9b771755d7ba
merge_commit: dbcbef2e8b4b35ce2d04007e46449c46af6b6c51
corrective_archive_commit: a796762f0600aa7c9ccd001bc0ac3638d0d62a5d
next_safe_action: "None; Issue #318 was completed by merged PR #324 and is archived by corrective Issue #330."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 318", "pwsh -NoProfile -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 318 handoff

## Objective

Archive the merged Issue #301 handoff by moving `docs/tasks/active/301.md` to
`docs/tasks/completed/2026/301.md`, preserving its archive history and recording PR #302's merge
commit, final CI, and anonymous archive evidence. The repository-contract counter command is
attempted and its availability is recorded without inventing an untracked schema.

## Acceptance

- Move only `docs/tasks/active/301.md` to `docs/tasks/completed/2026/301.md` and preserve its
  Issue #301 scope, limitations, archive commit, and PR #302 references.
- Record PR #302 merge commit `89c265b75e4bdd9ddc404f512150756de87d02a0` and final CI references.
- Add one anonymous evidence JSON under `evidence/0000-foundation/` with documentation-only
  limitations and no product or hardware claims.
- Attempt the repository-contract `tools/docs-check.ps1 -WriteCounters` command; if unavailable in
  this base, record the limitation rather than inventing a counters schema or shared file.
- Run the scoped/full handoff, docs self-test/docs-check, source-policy, source-only CI, doctor,
  verify, and `git diff --check` where supported; queued CI is not pass.
- Keep this archive independent from Issue #309's `tools/extension-check.ps1` scope and all other
  archive lanes; do not edit shared integration snapshots.

## Completed

- Fetched and confirmed latest `origin/main` at
  `20b6bc152ffe9fc837f824b857daacbf44aa0e68`.
- Confirmed PR #302 is merged at `89c265b75e4bdd9ddc404f512150756de87d02a0` and its merged PR
  checks are pass: push run `32575054597` / job `97036045083`, merge-ref run `32575057094` /
  job `97036051132`.
- Audited open Issues, PRs, remote branches, and active handoffs; no other lane owns the Issue
  #301 archive paths.
- Claimed Issue #318 as `ox-alpha-opencode` in an isolated worktree.
- Attempted `pwsh -NoProfile -File tools/docs-check.ps1 -WriteCounters`; the current gate rejects
  the parameter and `build/baseline-counters.json` is absent from this base, so no uncontracted
  counter file is added to this archive slice.
- PR #324 initial head CI passed: push verify run `32576580516` / job `97039704318` and
  merge-ref verify run `32576549292` / job `97039632121`.
- Issue #318 was archived by corrective Issue #330 after PR #324 merged at
  `dbcbef2e8b4b35ce2d04007e46449c46af6b6c51`; final PR #324 CI passed with push verify run
  `32576754848` / job `97040107021` and merge-ref verify run `32576752787` / job `97040102060`.

## Known limitations

- Documentation/governance archive only; no product API, IPC/schema, DSP, RT audio, driver, HLK,
  signing, or hardware behavior changes.
- Archive and user-space gates do not provide physical audio, loadable-driver, HLK, or Microsoft
  signing evidence.
- The current `tools/docs-check.ps1` has no `-WriteCounters` parameter and current main has no
  `build/baseline-counters.json`; this is recorded as a governance/tooling limitation.

## Last verification

- Issue #330 archive baseline: scoped handoff, docs self-test (13 cases), source-policy,
  source-only CI policy, doctor, and `git diff --check` passed. `tools/verify.ps1` passed with
  Visual Studio 2026/CMake build, CTest 2/2, and unsigned local verification.
- `tools/docs-check.ps1 -WriteCounters` was attempted and rejected because the current gate has
  no such parameter; no uncontracted counters file was added.
- Full `tools/handoff-check.ps1` and `tools/docs-check.ps1` remain blocked by unrelated open Issue
  #199's malformed Issue-body handoff; the gate reports an empty or unterminated block at
  `tools/handoff-check.ps1:207`. Issue #330 did not modify #199.

## Remaining work

None. PR #324 is merged and this handoff is moved to completed/2026 by corrective Issue #330.

## Next safe action

None; Issue #318 was completed by merged PR #324 and archived by corrective Issue #330.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 318
pwsh -NoProfile -File tools/docs-check.ps1
git diff --check
```
