---
schema_version: 2
issue: 32
branch: codex/32-extension-permission-csp-gate
target_branch: main
base_commit: 8cb294996d0e3ddbaccb053dba15f17916ea380c
status: completed
role: worker
owner: xup61069
updated_at: 2026-08-22T05:15:00+08:00
merge_commit: d401b8721c0528e54af3a94ecd1ac5cca21b66c6
environment_fingerprint: windows-26200-amd64-cmake-4.4.0-git-2.55.0-pwsh-7.6.5; target-vs2026-sdk-wdk-missing
scope_globs: ["tools/extension-check.ps1", "docs/specs/SPEC-0009-browser-tab-bridge.md", "evidence/0000-foundation/extension-permission-csp-gate-v1.json", "docs/tasks/active/32.md"]
shared_paths: ["docs/state/BASELINE.md", "docs/tasks/completed/2026/32.md"]
depends_on: []
specs: [SPEC-0009]
adrs: []
next_safe_action: "No Issue 32 task action remains; the MV3 extension gate fails closed on privilege expansion and CSP drift."
resume_commands: ["gh pr view 33 --repo xup61069/Hibiki-DSP"]
---

# Issue 32 handoff

## Objective

Make the MV3 browser extension source gate fail closed on privilege expansion and remote-code
drift. The extension must retain only the bounded tab-capture/offscreen permissions, localhost
bridge host permission, and self-only extension-page CSP required by SPEC-0009.

## Acceptance

- The gate rejects permissions outside `activeTab`, `tabCapture`, and `offscreen`.
- The gate rejects host permissions outside the exact `http://127.0.0.1/*` loopback boundary
  and rejects wildcard remote hosts.
- The gate requires exact self-only extension-page CSP with the fixed localhost WebSocket
  endpoint and rejects remote script/eval policy drift.
- A bounded `-SelfTest` covers allowed manifest, dangerous permission, broad host, and
  remote-code/CSP cases without modifying the repository.
- SPEC-0009 documents the least-privilege/CSP invariant and anonymous evidence records only
  source-only coverage.
- Required local gates and GitHub Actions verify pass. No browser runtime, physical audio,
  driver, HLK, or signing claim is made.

## Completed

- Fetched the repository and confirmed `origin/main` at
  `8cb294996d0e3ddbaccb053dba15f17916ea380c`.
- Created the isolated Issue 32 worktree and branch from the latest `origin/main` after a live
  ownership check against then-open Issues #21/#28/#30.
- Implemented the bounded manifest/CSP policy validator and `-SelfTest` cases in
  `tools/extension-check.ps1`; updated SPEC-0009 with the least-privilege/CSP invariant; recorded
  anonymous evidence in `evidence/0000-foundation/extension-permission-csp-gate-v1.json`
  (recorded on the feature branch as commit `75253ee docs: record Issue 32 verification evidence`).
- PR #33 merged to `main` as `d401b87`; Issue #32 closed.
- This handoff was archived from `docs/tasks/active/32.md` to
  `docs/tasks/completed/2026/32.md` by the Issue #47 hygiene slice; the move is net-zero for
  tracked-path and JSON counts.

## Known limitations

- This slice is a source-only manifest/CSP policy gate. It cannot prove browser permission
  prompts, user gesture enforcement, localhost service authentication, runtime WebSocket
  behavior, physical audio, driver, HLK, or Microsoft signing.
- The fixed permission/host allowlist is intentionally narrow; any future browser capability
  requires a separately reviewed Spec/Issue change.

## Last verification

- Local gates recorded by the original owner before review: handoff-check `-Issue 32`,
  doctor `-CheckOnly` (documented VS 2026/WDK warnings), probe-environment, plus the slice's
  extension-check gates and documentation/policy runs recorded in its evidence JSON.
- PR #33 merge to `main` verified by main push run `32524045933`: success.

## Remaining work

1. None within Issue #32.

## Next safe action

No Issue 32 task action remains; the MV3 extension gate fails closed on privilege expansion and CSP drift.

## Resume commands

```powershell
gh pr view 33 --repo xup61069/Hibiki-DSP
```
