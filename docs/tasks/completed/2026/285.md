---
schema_version: 2
issue: 285
branch: codex/285-asio-transport-selftest
target_branch: main
base_commit: b89a284c8e6dad0bdb7a3768b6265b985e51c954
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T21:18:00+08:00
environment_fingerprint: windows-26200-amd64-vs2026-sdk-10.0.28000.2526-wdk-10.0.28000.2526-cmake-4.4.2-pwsh-7.6.5
scope_globs: ["asio/src/asio_transport_selftest.cpp", "asio/CMakeLists.txt", "evidence/0000-foundation/asio-transport-selftest-v1.json", "docs/tasks/active/285.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0003]
adrs: [ADR-0004]
merge_commit: c0682736548c5fcc2f210113fd16577533377c10
next_safe_action: "None; Issue #285 was completed by merged PR #298 and archived by Issue #319."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 285", "pwsh -NoProfile -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 285 handoff: bounded offline self-test for the ASIO transport SPSC ring

## Objective

Give `hibiki_asio_transport_v1` (Apache-2.0 fixed-layout SPSC shared-memory ring) explicit,
deterministic in-memory fixtures: a valid 8-channel Float32 block roundtrip plus fail-closed
rejections for truncated region bytes, invalid init formats, corrupted region headers, channel/
frame mapping mismatch, ring overflow with dropped-block accounting, pop capacity overflow,
empty-ring pop, null arguments, and fixture-layer non-finite sample rejection. The production
COM transport and the sdk transport source stay unchanged; no COM launch, no file writes.

## Acceptance

- New `asio/src/asio_transport_selftest.cpp` builds as `hibiki_asio_transport_selftest`
  (guarded by `HIBIKI_BUILD_TESTS`) and is registered through in-scope `enable_testing()` /
  `add_test` inside `asio/CMakeLists.txt`, so `ctest` runs it without editing root or
  `tests/**` registry files.
- All fixtures are deterministic, offline, and bounded; failures return nonzero.
- Anonymous evidence `evidence/0000-foundation/asio-transport-selftest-v1.json` records case
  count, tool versions, and the offline/user-space limitation.
- This handoff reaches `ready_for_review` with base/head facts and gate results.
- Required gates pass: scoped + full `handoff-check`, `docs-check`, `source-policy`,
  `source-only-ci-check`, `doctor -CheckOnly`, `verify.ps1` (now 2 ctest cases), `git diff --check`.
- No product API, IPC/schema, DSP order, RT audio, driver, HLK, or signing behavior change;
  no claim of vendor ASIO, WaveRT, or physical audio evidence.

## Completed

- Fetched `origin`; confirmed base `origin/main=b89a284c8e6dad0bdb7a3768b6265b985e51c954`.
- Verified claim state: no linked PR, no remote `codex/285*` branch, no active `asio/**` scope
  overlap (#284/#286/#287 lanes touch vst-host, src calibration, extensions respectively).
- Read `sdk/include/hibiki/asio_transport_v1.h`, `sdk/src/asio_transport_v1.c`,
  `asio/CMakeLists.txt`, root `CMakeLists.txt`, and SPEC-0003 transport section.
- Reused the interrupted-but-clean prior-session worktree (zero commits/dirty files); declared
  continuation on Issue #285 before writing.
- Pushed claim commit `0e26e60ee45b4cc2dc00c7b41708c895fd3621ae` and opened draft PR #298.
- Added `asio/src/asio_transport_selftest.cpp` (12 deterministic offline cases) and the
  `HIBIKI_BUILD_TESTS`-guarded `hibiki_asio_transport_selftest` target with in-scope
  `enable_testing()/add_test` registration in `asio/CMakeLists.txt`.
- Added anonymous evidence `evidence/0000-foundation/asio-transport-selftest-v1.json`.

## Known limitations

- Offline source-level validation only; does not exercise the optional native ASIO COM DLL,
  any vendor SDK, real hardware, or the engine consumer path under load.
- Non-finite samples are rejected by the fixture layer of this self-test; production push keeps
  its current documented semantics (format/ring fail-closed), per "keep the production COM
  transport unchanged".
- No driver, WaveRT, HLK, Microsoft-signing, or physical-audio evidence; target Windows 11
  24H2+ acceptance still requires the locked environment.

## Last verification

- `pwsh -NoProfile -File tools/verify.ps1`: pass ??Visual Studio 2026/CMake build,
  ctest 2/2 (`hibiki_asio_transport_selftest` + `hibiki_contract_tests`), unsigned local
  verification; no warnings from the new target under `/W4 /permissive-`.
- Direct run of `.local/build/asio/RelWithDebInfo/hibiki_asio_transport_selftest.exe`:
  12 cases pass, exit 0.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 285`: pass.
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass (no scope overlap).
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: pass (13 cases); `tools/docs-check.ps1`: pass.
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass.
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: pass (Windows 26200 AMD64 / VS2026 18.9 /
  SDK-WDK 10.0.28000.2526 / cmake 4.4.2 / pwsh 7.6.5).
- `git diff --check`: pass.

## Remaining work

None for this slice; awaiting CI verify and integrator review of PR #298.

## Next safe action

Integrator reviews PR #298 against current `origin/main`; this worker must not merge it.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 285
pwsh -NoProfile -File tools/docs-check.ps1
git diff --check
```
