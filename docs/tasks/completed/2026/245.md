---
schema_version: 2
issue: 245
branch: codex/245-driver-source-selftest
target_branch: main
base_commit: 4f5e2c6f975970fcaf1aa371dca0907d433d04c5
status: completed
role: worker
owner: hibiki-worker
updated_at: 2026-08-22T18:25:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/driver-source-check.ps1", "evidence/0000-foundation/driver-source-selftest-v1.json", "docs/tasks/active/245.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0003]
adrs: [ADR-0002, ADR-0004]
archive_commit: eb96bdbf161df35f8a148db624986bb5c0aa14d0
archive_pull_request: 261
merge_commit: 5e5573f78653c87d36229979ad4c95aaa3cb2cda
merged_pull_request: 247
next_safe_action: "Push and open draft PR; wait for CI verify including Gate self-test sweep."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 245", "pwsh -NoProfile -File tools/docs-check.ps1", "pwsh -NoProfile -File tools/source-policy.ps1"]
---

# Issue 245 handoff: Driver-source gate bounded SelfTest

## Objective

Add a bounded `-SelfTest` mode to `tools/driver-source-check.ps1` so the WDK source-boundary gate validates its INF, topology, WaveRT stream, and miniport boundaries with fail-closed offline fixtures without requiring WDK, producing binaries, or claiming loadable driver evidence.

## Acceptance

- Main driver-source check preserves its deterministic file-existence + content-boundary validation and fail-closed messages.
- `pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest` runs bounded, deterministic in-memory fixtures without writing repository files, launching WDK tools, or producing binaries.
- Self-test covers: valid source passes, missing MS-PL, GPL linkage, missing required symbols, allocation detection, and clean success summary with explicit case count; failures remain fail-closed.
- Add anonymous evidence `evidence/0000-foundation/driver-source-selftest-v1.json` and record limitation that this is source-boundary validation only, not a loadable `.sys`, HLK, or signing evidence.
- Run handoff, docs, source-policy, source-only, driver-source, and `git diff --check` gates; CI verify passes.

## Completed

- Created Issue #245 and verified no active handoff claims `tools/driver-source-check.ps1` (2026-08-22T18:00 scan: `git ls-remote --heads origin` no `codex/245*`, active handoffs listed include 226 installer and 60 control-model but no driver-source SelfTest).
- Created isolated worktree at `C:\AI\Hibiki-DSP-245-driver-source-selftest` on branch `codex/245-driver-source-selftest` from `origin/main@4f5e2c6`.
- Established schema v2 handoff `docs/tasks/active/245.md`.
- Refactored `tools/driver-source-check.ps1` to extract `Assert-DriverAdapterPolicy`/`Assert-TopologyPolicy`/`Assert-StreamCorePolicy`/`Assert-StreamAdapterPolicy`/`Assert-InfPolicy` and added bounded `-SelfTest` with 11 in-memory fixtures (valid/missing-MS-PL/GPL, topology GUID, stream core allocation, stream adapter, INF credential); main-path file scan unchanged.
- Created anonymous evidence `evidence/0000-foundation/driver-source-selftest-v1.json` with tool versions and limitations.
- Verified all required gates pass locally (see Last verification).

## Known limitations

- Tooling/governance only; does not produce WDK build, `.sys`, HLK, or Microsoft signing evidence.
- This machine is Windows 26200 with cmake missing (portable/user-space verification only); full WDK validation requires locked target toolchain.

## Last verification

- `pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest`: pass (11 cases: valid/missing-MS-PL/GPL, topology GUID, stream core allocation, stream adapter, INF)
- `pwsh -NoProfile -File tools/driver-source-check.ps1`: pass (WDK source boundaries, 16 files)
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 245`: pass
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass (24 active handoffs, no scope overlap)
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: pass (13 cases)
- `pwsh -NoProfile -File tools/docs-check.ps1`: pass (85 required / 24 specs; live 524 tracked / 115 JSON)
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass (524 tracked)
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1 -SelfTest`: pass (16)
- `pwsh -NoProfile -File tools/extension-check.ps1 -SelfTest`: pass (6)
- `pwsh -NoProfile -File tools/distribution-check.ps1`: pass
- `pwsh -NoProfile -File tools/winui-shell-check.ps1 -SelfTest`: pass (4 cases, 32 controls)
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: pass (Windows 26200 / VS2026 18.9 / SDK-WDK 28000.2526 present, cmake missing as expected)
- Gate self-test sweep: pass (28 modes including new driver-source)
- `git diff --check`: pass (exit 0)
- `evidence/0000-foundation/driver-source-selftest-v1.json`: valid JSON

## Remaining work

None. PR #247 merged at `5e5573f78653c87d36229979ad4c95aaa3cb2cda`; the handoff was moved
to completed by batch archive PR #261 (`eb96bdbf161df35f8a148db624986bb5c0aa14d0`), and the
merge/archive references above were recorded by corrective Issue #268.

## Next safe action

None. Issue #245 is merged and archived; see Issue #268 for the reference-recording slice.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/driver-source-check.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 245
pwsh -NoProfile -File tools/docs-check.ps1
pwsh -NoProfile -File tools/source-policy.ps1
```
