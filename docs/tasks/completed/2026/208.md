---
schema_version: 2
issue: 208
branch: codex/208-archive-193
target_branch: main
base_commit: 0f1cdb1a44ddec3ac2c477357bbec001a00c741e
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T17:29:45+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; vs2026-sdk-wdk-on-disk; target-24h2-machine-missing
scope_globs: ["docs/tasks/active/193.md", "docs/tasks/completed/2026/193.md", "docs/tasks/active/208.md", "evidence/0000-foundation/issue-208-archive-193-v1.json", "build/baseline-counters.json"]
shared_paths: []
depends_on: [193, 194]
specs: [SPEC-0005]
adrs: []
next_safe_action: "Request integrator review and merge for PR #209; after merge, archive this handoff under docs/tasks/completed/2026/208.md."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 208", "git diff --check"]
---

# Issue 208 handoff: archive merged Issue 193

## Objective

封存已由 PR #194 合併完成的 Issue #193 handoff，保留 TARGET_MACHINE_CHECKLIST 的原始 scope、驗證與未驗證限制，並以獨立 archive lane 提供可重現的文件交接證據。

## Acceptance

- `docs/tasks/active/193.md` 只移動至 `docs/tasks/completed/2026/193.md`，不改寫其 implementation scope。
- `docs/tasks/active/208.md` 保持 schema v2，記錄 archive status、exact base/head、source/merge commit、CI 結果與限制。
- 新增 archive evidence 只描述文件封存與可重現的檢查結果，不宣稱 target machine、產品、RT audio、driver、HLK 或 signing 能力。
- Required handoff/docs/source-policy checks 與 `git diff --check` 通過；可用時執行 `tools/verify.ps1` 與 `tools/source-only-ci-check.ps1`。
- Draft PR body 包含 `Closes #208`、owner、handoff、head/target、scope、shared paths、depends_on、Spec/ADR impact、local/CI verification、evidence 與限制。

## Completed

- 已建立 Issue #208，確認 Issue #193 已關閉且 PR #194 已合併。
- 已確認沒有既有 Issue #193 archive PR，也沒有 `codex/193-archive-*` remote branch。
- 已從最新 `origin/main=0f1cdb1a44ddec3ac2c477357bbec001a00c741e` 建立獨立 worktree 與 branch，尚未修改產品程式碼。
- 已檢查 open Issue、open/draft PR、active handoff 與 remote branch ownership；本 slice scope 不與其他 active claim 重疊。
- 已提交並 push claim `549aa29`，建立 Draft PR #209。
- 已將 `docs/tasks/active/193.md` 移至 `docs/tasks/completed/2026/193.md`，archive/counter commit 為 `f33f5784507e663d23f58ac134fc5fbead9ea495`，並新增匿名 archive evidence。
- `origin/main` 後續合併 PR #189 至 `1adf947a50f836ee6371050ff5db58a09485df0e`；本 lane 以普通 merge `641adc2d7d6cf0b6c02e6d19ab322c46453282b4` 同步，counters 重新量測為 508 tracked paths / 111 repository JSON files。

## Known limitations

- 本 lane 僅是 governance/documentation archive，不改變產品 API、IPC/schema、DSP 順序、RT thread、driver、HLK 或簽章行為。
- 本機 CMake、Visual Studio 2026、Windows SDK/WDK 皆可由 doctor 找到，`tools/verify.ps1` 已完成 user-space build/CTest；實際 target machine、loadable driver、HLK 與 signing 仍未驗證，且 queued/in_progress CI 不算通過。
- TARGET_MACHINE_CHECKLIST 的實際 Windows 11 24H2/VS/SDK/WDK provisioning 仍須在鎖定 target machine 驗證；文件 archive 不增加該 evidence。

## Last verification

- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`：pass（Windows 11 build 26200、VS 2026、SDK/WDK on disk）。
- `pwsh -NoProfile -File tools/probe-environment.ps1`：pass，fingerprint 只寫入 ignored `.local/context.json`。
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 208`：pass。
- `pwsh -NoProfile -File tools/handoff-check.ps1`：pass。
- `pwsh -NoProfile -File tools/docs-check.ps1 -WriteCounters` 與 strict re-run：pass（502 tracked paths / 109 repository JSON files）。
- `pwsh -NoProfile -File tools/source-policy.ps1`：pass（502 tracked paths）。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`：pass（1 workflow / 502 tracked paths）。
- `pwsh -NoProfile -File tools/verify.ps1`：pass（user-space build、1/1 CTest）。
- `git diff --check`：pass。
- 最新主線同步 `1adf947` 後上述 handoff/docs/source/source-only/verify/diff gates 全部重跑並 pass；實測 counters 為 508/111。
- PR #209 metadata head `2df65cd` push verify：pass，run `32564927597` / job `97012075726`；PR merge-ref verify：pass，run `32564930454` / job `97012082539`。

## Remaining work

1. Request integrator review and merge for PR #209 now that both CI checks are green.
2. After merge, archive this handoff to `docs/tasks/completed/2026/208.md` with merge SHA and final CI.

## Next safe action

Request integrator review and merge for PR #209; after merge, archive this handoff under `docs/tasks/completed/2026/208.md`.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 208
git diff --check
```
