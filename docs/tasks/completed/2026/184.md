---
schema_version: 2
issue: 184
branch: codex/184-counter-doc-sync
target_branch: main
base_commit: 7892245618bedc57062e5bd26b1d4d271939b5ca
status: in_progress
role: integrator
owner: ox-alpha-opencode
updated_at: 2026-08-22T16:00:00+08:00
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-winget-msix; cmake-missing-wdk-missing-portable-user-space
scope_globs: ["AGENTS.md", "evidence/0000-foundation/counter-doc-sync-v1.json", "docs/tasks/active/184.md"]
shared_paths: ["docs/AI_HANDOFF.md"]
depends_on: [137, 182]
specs: [SPEC-0004]
adrs: []
next_safe_action: "Implement the AGENTS.md paragraph and AI_HANDOFF.md bullet rewrite, refresh counters JSON, run local gates."
resume_commands: ["git status --short --branch", "pwsh -File tools/docs-check.ps1 -SelfTest", "pwsh -File tools/handoff-check.ps1 -Issue 184", "pwsh -File tools/docs-check.ps1", "git diff --check"]
---

# Issue 184 handoff

## Objective

把 `AGENTS.md` 的計數指引段落與 `docs/AI_HANDOFF.md` 的 process-gates bullet 從已廢除的
「散文數字機械更新」制式改寫為 #137/#183 後的真實語意：揮發性計數真值在
`build/baseline-counters.json`、file-adding slice 以 `-WriteCounters` 重生並隨實作 commit、
counters 編輯一律受 head 嚴格嚴格驗證（僅 merge base 缺檔的首次引入容忍）。

## Acceptance

- `AGENTS.md` 該段落不再指示更新 BASELINE 散文數字；明確指向 JSON + `-WriteCounters`
  與 #183 嚴格語意。
- `docs/AI_HANDOFF.md` bullet 同步同組事實（一句話）。
- 本 slice 新增 tracked 檔案，implementation commit 內含 `-WriteCounters` 重生的 counters JSON。
- Required local gates pass；GitHub Actions verify pass。

## Completed

- Read AGENTS.md / START_HERE.md / AI_HANDOFF.md / MULTI_AGENT.md；synced main 至 `7892245`。
- Triple-checked claim：無 codex/184* branch 佔位、無 linked PR；`AGENTS.md` 無 active 宣稱；
  `docs/AI_HANDOFF.md` 由 0.md scope 宣稱，依 #67 BASELINE 先例以 shared_paths 協調。

## Known limitations

- Documentation only；不宣稱任何 RT audio、driver、簽章或產品行為。
- `docs/AI_HANDOFF.md` 屬 integrator 快照；本切片只改寫該一個 bullet，不動其他敘事。

## Last verification

- Baseline on base commit：`docs-check.ps1 -SelfTest` 21 cases pass（含 #183 guard cases）。

## Remaining work

1. 改寫兩處文件內容。
2. 新增 evidence JSON；`-WriteCounters` 重生 counters。
3. Local gates 全綠 → push → CI 綠燈轉 ready。

## Next safe action

Implement the two document edits, then refresh build/baseline-counters.json via -WriteCounters and run local gates.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/docs-check.ps1 -SelfTest
pwsh -File tools/handoff-check.ps1 -Issue 184
pwsh -File tools/docs-check.ps1
git diff --check
```
