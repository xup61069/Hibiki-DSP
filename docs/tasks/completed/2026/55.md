---
schema_version: 2
issue: 55
branch: codex/55-source-only-ci-selftest
target_branch: main
base_commit: cc899e08fa182fd02efb9d65a40b9e41c5ff7fe9
status: ready_for_review
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T05:43:07+08:00
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["tools/source-only-ci-check.ps1", "evidence/0000-foundation/source-only-ci-selftest-v1.json", "docs/tasks/active/55.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0005]
adrs: []
next_safe_action: "Refactor workflow/tracked-path scans into reusable functions, add bounded -SelfTest cases and evidence JSON, stage everything before measuring BASELINE counters."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 55", "pwsh -File tools/source-only-ci-check.ps1 -SelfTest", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 55 handoff

## Objective

延續 fail-closed 自測 wave：`tools/source-only-ci-check.ps1` 新增 bounded `-SelfTest`，以
in-memory fixtures 驗證 source-only 發行政策的不變量——workflow 禁用 pattern 掃描、必要
source-policy 參考、tracked binary 副檔名拒絕——全程不改動 repository 檔案。

## Acceptance

- workflow 掃描與 tracked-path 掃描重構為可重用函式；main path 行為等價。
- `-SelfTest` 覆蓋：乾淨 workflow＋source-policy 參考通過；缺 source-policy 參考被拒；
  各禁用 pattern 類別被拒且訊息指出 pattern 與檔名；blocked binary 副檔名不分大小寫被拒；
  允許的文字副檔名通過；多 workflow 場景仍能指認違規檔案；計數輸出並 exit 0。
- 新增匿名 evidence JSON。
- BASELINE 計數在所有本切片檔案 staged 之後量測刷新（應用 #41/#47 的量測教訓）。
- 本機 gates（含兩個 `-SelfTest` 模式）通過；GitHub Actions verify 全綠後才請求 review。

## Completed

- Fetch 最新 origin/main（`6cbadf6`，含 PR #49 的 Issue #47 封存）；盤點所有 open work：
  #30→PR #31、#39→PR #40、#41→PR #42（本 owner）、#43→PR #44、#46→PR #48、#50/#51/#52 均有
  遠端 branch 或 integrator claim。確認 `source-only-ci-check.ps1` 無任何 owner 涉及。
- 建立 Issue #55、指派 `xup61069`、獨立 worktree／branch／本 handoff。
- 掃描邏輯重構為 `Get-ForbiddenCiPattern`／`Test-WorkflowText`／
  `Assert-WorkflowPolicyReferencePresent`／`Get-BlockedTrackedPaths`；main path 行為等價
  （同一組 pattern、extension 常數與錯誤訊息）。
- 新增 `-SelfTest`：16 個 bounded cases——乾淨 workflow＋policy 參考通過、10 類禁用
  pattern 各自被拒且訊息指出 pattern 名稱、缺 source-policy 參考被拒、跨 workflow OR 語意、
  18 種 binary 副檔名＋大小寫全數攔截、多違規按輸入序合併回報、允許文字路徑零誤報；
  計數輸出並 exit 0。
- 新增 `evidence/0000-foundation/source-only-ci-selftest-v1.json`（錨定實作 commit `537f6f4`）。
- BASELINE 計數於所有檔案 staged 後量測刷新為 413 tracked paths／72 JSON；順手清除 main 上
  殘留的重複 repository JSON 摘要句（71/70 並存）。

## Known limitations

- SelfTest fixtures 與 production gate 共用 pattern/extension 常數；政策清單擴充時必須同步
  更新 fixtures——設計上的 fail-closed 提醒。
- `actions/upload-artifact` 條目在子字串比對下恆被較早的 `upload-artifact` 條目遮蔽；
  兩者仍同時強制執行，樣本斷言第一個命中的名稱。
- gate 只驗證 PR 自己的 branch；合併瞬間的計數漂移由下一個基於新 main 的切片修正
  （系統性限制，integrator 正以 Issue #51 自動化中）。
- CHANGE_CONTRACT 歸類：只新增既有 accepted 政策的測試覆蓋，無新 invariant／API／建置鏈變更。

## Last verification

- 本機（portable pwsh 7.6.5）：source-only-ci-check `-SelfTest` 16 cases ✓ EXIT=0；
  source-only-ci-check 主路徑 ✓（413 tracked）；handoff-check -Issue 55 ✓；handoff-check
  全部 ✓；docs-check ✓（85/24/413/72）；source-policy ✓；`git diff --check` clean ✓。
- Claim commit push run 曾在 BASELINE 未同步（412 vs 413）時失敗，已於後續 commit 修正；
  最終 branch runs 以 PR CI 清單為準；queued/in_progress 不算通過。

## Last verification (2026-08-22 revival)

- 同 owner（ox-alpha-opencode）中斷續作：以遠端 HEAD 為真值，本地 merge origin/main 乾淨無衝突
  （GitHub update-branch 的衝突回報為暫態誤報）。
- `tools/source-only-ci-check.ps1 -SelfTest` 16 cases pass；docs-check 全綠（counters JSON 由
  `-WriteCounters` 重生為本 head 實測值）；source-policy / source-only-ci / git diff --check pass。
- 本 worktree branch 名稱不同於 handoff 宣告（原 branch 被舊 worktree 佔用），`-Issue 55` 嚴格
  branch 核對由 CI 以正確名稱執行。
- `tools/verify.ps1` 本機缺 cmake 未跑，由 GitHub Actions 覆蓋。

## Remaining work

1. Issue #55 scope 內無剩餘工作；等 integrator review 並合併。
2. 合併後由 integrator 把本 handoff 移到 `docs/tasks/completed/2026/55.md`，記錄 merge SHA
   與最終 CI run。

## Next safe action

Integrator reviews this archive of the source-only CI self-test; after merge, archive this handoff to docs/tasks/completed/2026/55.md with merge SHA and final CI run.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 55
pwsh -File tools/source-only-ci-check.ps1 -SelfTest
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
