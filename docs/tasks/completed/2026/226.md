---
schema_version: 2
issue: 226
branch: codex/226-installer-check-selftest
target_branch: main
base_commit: 1345618edb43faa47cf00f20e09f6a1ace311b67
status: completed
role: worker
owner: hibiki-worker
updated_at: 2026-08-22T17:20:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["tools/installer-check.ps1", "evidence/0000-foundation/installer-check-selftest-v1.json", "docs/tasks/active/226.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0005]
adrs: []
merge_commit: 40cef330dbae616928d9268fedca8fa6098928fd
archive_commit: eb96bdbf161df35f8a148db624986bb5c0aa14d0
next_safe_action: "None; Issue #226 was completed by merged PR #231, moved by batch archive PR #261 (`eb96bdbf`), and its merge references were recorded by corrective Issue #290."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 226", "pwsh -NoProfile -File tools/docs-check.ps1", "pwsh -NoProfile -File tools/source-policy.ps1"]
---

# Issue 226 handoff: Installer gate bounded SelfTest

## Objective

Add a bounded `-SelfTest` mode to `tools/installer-check.ps1` so the installer source gate validates its manifest/hash boundary and fail-closed error handling without touching repository files, claiming artifact signing, or performing physical installer delivery.

## Acceptance

- Main installer check preserves its existing deterministic parse + boundary-containment validation and fail-closed behavior.
- `pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest` runs bounded, deterministic in-memory fixtures without writing repository files, compiled artifacts, endpoint identity, or private paths.
- Self-test covers: valid installer source passes, missing required boundary fails, parse-error handling, forbidden pattern detection, and clean success summary with explicit case count; failures remain fail-closed.
- Add anonymous evidence `evidence/0000-foundation/installer-check-selftest-v1.json` and record limitation that this is source-policy validation only, not physical installer, signing, or device evidence.
- Run handoff, docs, source-policy, source-only, installer, and `git diff --check` gates; CI verify passes.

## Completed

- Created Issue #226 via `gh issue create` and verified no active handoff claims `tools/installer-check.ps1` (2026-08-22T17:00+08:00 scan of `docs/tasks/active/*.md` and `git ls-remote --heads origin` shows no `codex/226*` branch).
- Created isolated worktree at `C:\AI\Hibiki-DSP-226-installer-check-selftest` on branch `codex/226-installer-check-selftest` from `origin/main@1345618`.
- Established schema v2 handoff `docs/tasks/active/226.md`.
- Refactored `tools/installer-check.ps1` to extract `Assert-InstallerSourcePolicy` / `Assert-InstallerParseInput` / `Assert-InstallerParseFile` and added bounded `-SelfTest` with 8 in-memory fixtures (valid pass, 6 missing-boundary rejections, parse-error rejection); main-path `ParseFile` + boundary check unchanged.
- Created anonymous evidence `evidence/0000-foundation/installer-check-selftest-v1.json` with tool versions and limitations.
- Verified all required gates pass locally (see Last verification).

## Known limitations

- Tooling/governance only; does not produce EXE/MSI/MSIX, signing, or physical device evidence.
- This machine is Windows 26200 with cmake missing (portable/user-space verification only); full Windows build requires locked target toolchain, verified via `tools/doctor.ps1 -CheckOnly`.

## Last verification

- `pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest`: pass (8 cases: valid, missing Read-ReleaseManifest/sha256/pnputil/IsPathRooted/thumbprint/rfc3161, parse-error)
- `pwsh -NoProfile -File tools/installer-check.ps1`: pass (real installer/HibikiSetup.ps1 parse + 12 boundaries)
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 226`: pass
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass (21 active handoffs, no scope overlap)
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: pass (13 cases)
- `pwsh -NoProfile -File tools/docs-check.ps1`: pass (85 required / 24 specs; live 514 tracked / 111 JSON)
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass (514 tracked)
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1 -SelfTest`: pass (16 cases)
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass (1 workflow, 514 tracked)
- `pwsh -NoProfile -File tools/extension-check.ps1` / `-SelfTest`: pass (6 cases)
- `pwsh -NoProfile -File tools/distribution-check.ps1`: pass
- `pwsh -NoProfile -File tools/winui-shell-check.ps1` / `-SelfTest`: pass (4 cases, 32 controls)
- `pwsh -NoProfile -File tools/driver-source-check.ps1`: pass
- `pwsh -NoProfile -File tools/driver-signability-check.ps1`: pass
- `pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly`: pass (Windows 26200 / VS2026 18.9 / SDK-WDK 28000.2526 present, cmake missing as expected)
- `pwsh` Gate self-test sweep: pass (26 modes including new installer-check)
- `git diff --check`: pass (only LF->CRLF autocrlf warning, no whitespace errors)
- `evidence/0000-foundation/installer-check-selftest-v1.json`: valid JSON
- PR #231 merged at `40cef330dbae616928d9268fedca8fa6098928fd`; handoff moved to
  `docs/tasks/completed/2026/` by batch archive PR #261 (`eb96bdbf161df35f8a148db624986bb5c0aa14d0`);
  merge/archive references recorded by corrective Issue #290
  (`evidence/0000-foundation/issue-290-complete-archive-226-245-v1.json`).

## Remaining work

None. PR #231 is merged and this handoff is in `completed/2026`; references recorded by Issue #290.

## Next safe action

None; Issue #226 was completed by merged PR #231, moved by batch archive PR #261 (`eb96bdbf`), and its merge references were recorded by corrective Issue #290.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/installer-check.ps1 -SelfTest
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 226
pwsh -NoProfile -File tools/docs-check.ps1
pwsh -NoProfile -File tools/source-policy.ps1
```
