---
schema_version: 2
issue: 284
branch: codex/284-vst-worker-protocol-selftest
target_branch: main
base_commit: b89a284
status: completed
merge_commit: aeda75fadfda31159e78caac0e9f84b187a8077a
role: worker
owner: ox-alpha
updated_at: 2026-08-22T22:45:00+08:00
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["vst-host/**", "tests/unit/contract_tests.cpp", "evidence/0000-foundation/vst-worker-protocol-selftest-v1.json", "docs/tasks/active/284.md"]
shared_paths: ["tests/unit/contract_tests.cpp"]
depends_on: [279, 285, 286]
specs: [SPEC-0012, SPEC-0014]
adrs: []
next_safe_action: "Integrator reviews PR #289 and revalidates CI on the target toolchain; this worker must not merge."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 284", "pwsh -File tools/docs-check.ps1", "pwsh -File tools/source-policy.ps1"]
---

# Issue 284 handoff

## Objective

Add a bounded, offline regression for `vst3_worker_protocol` frame codec so the
36-byte little-endian header, finite-sample check, and fail-closed error paths
have explicit in-memory fixtures. Keep the production supervisor/worker transport unchanged.

## Acceptance

- Deterministic in-memory fixtures cover: valid frame round-trip, truncated frame,
  non-finite sample rejection, and wrong-endian rejection.
- Main supervisor/worker framing still validates little-endian 36-byte headers and
  finite samples fail-closed.
- Anonymous evidence records the case count and offline limitation.
- Schema-v2 active handoff has exclusive scope, dependencies, verification commands,
  and CI evidence.
- Repository-required gates pass; no driver, WaveRT, HLK, or Microsoft-signing claims.

## Completed

- Claimed Issue 284 on GitHub; created worktree/branch from b89a284 (PR #289).
- Added six deterministic in-memory fixtures to tests/unit/contract_tests.cpp covering
  valid little-endian round-trip, explicit wire byte-order, truncated header,
  non-finite sample, wrong-endian rejection, and a parameter-frame regression guard.
- Added anonymous evidence JSON recording case count and offline limitation.

## Known limitations

- Offline in-memory fixtures only; no process launch, file writes, or device access.
- Does not prove driver, WaveRT, HLK, Microsoft-signing, or physical audio delivery.
- Local Windows SDK/WDK is missing; full MSVC compile evidence must be revalidated
  on the target toolchain.

## Last verification

- pwsh -NoProfile -File tools/doctor.ps1 -CheckOnly (pass; WDK missing warning noted).
- pwsh -NoProfile -File tools/probe-environment.ps1 (pass; fingerprint in .local/context.json).
- pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 284 (pass after handoff creation).
- cmake --preset windows-dev && cmake --build .local/build --target hibiki_contract_tests --config RelWithDebInfo -- /m:1 (pass).
- ctest --test-dir .local/build -C RelWithDebInfo (100% pass out of 1).
- pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 284 / docs-check / source-policy /
  source-only-ci-check / git diff --check (all pass).

## Remaining work

1. Integrator review of PR #289; no further worker edits planned.

## Next safe action

Integrator reviews PR #289 and revalidates CI on the target toolchain; this worker must not merge.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 284
pwsh -File tools/docs-check.ps1
pwsh -File tools/source-policy.ps1
```
