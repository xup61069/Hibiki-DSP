---
schema_version: 2
issue: 114
branch: codex/114-engine-preview-build-selftest
target_branch: main
base_commit: 6f3eb6b91294ce77961601a798b8087263aae3e2
status: completed
role: worker
owner: xup61069
updated_at: 2026-08-22T09:22:00+08:00
merge_commit: e3889a1
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["tools/build-engine-preview.ps1", "evidence/0000-foundation/build-engine-preview-selftest-v1.json", "docs/tasks/active/114.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: []
adrs: []
next_safe_action: "Merge PR #115 once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 114", "pwsh -NoProfile -File tools/build-engine-preview.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 114 handoff

## Objective

Add a bounded, offline `-SelfTest` mode to `tools/build-engine-preview.ps1` that validates the existing Engine Preview build dispatch contract without invoking CMake, compiling, launching a process, connecting IPC, enumerating devices, touching audio, or writing files.

## Acceptance

- `-SelfTest` is deterministic, source-only, offline, no-build, no-process, and no-file-write.
- It validates the existing Engine Preview configure/build target metadata and ignored output-root contract.
- Normal build behavior remains unchanged and still invokes CMake only without `-SelfTest`.
- Anonymous evidence and this schema-v2 active handoff are included.
- Required local gates and CI results pass.

## Completed

- Rebased on latest `origin/main` at `6f3eb6b91294ce77961601a798b8087263aae3e2`.
- Added pure `Get-EnginePreviewBuildPlan` and `Assert-EnginePreviewBuildPlan` helpers plus a 5-case `-SelfTest`.
- Preserved normal CMake build behavior.
- Added anonymous evidence at `evidence/0000-foundation/build-engine-preview-selftest-v1.json`.
- Verified all local gates pass.

## Known limitations

- The self-test is build-dispatch/source evidence only; it does not prove a successful CMake configure, compilation, Engine Preview launch, IPC delivery, physical audio delivery, driver, WaveRT, HLK, signing, or release behavior.

## Last verification

- `pwsh -NoProfile -File tools/build-engine-preview.ps1 -SelfTest`: pass, 5 cases.
- `pwsh -NoProfile -File tools/build-engine-preview.ps1`: pass.
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 114`: pass.
- `pwsh -NoProfile -File tools/docs-check.ps1`: pass (85 required paths, 24 specs, 436 tracked paths, 82 repository JSON files).
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass for 436 tracked paths.
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass.
- `pwsh -NoProfile -File tools/verify.ps1`: pass (CTest 1/1).
- `git diff --check`: pass.

## Remaining work

1. Wait for GitHub Actions verification on PR #115.
2. Merge PR #115.

## Next safe action

Merge PR #115 once GitHub Actions verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 114
pwsh -NoProfile -File tools/build-engine-preview.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
