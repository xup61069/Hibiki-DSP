---
schema_version: 2
issue: 173
branch: codex/173-handoff-selftest
target_branch: main
base_commit: e944084f0c7987903c9726528d865af83015a2ad
status: completed
role: worker
owner: codex-xup61069
updated_at: 2026-08-22T15:30:00+08:00
environment_fingerprint: windows-26200-pwsh7-dotnet10-no-vs2026-no-wdk; archive-refresh-pwsh-7.6.5
scope_globs: ["tools/handoff-check.ps1", "docs/tasks/active/173.md"]
shared_paths: []
depends_on: []
specs: []
adrs: []
merge_commit: e07b69802dde1a273e53aafb54965f71a205ec59
next_safe_action: "None; Issue #173 is merged via PR #174."
resume_commands: ["git status --short --branch", "pwsh -File tools/handoff-check.ps1 -Issue 173"]
---

# Issue 173 handoff

## Objective

把 `tools/handoff-check.ps1 -SelfTest` 從目前只有 9 個 glob-intersection cases 擴充為涵蓋完整驗證管線的 bounded 自測。

## Acceptance

- `-SelfTest` 覆蓋 front matter 解析、scalar/array 解析、schema_version、issue/filename 一致、safe branch、status/role 枚舉、base_commit SHA 格式、scope 重疊 fail-closed、depends_on 自我依賴、必要 headings、resume commands 前後一致。
- 全程使用 in-memory fixtures，不碰 repository 檔案。
- main path 行為等價（不影響現有 handoff 驗證）。
- bounded case count；無無界迴圈。

## Completed

- 建立 Issue #173 與 worktree。
- 建立 claim handoff docs/tasks/active/173.md（schema v2，in_progress）。
- 抽出 Test-HandoffFrontMatter 純函式（git 檢查留在主路徑）。
- 擴充 SelfTest 到 37 cases，涵蓋 glob overlap、front matter、scalar/array、
  schema_version、issue/filename 一致、safe branch、status/role 枚舉、SHA 格式、
  scope 重疊、depends_on 自我依賴、必要 headings、resume commands 前後一致。
- 主路徑改用新函式，行為等價。

## Known limitations

- 本切片只擴充 self-test 覆蓋率，不改變主驗證管線行為。

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -SelfTest` → passed (37 cases)。
- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 173` → passed。
- `pwsh -NoProfile -File tools/handoff-check.ps1` (all) → passed。
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest` → passed (18 cases)。
- `pwsh -NoProfile -File tools/source-policy.ps1` → passed (474 paths)。
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1` → passed。
- `git diff --check` → clean。

## Remaining work

None. The implementation was merged by PR #174.

## Next safe action

None; Issue #173 is merged via PR #174.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/handoff-check.ps1 -Issue 173
```
