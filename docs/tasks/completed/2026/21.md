---
schema_version: 2
issue: 21
branch: codex/21-baseline-summary-gate
target_branch: main
base_commit: 5c15b8edbbc806ee302bfd42a951fd24e586a827
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T04:27:19+08:00
merge_commit: 8cb294996d0e3ddbaccb053dba15f17916ea380c
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["tools/docs-check.ps1", "docs/tasks/active/21.md"]
shared_paths: ["docs/state/BASELINE.md", "docs/tasks/completed/2026/21.md"]
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "No Issue #21 action remains; keep BASELINE summary counters in sync with every merged change, enforced by the new docs-check gate."
resume_commands: ["gh pr view 23 --repo xup61069/Hibiki-DSP"]
---

# Issue 21 handoff

## Objective

把 `docs/state/BASELINE.md` 驗證摘要中的計數從人工維護改成 fail-closed：`tools/docs-check.ps1`
量測實際值（git tracked paths、repository JSON 檔數、自身必要入口與 Spec 數），並與 BASELINE
宣稱值比對，不一致即失敗。背景：Issue #8 整合時發現摘要宣稱 379/78/35 但實際已不同；PR
#12/#15/#17/#18 合併後 main 又宣稱 387/62，實際為 396/65。漂移應由 CI 攔截，不靠人工察覺。

## Acceptance

- docs-check 量測並比對四個計數：tracked paths、repository JSON 檔數、必要入口數、Spec 數；
  宣稱缺失或不符合時 throw，錯誤訊息同時列出宣稱值與實測值。
- BASELINE.md 摘要段落缺 marker 時同樣 fail-closed。
- 本 branch 先把 BASELINE 計數更新成本 branch 實測值讓 gate 通過。
- 本機 gates（handoff-check -Issue 21、docs-check、source-policy、`git diff --check`）通過；
  GitHub Actions verify workflow 全綠後才請求 review。

## Completed

- Fetch 最新 origin/main（`5c15b8e`）；確認無 open issue、open PR 僅 #19（gemini 的 handoff
  封存，只動 `docs/tasks/completed/**`，與本切片無檔案重疊）；實測 main 漂移：396 tracked
  paths / 65 JSON vs 宣稱 387 / 62。
- 建立 Issue #21、指派 `xup61069`、獨立 worktree／branch／本 handoff claim；draft PR #23。
- `tools/docs-check.ps1` 新增 fail-closed gate：量測 git tracked paths、repository JSON 數，
  並與 BASELINE 驗證摘要宣稱的必要入口數、Spec 數逐一比對；marker 缺失或數值不符即 throw，
  錯誤訊息同時列出宣稱值與實測值與修正提示。
- BASELINE.md 計數更新為 branch 實測值並加一句說明新 gate；負向測試：暫時改錯計數後
  docs-check 以 EXIT=1 失敗（「BASELINE.md claims 396 tracked paths but git reports 397」），
  還原後恢復通過。
- 多 writer 事件（CONFLICT_POLICY 記錄）：本 branch ready_for_review 後，另一 writer 在
  未更新 owner 的情況下把 main（含 PR #26）merge 進本 branch（`02a5027`），首次宣稱
  403 tracked paths；CI gate 立即以 failure 攔下（runs `32522545492`、`32522549957`），
  該 writer 修正為實測 401／67（`e313f0a`）後轉綠（push `32522781674`、PR `32522785024`
  success）。本 owner 檢查遠端 head 內容完整包含本切片所有檔案且自洽後，放棄本地未發布
  的平行 merge commit，將本地 branch 對齊遠端真值；事件已記錄於 Issue #21 comment，
  並重申單一 writer 界線。
- PR #23 以 head `e313f0a` 合併進 main，merge commit `8cb2949`；Issue #21 已關閉。
- 本 handoff 由 `codex/21-archive-handoff` 封存至 `docs/tasks/completed/2026/21.md`；
  移動為淨零 tracked-path 變化，BASELINE 計數不受影響。

## Known limitations

- gate 只涵蓋 BASELINE 驗證摘要段落中明確標記的計數；各 handoff 內的歷史驗證紀錄是時間點
  記錄，故意不納入 gate，避免每個 feature PR 都被迫改 integrator-owned handoff。
- 「verify.ps1 的 1 個 CTest」需要 cmake toolchain，無法在 docs-check 內量測，維持文字描述。
- regex 以中文措辭為錨點（與 handoff-check.ps1 同樣需要 `pwsh`；CI 與 AGENTS 必跑命令均已
  固定使用 pwsh）。若未來改寫摘要句式，docs-check 會以 missing-counter 訊息 fail-closed，
  提醒同步更新 gate。
- CHANGE_CONTRACT 歸類：不改 public API/schema/DSP/建置鏈；CI workflow YAML 不變，只強化
  既有 docs-check gate，因此不需要 adr/readme/ci-update 全套。

## Last verification

- Claim commit runs：push `32520972993` success、PR run `32521011382` success。
- Implementation push run `32521693718`：success（verify.yml 全部 gates 通過，含新 gate）。
- Gate 攔截漂移的實證：runs `32522545492`、`32522549957` failure（宣稱 403 vs 實測 401），
  修正後 runs `32522781674`、`32522785024` success。
- Merge 後 main 的 verify run `32523043178`：success。
- 本機（對齊合併狀態後，portable pwsh 7.6.5）：handoff-check 全部 ✓、handoff-check
  `-SelfTest` ✓（PR #26 新增）、docs-check gate ✓、source-policy ✓、`git diff --check`
  clean ✓。

## Remaining work

1. Issue #21 無剩餘工作。

## Next safe action

No Issue #21 action remains; keep BASELINE summary counters in sync with every merged change, enforced by the new docs-check gate.

## Resume commands

```powershell
gh pr view 23 --repo xup61069/Hibiki-DSP
```
