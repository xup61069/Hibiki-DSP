---
schema_version: 2
issue: 256
branch: codex/256-live-counters-doc-sync
target_branch: main
base_commit: 5e5573f78653c87d36229979ad4c95aaa3cb2cda
status: completed
role: worker
owner: hibiki-worker
updated_at: 2026-08-22T20:58:00+08:00
environment_fingerprint: windows-26200-amd64-vs17-sdk-26100-dotnet10-winapp-runtime-1.7.7000.785; target-24h2-vs2026-wdk-missing
scope_globs: ["AGENTS.md", "docs/AI_HANDOFF.md", "evidence/0000-foundation/live-counters-doc-sync-v1.json", "docs/tasks/active/256.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
merge_commit: d31c820ec3fe51003788c644401ec91162996793
corrective_archive_commit: 900410917893bcb2a656f8cd203ed9929784eec7
next_safe_action: "None; Issue #256 was completed by merged PR #258 and is archived by corrective Issue #301."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 256", "pwsh -NoProfile -File tools/docs-check.ps1", "pwsh -NoProfile -File tools/source-policy.ps1", "git diff --check"]
---

# Issue 256 handoff: Live counters doc sync

## Objective

Sync `AGENTS.md` and `docs/AI_HANDOFF.md` to the live-measurement truth established in `docs/ai/MULTI_AGENT.md:96` and `tools/docs-check.ps1` after Issue #197 retired `build/baseline-counters.json` and the `-WriteCounters` chore.

## Acceptance

- `AGENTS.md` no longer mentions `-WriteCounters` or `build/baseline-counters.json`; it states counters are measured live via `git ls-files` and no chore is needed.
- `docs/AI_HANDOFF.md` bullet similarly updated; no WriteCounters instruction remains.
- `tools/docs-check.ps1` still passes (85/24 live), `handoff-check -Issue 256` and full check pass, `source-policy` and `git diff --check` pass.
- Anonymous evidence recorded; docs-only, no product behavior change.

## Completed

- Created Issue #256 and verified no active handoff claims `AGENTS.md`/`docs/AI_HANDOFF.md` live-doc lane (2026-08-22T18:30 scan: `git ls-remote --heads origin` no `codex/256*`, active handoffs include 226/232/245 but distinct docs).
- Created isolated worktree at `C:\AI\Hibiki-DSP-256-live-counters-doc-sync` on branch `codex/256-live-counters-doc-sync` from `origin/main@5e5573f` (includes merged #247 driver-source SelfTest, live 524/115 at base, now 530/118 with recent archives).
- Established schema v2 handoff `docs/tasks/active/256.md`.
- Edited `AGENTS.md:39` to replace `-WriteCounters`/`build/baseline-counters.json` paragraph with live-measurement wording matching `docs/ai/MULTI_AGENT.md:96` (live `git ls-files`, no chore, #197 retired JSON).
- Edited `docs/AI_HANDOFF.md:131` bullet to live measurement (retired WriteCounters, now live `git ls-files`).
- Verified all required gates pass locally (see Last verification).

## Known limitations

- Docs-only; does not change `tools/docs-check.ps1` logic, which already measures live.
- This machine is Windows 26200 with cmake missing (portable/user-space verification only).

## Last verification

- `pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 256`: pass (after resume_commands fix)
- `pwsh -NoProfile -File tools/handoff-check.ps1`: pass (27 active handoffs, no scope overlap)
- `pwsh -NoProfile -File tools/docs-check.ps1 -SelfTest`: pass (13 cases)
- `pwsh -NoProfile -File tools/docs-check.ps1`: pass (85 required / 24 specs; live 530 tracked / 118 JSON)
- `pwsh -NoProfile -File tools/source-policy.ps1`: pass (530 tracked)
- `pwsh -NoProfile -File tools/source-only-ci-check.ps1`: pass (1 workflow)
- `Select-String` confirms no `WriteCounters`/`baseline-counters` remains in `AGENTS.md`/`AI_HANDOFF.md` except the new live wording
- `git diff --check`: pass (exit 0)
- `evidence/0000-foundation/live-counters-doc-sync-v1.json`: valid JSON (to be created)
- PR #258 final CI passed: push run `32568436753` / job `97020358389` and merge-ref run
  `32568450110` / job `97020388212`; PR #258 merged at
  `d31c820ec3fe51003788c644401ec91162996793`.

## Remaining work

None. PR #258 is merged and this handoff is moved to completed/2026 by corrective Issue #301.

## Next safe action

None; Issue #256 was completed by merged PR #258 and is archived by corrective Issue #301.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 256
pwsh -NoProfile -File tools/docs-check.ps1
pwsh -NoProfile -File tools/source-policy.ps1
git diff --check
```
