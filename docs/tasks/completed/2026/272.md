---
schema_version: 2
issue: 272
branch: codex/272-ir-clear-race
target_branch: main
base_commit: c6516e1
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T21:25:00+08:00
merge_commit: 80e0f365d1215a20897140ebcd14c4c01e40c3f7
environment_fingerprint: windows-26200-amd64-pwsh-7.6.5-dotnet-10.0.400-cmake-4.4.2-vs2026-18.9; wdk-missing
scope_globs: ["apps/control-model-engine-smoke/Program.cs", "docs/tasks/active/272.md"]
shared_paths: []
depends_on: []
specs: [SPEC-0004]
adrs: []
next_safe_action: "Merge once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -File tools/control-model-engine-smoke.ps1", "pwsh -File tools/docs-check.ps1"]
---

# Issue 272 handoff

## Objective

消除 `tools/control-model-engine-smoke.ps1` 的間歇性失敗（IR race）：scene-apply ack 與狀態
快照最終一致——engine 在 scene 交易完成前算出的快照會把本地「IR 已清除」覆蓋回 prepared，
單一取樣斷言因此隨機失敗。改為 bounded poll（30×100 ms，附主動 refresh），deadline 內未清才失敗。

## Acceptance

- IR-cleared 檢查容忍已記錄的輪詢延遲模型；行為回歸仍會在 deadline 後 fail-closed。
- 僅測試碼變更；產品碼零觸碰。
- 本機多次執行不再出現「Scene switch did not clear the prepared IR state.」。

## Completed

- Reproduced：原版本機 ~1/3 失敗（CI 三連過為時序僥倖）。
- Root cause 定位於 `SelectSceneAsync` 的單次 `RefreshControlStatusAsync` 競態（見 issue #272）。
- Program.cs 改為 bounded poll；ack 失敗與 deadline 失敗分開拋出、訊息明確。

## Known limitations

- 第二種獨立 flake（scene ack 間歇未收到）屬產品端調查，已另開 #278 追蹤；
  本切片不遮蔽該訊號（ack 失敗立即失敗，不重試）。
- 本機樣本受負載影響大；正確性論證基於輪詢模型而非統計。

## Last verification

- 修復後本機連跑：IR-race 失敗模式未再現（觀察到的失敗全為 #278 的 ack 模式）。
- `tools/docs-check.ps1` live-measurement 全綠；handoff-check `-Issue 272` pass。

## Remaining work

1. GitHub Actions verify 綠燈後合併。

## Next safe action

Merge once verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -File tools/control-model-engine-smoke.ps1
pwsh -File tools/docs-check.ps1
```
