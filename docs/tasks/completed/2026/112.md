---
schema_version: 2
issue: 112
branch: codex/112-live-process-loopback-selftest
target_branch: main
base_commit: bb667e9e9966fcc061ae52cec6805ff6997fa9ee
status: completed
role: worker
owner: xup61069
updated_at: 2026-08-22T09:27:00+08:00
merge_commit: d09800c
environment_fingerprint: windows-11-26200-x64-preview
scope_globs: ["tools/live-process-loopback-check.ps1", "evidence/0000-foundation/live-process-loopback-selftest-v1.json", "docs/tasks/active/112.md"]
shared_paths: ["docs/state/BASELINE.md"]
depends_on: []
specs: [SPEC-0016]
adrs: [ADR-0002]
next_safe_action: "Merge PR #113 once GitHub Actions verify passes."
resume_commands: ["git status --short --branch", "pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 112", "pwsh -NoProfile -File tools/live-process-loopback-check.ps1 -SelfTest", "pwsh -NoProfile -File tools/docs-check.ps1"]
---

# Issue 112 handoff

## Objective

Add a deterministic, no-build `-SelfTest` mode to `tools/live-process-loopback-check.ps1` that
validates its Windows-only guard and exact CMake/build/probe plan without configuring CMake,
compiling, starting the probe, activating Audio Service process loopback, capturing frames,
printing endpoint or process identities, or changing audio state.

## Acceptance

- `-SelfTest` is bounded, offline, deterministic, counted, and performs no file, build, process,
  COM, Audio Service, endpoint, process-loopback, or audio side effect.
- The self-test covers Windows guard rejection, exact CMake definitions, build root, target,
  executable path, and mismatched/unsafe plan rejection.
- Normal Windows probe behavior remains unchanged and its output remains anonymous format/frame
  aggregate evidence.
- Required local gates and CI results pass.

## Completed

- Rebased on latest `origin/main` at `bb667e9e9966fcc061ae52cec6805ff6997fa9ee`.
- Added pure Windows guard, plan construction, exact plan validation, and a 5-case bounded `-SelfTest` in `tools/live-process-loopback-check.ps1`.
- Added anonymous evidence at `evidence/0000-foundation/live-process-loopback-selftest-v1.json`.
- Verified all local gates pass.

## Known limitations

- This slice is limited to wrapper planning and anonymous user-space process-loopback probe boundaries.
- It must not record endpoint IDs, friendly names, PIDs, session IDs, serials, private paths, or compiled artifacts.

## Last verification

- `tools/live-process-loopback-check.ps1 -SelfTest`: pass, 5 cases.
- `tools/handoff-check.ps1 -Issue 112`: pass.
- `tools/handoff-check.ps1`: pass.
- `tools/source-policy.ps1`: pass for 438 tracked paths.
- `tools/source-only-ci-check.ps1`: pass.
- `tools/docs-check.ps1`: pass (85 required paths, 24 specs, 438 tracked paths, 83 repository JSON files).
- `tools/verify.ps1`: pass (CTest 1/1).
- `git diff --check`: pass.

## Remaining work

1. Wait for GitHub Actions verification on PR #113.
2. Merge PR #113.

## Next safe action

Merge PR #113 once GitHub Actions verify passes.

## Resume commands

```powershell
git status --short --branch
pwsh -NoProfile -File tools/handoff-check.ps1 -Issue 112
pwsh -NoProfile -File tools/live-process-loopback-check.ps1 -SelfTest
pwsh -NoProfile -File tools/docs-check.ps1
```
