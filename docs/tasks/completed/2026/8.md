---
schema_version: 2
issue: 8
branch: codex/8-engine-system-volume-integration
target_branch: main
base_commit: 12ee53b8e30c5a6f696253d3d1a34cdb827368eb
status: completed
role: worker
owner: ox-alpha-opencode
updated_at: 2026-08-22T03:44:00+08:00
merge_commit: b8c4fd73b22cf33614ba6cfb297b7119ff39a8c2
environment_fingerprint: windows-26200-amd64-powershell-5.1-dotnet-only-portable-pwsh-7.6.5-no-cmake-no-vs; ci-github-actions-windows-2025
scope_globs: ["tests/live_engine_system_volume_probe.cpp", "tests/CMakeLists.txt", "tools/live-system-volume-check.ps1", "evidence/0000-foundation/system-volume-live-v1.json", "docs/tasks/active/8.md"]
shared_paths: ["AGENTS.md", "README.md", "docs/AI_HANDOFF.md", "docs/START_HERE.md", "docs/state/BASELINE.md", "docs/tasks/active/0.md"]
depends_on: []
specs: [SPEC-0002]
adrs: []
next_safe_action: "No Issue #8 task action remains; work is merged."
resume_commands: ["gh pr view 13 --repo xup61069/Hibiki-DSP"]
---

# Issue 8 handoff

## Objective

把平行 AI snapshot `codex/preview-runtime-fix` 的 system-volume write-through commits
（`c42cabd`、`5782c9e`、`ab2d677`）整合進最新 `main`（`12ee53b`），成為獨立於 Issue #10
route-rule scope 的工作切片；保留多 AI v2 protocol 與 Issue #0 integrator ownership，
並維持「user-space Engine Preview 控制面證據，不是實體 endpoint／driver／HLK／signing
evidence」的限制宣告。

## Acceptance

- 使用專屬 integration branch 與本 handoff；不修改、不 force-push 平行 AI 的
  `codex/preview-runtime-fix` branch。
- 保留 snapshot 的 probe（`tests/live_engine_system_volume_probe.cpp`）、CMake target
  註冊、`tools/live-system-volume-check.ps1` 預設 Engine Preview write-through 路徑與
  `-DirectBroker` 除錯旁路。
- 全域 handoff/baseline 文件以 integrator-owned shared paths 方式套用最小必要更新，
  不退化 main 的多 AI protocol 內容。
- GitHub Actions 的 build/test 與 documentation/source-only gates 全綠後才請求 review；
  queued/in_progress 不算通過。
- 維持明確限制：write-through probe 是 user-space evidence，不得宣稱實體 endpoint、
  driver、WaveRT、HLK 或 Microsoft signing 完成。

## Completed

- Fetch 最新 origin/main 並確認 base `12ee53b`；檢查 open Issues（#8、#10 皆無
  assignee）、open/draft PR（0 個）、active handoffs（僅 `0.md`），確認無 scope overlap。
- 建立 Issue #8 claim：獨立 worktree、branch `codex/8-engine-system-volume-integration`、
  schema v2 handoff；Issue 已指派 `xup61069`，draft PR #13 已開啟。
- Cherry-pick snapshot commit `c42cabd`（probe＋CMake target＋tool 預設 write-through 路徑與
  `-DirectBroker`）成為本 branch `e4ea524`（保留原 authorship 與 `-x` provenance）。
- 將 `5782c9e`＋`ab2d677` 的文件/evidence 內容改寫成 current main 結構：
  AGENTS.md、README.md、docs/AI_HANDOFF.md、docs/START_HERE.md、docs/state/BASELINE.md、
  evidence JSON；`docs/tasks/active/0.md` 只套用系統音量相關段落，並把 base commit 對齊
  snapshot head `ab2d677`。
- BASELINE／Issue 0 handoff 的驗證摘要數字改以本機實測更新：source-policy 387 tracked
  paths、docs-check 85 required paths、62 個 repository JSON 可解析。
- PR #13 passed GitHub Actions CI runs and merged into `main` as `b8c4fd73b22cf33614ba6cfb297b7119ff39a8c2`, closing Issue #8.

## Known limitations

- 本機環境沒有 `cmake` 與 MSVC，無法在本機重建 C++ probe；CI 的 `verify.ps1` 也不開
  `HIBIKI_BUILD_LIVE_PROBES`，所以 probe 的 compile/run evidence 來自 source snapshot
  在原機器成功執行 `-WriteTest` 的紀錄（evidence JSON `source_commit: c42cabd`）。整合
  branch 以 cherry-pick 保證內容一致（diff 已核對），但未在整合 branch 重新編譯執行。
- `-WriteTest` 是 opt-in 且會短暫改變本機系統音量；本切片未在任何機器上重新觸發它，
  也要求 CI 與應用程式啟動不得隱式執行。
- 整合結果仍只是 user-space IPC→`IAudioEndpointVolume` write-through 證據；不是實體
  per-App delivery、driver、HLK 或 signing evidence。

## Last verification

- Source push verify run `32514507807`（snapshot `ab2d677`）：passed。
- Claim commit runs：push `32517100896` success、PR `32517138099` success。
- Implementation runs：push `32517921210` success、PR run `32517924881` success
  （verify.yml 全部 14 個 gate 通過，含 Configure/test、docs、source-policy、
  source-only CI、control model、driver boundary、JSON parse、stable identity）。
- PR #13 merged as `b8c4fd7`.

## Remaining work

1. None within Issue #8; work is merged and archived.

## Next safe action

No Issue #8 task action remains; work is merged.

## Resume commands

```powershell
gh pr view 13 --repo xup61069/Hibiki-DSP
```
