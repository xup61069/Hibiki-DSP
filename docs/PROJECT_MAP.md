# Hibiki DSP 專案地圖

新 AI 先讀 `docs/AI_HANDOFF.md`，再用本檔定位子系統；本檔描述結構，不取代
`docs/state/BASELINE.md` 的已驗證狀態或 foundation integration Issue 的下一步。

| 子系統 | 目的 | 目前狀態 | 主要契約 |
| --- | --- | --- | --- |
| `driver/` | 固定虛擬端點、KS volume/mute、私有 control IPC | ABI + MS-PL endpoint control core + fixed four-endpoint/channel-mask catalog + portable WaveRT Float32 SPSC ring/underrun boundary + topology-indexed WDK render/capture format/pin adapters + source-only INF package template + local kernel-mode .sys/Inf2Cat/signability build evidence + local self-signed test-sign packaging evidence (.sys/.cat signed; cert import, TESTSIGNING boot and install remain user-consented)；PortCls runtime 接線與載入驗證待 target 機器 | ADR-0002/ADR-0004/ADR-0005/SPEC-0003 |
| `src/` | C++20 RT graph、AudioEngine、per-output-group render、graph-to-WASAPI adapter、legacy channel-map＋optional 8×8 matrix routing、per-group plugin latency compensation 與跨 block lane delay bank、bounded multi-sink output fan-out、dB-domain volume ramp、session registry/session-route graph builder、bounded per-App session route rule store、Windows session→immutable route-graph coordinator、bounded process-loopback plan（PID collision fail-closed）、Windows process-loopback Float32 source boundary、bounded physical device catalog、Windows COM device catalog worker/snapshot publisher/store/service/control runtime、worker-owned App session catalog with ephemeral handles、validated SessionVolumeCommand handle boundary、validated SessionRouteCommand graph-candidate boundary、validated SessionRouteRuleCommand candidate-rule boundary、Windows session adapter、固定容量 session COM worker queue、Windows volume/device watcher/recovery、WindowsVolumeLink broker-to-Group-Master adapter、endpoint-ID-preserving volume rebind、ISO math、program-aware RMS／fixed-state K-weighted proxy level controller、fixed-capacity PEQ biquad processor、bounded measured-response→PEQ calibration compiler、fixed-capacity FIR IR convolver、bounded RIFF/WAVE IR decoder、control-plane minimum/mixed/linear phase kernel builder、basic high-pass/downward-gate suppressor、IR phase policy、IPC codec/named-pipe worker、固定 SPSC control queue、Easy Scene control-worker adapter、transactional output handoff、persistent SRC、scene safety policy、sink crossfade、ASIO/外部 Lane block API、WASAPI Float32/PCM output boundary、dual-worker WASAPI sink handoff、Windows WASAPI multi-sink fan-out、Virtual Mic privacy/reference、bounded echo cancellation/noise gate、scene IR reference preservation（schema ir reference/hub contracts/engine carry-through） | 可測試 user-space；實體端點/driver 尚未 soak | SPEC-0001/0002/0003/0006/0007/0011/0012/0013/0015/0016/0019/0020/0021/0022/0023 |
| `apps/` | WinUI 3 control plane、Easy/Expert UX | UI-independent .NET control model + fixed output-group catalog + bounded custom Scene card mirror + observable Vst3TimelineSurfaceModelV1 mirror/binding notifications/editor view model with RemoveSelectedRow/ClearHistory + bounded persistent Expert per-App route-rule catalog + bounded calibration response/PEQ profile control model and Equalizer APO/CamillaDSP/REW/Hibiki exporters + physical device catalog mirror/DeviceSwitch request + ephemeral App session catalog mirror/SessionCatalogRequest transport + handle-bound SessionVolumeCommand／SessionRouteCommand／SessionRouteRuleCommand builders + DeviceCatalogRequest/ControlStatusRequest transport + atomic status/route/volume projection + unsolicited-frame transport + async Hello/Scene/volume transport + source-only WinUI 3 shell with integrated title bar (drag region/status pill), adaptive hero quick-start panel (connect, busy indicator, one-tap enhance and status chips) and responsive two-column custom scene form with unified card chrome (corner radius 12, 24px padding, accent icons, chip readouts, tinted IR phase sub-panel), Expert expander content grouped into matching tinted sub-panel cards with caption-chip readouts, Mica BaseAlt backdrop and Fluent type ramp；SDK/編譯與實機驗證待目標環境 | SPEC-0010 / SPEC-0011 / SPEC-0014 / SPEC-0017 / SPEC-0019 / SPEC-0020 / SPEC-0021 / SPEC-0023 / SPEC-0024 / SPEC-0008 / SceneProfile / IPC v1 |
| `apps/engine-preview/` | C++ local named-pipe host、control worker loop、explicit WASAPI output opt-in | user-space control smoke；預設不開 sink；`--enable-wasapi-output` 只啟動既有 shared-mode worker，仍不含 driver 或完整實體播放證據 | SPEC-0004 / SPEC-0017 |
| `apps/control-model-engine-smoke/` | C# control model 對 Engine Preview 的跨程序 smoke | 驗證 Connect、音量狀態往返與 Game SceneApply Ack；輸出只在 `.local/` | SPEC-0004 / IPC v1 |
| `asio/` | Hibiki ASIO bridge、可選原生 COM transport 與 DAW lane | stream model + optional unsigned DLL target | SceneProfile / distribution profile |
| `vst-host/` | Out-of-process VST3 host | trusted/certified model + Job Object supervisor + tested frame codec/named-pipe boundary + bounded passthrough worker + optional pinned SDK factory catalog/one-main-bus processor/worker executable + bounded parameter frame + supervisor Hello/ProcessBlock exchange + fixed-capacity parameter timeline/lane session + bounded supervisor parameter-timeline editor transaction model + scheduler-bound edit sessions with snapshot readback + canonical fail-closed timeline JSON persistence + fixed-capacity per-timeline file store with strict filename-safe IDs + store-to-scheduler sync adapter + sorted timeline-ID/binding-view introspection + selection-aware supervisor editing surface composing editor with non-owned store handle + fail-closed remove_selected() (refuses while detached/unselected/edit-session-open; removes via store and clears state) + bounded surface-level history introspection/clear_history + redacted sandbox diagnostic reason codes/incident summary + bounded Scene automation store/scheduler with per-lane back-pressure + private identity/version-checked plugin state store + explicit 16-rule state migration registry + fixed-capacity Scene state coordinator + bounded multi-bus/side-chain admission validator + EngineControl preflight adapter + third-party state compatibility review contract + PluginHostModel quarantine wiring + latency alignment/delay primitive + fixed-capacity latency graph commit transaction；UI automation editor、實際 side-chain/multi-bus worker process、第三方 certification pending | SPEC-0008/0012 / plugin contract |
| `extensions/` | Chrome/Edge MV3 tab capture + HIBT AudioWorklet packetizer、loopback WebSocket、TabCaptureQueue SPSC、graph lane adapter | user-gesture capture source + bounded queue + shared graph/Group Master adapter；noise-reduction model 待接 | SPEC-0009 / browser lane |
| `installer/` | GPL bootstrapper、driver transaction、rollback | PowerShell source bootstrapper with dry-run, traversal-safe payload hashes and ReleaseManifest signature/SBOM metadata gate；signed package pending | release manifest |
| `sdk/` | driver／engine 的 Apache-2.0 C ABI 與 schema boundary | driver-control-v1 + fixed little-endian driver-control endpoint-state/volume packet with non-zero request-ID validation + fixed driver-stream packet encode/validate/payload ABI with non-zero sequence/generation validation + ASIO shared-memory transport rejecting non-zero reserved bytes | driver-control-v1 / driver-control-transport-v1 / driver-stream-transport-v1 / asio transport |
| `schemas/` | IPC、Graph、Scene、custom Scene cards、physical device catalog/snapshot、device switch request、volume、AudioSession、PEQ、ISO status、release JSON schemas | 初始骨架 | versioned v1 |
| `build/` | 固定 VS／SDK／WDK 與可重建條件 | 初始鎖定 | toolchain-lock.yml |
| `tools/` | doctor、probe、live Windows device catalog／WASAPI handoff／audio-session probes、verify、docs/source-only CI、license、driver 與 release policy gates | source-only workflow publication gate 會拒絕 artifact/package/release upload、signing permission 與 tracked binary；build-driver.ps1 從腳本位置錨定 repo root、輸出鎖在 .local 並提供 -SelfTest，且在寫入 object/package 輸出目錄前對 .local 外／reparse point／非目錄 target fail-closed（含離線 self-test）；build-preview.ps1 與 build-driver.ps1 的既有路徑檢查只把 ObjectNotFound 視為不存在，其他檢查錯誤在建置／寫入前 fail-closed（含離線 self-test）；probe-environment.ps1 在寫入 .local/context.json 前套用同一 path-guard 模式（含離線 self-test）；handoff-check 接受 TBD pre-claim drafts；PR verify 只驗證 branch-owned issue，全域 handoff 健康由 handoff-audit workflow 依事件與排程稽核；docs-check 強制 ADR frontmatter 欄位並 fail-closed 驗證 markdown 相對連結（回報檢查數量）；run-preview.ps1、winui-a11y-smoke.ps1、build-preview.ps1、build-engine-preview.ps1 與 engine-preview-smoke.ps1、control-model-engine-smoke.ps1、live-audio-session-check.ps1、live-process-loopback-check.ps1、live-wasapi-handoff-check.ps1、live-device-catalog-check.ps1 對 .local 外／reparse point／非檔案或非目錄 target fail-closed（self-test 共廿三案例以上），其中 live-device-catalog-check.ps1 的既有路徑檢查亦只接受 ObjectNotFound 為不存在；live-system-volume-check.ps1 與 live-session-volume-check.ps1 在 build／寫入／啟動前對 build roots 與 engine executable 套用同一 path-guard 模式（含離線 self-test）；verify -Clean 對 build-root 以外的刪除目標 fail-closed（含 reparse point 防護）；probe-environment、build-engine-preview、engine-preview-smoke、live-wasapi-handoff-check、live-system-volume-check 與 live-session-volume-check 的既有路徑屬性檢查只把 ObjectNotFound 視為路徑不存在，其他檢查錯誤一律 fail-closed；control-model-check 與 verify 在建置輸出寫入前新增同級 fail-closed 驗證（self-test 各自擴充離線案例）；GitHub Issue intake 由結構化表單強制（空白 Issue 停用，保留安全通報聯絡與維護者逃生口）；live probes 僅 opt-in 且輸出不含私人 endpoint/session identity | AGENTS.md |

`SceneCatalogV1` 位於 `src/hub/` 的 control plane，保存最多 32 個自定義 Scene definition，
並由 `EngineControlWorkerV1` 以 output-group exact-match resolver 接入既有
Validate → Prepare → Commit；RT graph 只接收 immutable snapshot。

即時執行緒只能讀 immutable snapshots；control plane 以 Validate → Prepare →
Commit/Rollback 修改 graph。跨子系統不得硬編碼個人裝置名稱、Endpoint ID 或磁碟機路徑。
